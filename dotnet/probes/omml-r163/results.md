# omml-r163 — OMML formulas on `ABCD-FE-01-00 Flight Envelope`

Round measured 2026-09-24 in `/home/user/libreoffice-core`, branch
`claude/renderer-comparison-artifact-m1g0wy`.

| | |
|---|---|
| ours | `dotnet/tools/Paperless.Cli/bin/Release/net10.0/linux-x64/Paperless.Cli` (as found, not rebuilt) |
| reference | `/opt/libreoffice26.2/program/soffice` — **26.2.4.2** `0229ac93fcf0d7cbc6376066c6f35021cef002dc` |
| document | `/home/user/sample-files/words/ceiling-001/docx/ABCD-FE-01-00 Flight Envelope - v1 08.03.16.docx` |
| C++ read | this checkout, which is `27.2.0.0.alpha0+` and **not** the binary's source — every arm below is a hypothesis from source confirmed by measurement against 26.2.4.2 |

`PATH` was pointed at `/opt/libreoffice26.2/program` before every `lo-convert.sh`
call. `lo-convert.sh` takes `soffice` from `PATH` and does **not** pin a version;
`/usr/bin/soffice` here is 24.2.7.2 and is the wrong reference.

---

## 0. What the formulas are: **OMML**, not OLE and not images

Settled before anything else, because everything depends on it.

`word/document.xml` holds **54 `m:oMath`** and **22 `m:oMathPara`** (108 and 44 tags).
Construct census over the whole part: `m:sSub` **77**, `m:f` **23**, `m:rad` **10**,
`m:sSup` **2**, `m:d` **2**. There are **zero `w:object`** elements, **zero**
occurrences of the string `Equation`, and only **2 `w:drawing`** (the two JPEGs in
`word/media`). So: no Microsoft Equation 3.0 OLE, no MathType, no pictures of
formulas — pure inline OMML in the `…/2006/math` namespace. `[bin]`

A representative one, verbatim from the file:

```xml
<m:oMath><m:sSub><m:sSubPr><m:ctrlPr><w:rPr><w:rFonts w:ascii="Cambria Math"
 w:hAnsi="Cambria Math"/><w:i/></w:rPr></m:ctrlPr></m:sSubPr>
 <m:e><m:r><w:rPr><w:rFonts w:ascii="Cambria Math"/></w:rPr><m:t>c</m:t></m:r></m:e>
 <m:sub><m:r>…<m:t>L⁡_profile_max</m:t></m:r></m:sub></m:sSub>…</m:oMath>
```

## 1. Which page

The user said page 6. Both renderings put the **inline** formulas
(`c_{L_profile_max}=1.35`, `δ_f=40 deg`, …) on page 6, so the report is exact.
The *display* formulas — the stall-speed radicals and fractions — are the
reference's **page 8** and our **page 7**; the document paginates 16 against 14. `[bin]`

## 2. Root cause

**OMML is not implemented anywhere in `dotnet/src`.**
`git grep -il 'omath\|sSub\|officeDocument/2006/math' -- dotnet/src` returns
nothing but a font-name list in `SystemFontResolver.cs`. `[src]`

What draws the flat text is a **missing namespace guard** in the layout walk.
`DocxLayoutSource.RunWalker.Append` (`dotnet/src/Paperless.WordProcessing/Ooxml/
DocxLayoutSource.cs`:1912) switches on `child.Name.LocalName` alone — no
`NamespaceName` test anywhere in the method. So:

* `m:oMath`, `m:oMathPara`, `m:sSub`, `m:e`, `m:sub`, `m:f`, `m:num`, `m:den`,
  `m:rad`, `m:deg`, `m:d`, `m:nary`, `m:m`, `m:mr` all fall to
  `default: Append(child, depth + 1)` (`:2244`) and are walked straight through;
* `m:r` matches `case "r":` (`:2235`) and is treated as an ordinary `w:r` —
  and because an OMML run's formatting really is a `w:rPr` child, its
  `Cambria Math` font table entry is even picked up, which is why the flattened
  text is drawn in a substituted serif;
* `m:t` matches `case "t" when !Suppressed: Emit(child.Value);` (`:2065`).

The result is that every formula is emitted as **the concatenation of its `m:t`
leaves, in document order, at the body's size, on the body's baseline**. Nothing
generated by the construct itself — the fraction bar, the radical sign and its
vinculum, the delimiters (`m:begChr`/`m:endChr` are *attributes*), the n-ary
operator (`m:chr`), the accent (`m:chr`) — exists as an `m:t`, so all of it is
silently absent. `[src]`, `[bin]`

The **extraction** path is guarded and therefore does the opposite:
`DocxContentReader.ReadRuns` / `ReadRun` (`DocxContentReader.cs`:366, :446) both
`continue` on `child.Name.NamespaceName != OoxmlNamespaces.WordprocessingML`, so
`paperless extract` drops every formula entirely. Measured on the fixture: the
extracted line reads `sub inline: before  after (bare subscript).` `[bin]`
So the two paths disagree — the drawing path draws text the extraction path
cannot see.

### What the reference does instead

`OOXMLFastContextHandlerMath::process` (`sw/source/writerfilter/ooxml/
OOXMLFastContextHandler.cxx`:2517-2559) creates an **embedded StarMath object**
(`SO3_SM_CLASSID`) per `m:oMath` and hands the buffered OMML to
`readFormulaOoxml`; `m:oMathPara` additionally carries its `m:jc` through
`mnMathJcVal`, defaulting to **centre** (`:185-187`). The formula is then laid
out by `starmath/` with `SmFormat`'s defaults, not by Writer's text layout. `[src]`

The tarball ships Math — `libsmlo.so`, `libsmdlo.so`, `share/registry/math.xcd`
are all present under `/opt/libreoffice26.2` — and 26.2.4.2 does render every
construct. `[bin]`

## 3. The numbers

### 3.1 The reference's subscript rule, and why "20 %" is the wrong constant

`SmFormat::SmFormat` (`starmath/source/format.cxx`:32, 40-41) sets
`vSize[SIZ_INDEX] = 60` and `vDist[DIS_SUBSCRIPT] = vDist[DIS_SUPERSCRIPT] = 20`. `[src]`

`SmSubSupNode::Arrange` (`starmath/source/node.cxx`:1143-1225) then, for `RSUB`:

1. scales the subscript to `SIZ_INDEX/100` of the body (`:1188`);
2. `nDist = nOrigHeight * DIS_SUBSCRIPT / 100` (`:1203-1204`);
3. aligns with `RectVerAlign::Bottom`, and `SmRect`'s `nAlignB` **is the
   baseline** (`starmath/source/rect.cxx`:196), so this is a *baseline* align;
4. adds `nDist`;
5. **clamps**: `nDelimLine = SmFromTo(GetAlignB(), GetAlignT(), 0.4)` with
   `nAlignT = nBaseline - 0.75 * nFontHeight` (`rect.cxx`:191), i.e. the
   delimiter line is `baseline − 0.30 × H`; if the subscript's *top* is above it
   the whole subscript is pushed down (`node.cxx`:1209-1211).

So the drop is `max(0.20·H, ascent_em·0.60·H − 0.30·H)`, and **the clamp wins**:
for Liberation Serif (`ascent/em = 1825/2048 = 0.89111`) the second arm is
`0.2347·H` against the first's `0.2000·H`. Anyone implementing the stated 20 %
alone lands the subscript **17 % too high**.

Measured, four independent cases, from PyMuPDF per-character `origin` (the
baseline the writer's `Td`/`Tm` stated — *not* `pdftotext -bbox`, whose ink box
carries a constant per-face offset that looks exactly like this defect):

| site | base pt | sub pt | ratio | measured drop | predicted | err |
|---|---:|---:|---:|---:|---:|---:|
| fixture `sub` inline | 11.90 | 7.19 | 0.6042 | **2.780** | 2.837 | +0.057 |
| fixture `fracsub` numerator | 12.03 | 7.19 | 0.5977 | **2.790** | 2.798 | +0.008 |
| real doc p6 `δ_f` | 11.96 | 7.226 | 0.6042 | **2.782** | 2.851 | +0.069 |
| real doc p6 `c_{L_profile_flapped_max}` | 11.995 | 7.197 | 0.6000 | **2.800** | 2.815 | +0.015 |

Residual ≤ 0.07 pt (2.5 %), consistent with starmath working in 1/100 mm and the
OLE frame being scaled by ~0.997. `[bin]` confirming `[src]`.

**Ours draws the subscript at the base size on the base baseline**: ratio 1.000,
drop 0.000, at all four sites. `[bin]`

### 3.2 Other constants read off the fixture (reference, base ≈ 12 pt)

| quantity | measured |
|---|---|
| superscript size / baseline rise | 7.19 pt (0.604) / **−5.96 pt** |
| fraction numerator→denominator baseline distance | **15.05 pt** (bare), **16.85 pt** when the terms carry subscripts |
| radical sign drawn size | **13.28 pt** against a 12.00 body (stretched) |
| n-ary `∑` size | **17.39 pt** = 1.448 × body (`DIS_OPERATORSIZE = 50`) |
| `∑` limits size / rise / drop | 7.17 pt (0.597) / **−15.40** / **+9.02** |
| `∫` size, limits beside the sign | **17.50 pt**, **−16.30** / **+9.85** |
| stretched `(` `)` round a fraction | **19.27 pt**; round a 2×2 matrix **18.02 pt** |
| matrix row baselines | **+0.00 / +2.90** |
| formula base size vs the 11 pt body | **≈ 12 pt** — StarMath's own base size, not the paragraph's |

### 3.3 Drawn geometry

Vector drawings on the fixture: **reference 20, ours 0.** Fraction bars,
radical vinculums and the accent overbar are strokes, and we draw none of them. `[bin]`

## 4. Per-construct verdict table

Fixture `dotnet/tests/corpus/features/words-formula-complexity.docx`, inline rung
of each construct. `sizes` are the distinct glyph sizes inside the formula;
`rows` the distinct baselines relative to the formula's own base. Raw table in
`ladder.tsv`, generated by `ladder.py`.

| construct | ref sizes / rows | ours sizes / rows | ref glyphs | ours glyphs | verdict |
|---|---|---|---|---|---|
| `m:sSub` bare subscript | 11.90 / 7.19 — 2 rows `+0.00/+2.78` | 11.00 — 1 row | `x1` | `x1` | **FAIL** — no size drop, no baseline drop |
| `m:sSup` superscript | 11.90 / 7.19 — 2 rows `−5.96/+0.00` | 11.00 — 1 row | `2x` | `x2` | **FAIL** |
| `m:sSubSup` | 11.91 / 7.23 — 3 rows | 11.00 — 1 row | `2x1` | `x12` | **FAIL** |
| `m:sPre` pre-sub/sup | 11.91 / 7.23 — 3 rows | 11.00 — 1 row | `2X1` | `12X` | **FAIL** |
| `m:f` fraction | 12.03 — 2 rows `+0.00/+15.05` + bar | 11.00 — 1 row, no bar | `ab` | `ab` | **FAIL** — not stacked, no bar |
| `m:f` with subscripted terms | 12.03 / 7.19 — 4 rows | 11.00 — 1 row | `a1b2` | `a1b2` | **FAIL** |
| `m:rad` square root | 13.28 / 12.00 — 2 rows + vinculum | 11.00 — 1 row | `x√` | `x` | **FAIL** — **√ absent** |
| `m:rad` with `m:deg` over a fraction | 20.49 / 11.96 / 7.18 — 4 rows | 11.00 — 1 row | `a3b√` | `3ab` | **FAIL** — √ and bar absent |
| `m:nary` `∑` with limits | 17.39 / 12.01 / 7.17 — 5 rows | 11.00 — 1 row | `nx∑ii=1` | `i=1nxi` | **FAIL** — **∑ absent**, limits inline |
| `m:nary` `∫` limits beside sign | 17.50 / 12.00 / 7.20 — 4 rows | 11.00 — 1 row | `∞f(t)dt∫0` | `0∞f(t)dt` | **FAIL** — **∫ absent** |
| `m:d` nested delimiters | 19.27 / 19.23 / 12.00 — 5 rows | 11.00 — 1 row | `ba+[]()c` | `a+bc` | **FAIL** — **( ) [ ] absent** |
| `m:acc` overbar | 11.91 — 1 row + stroke | 11.00 — 1 row, no stroke | `x` | `x` | **FAIL** — bar absent |
| `m:func` | 11.98 / 8.53 — 2 rows | 11.00 — 1 row | `()sin2θ` | `sin2θ` | **FAIL** — **( ) absent** |
| `m:m` 2×2 matrix | 18.02 / 11.98 — 3 rows | 11.00 — 1 row | `ab()cd` | `abcd` | **FAIL** — one row, no delimiters |
| mixed frac/rad/sub | 13.79 / 12.03 / 7.19 — 5 rows | 11.00 — 1 row | `W√MTOMρS0w` | `WMTOMρ0Sw` | **FAIL** |
| `m:oMathPara` display, all 15 | own centred block, tall | one left-aligned body line | — | — | **FAIL** — not centred, no vertical room |

**15 of 15 constructs fail, inline and display.** Nothing in this area is
partially right, so there is no rung of the ladder that isolates a smaller bug:
the ladder's value is the per-construct expected geometry in §3.2, which is what
an implementation has to hit.

## 5. What the gate can and cannot see

| | ref | ours |
|---|---:|---:|
| real document, pages | 16 | **14** |
| real document, alphanumerics | 18480 | 18409 (−0.38 %, **inside** the max(2 %, 15) band) |
| fixture, pages | 2 | **1** |
| fixture, alphanumerics | 967 | **967** |

The fixture's text column is **exact** while every formula on it is wrong. So
this defect is invisible to the alphanumeric column by construction — flattening
an OMML tree preserves every `m:t` character — and only `pages` catches it, and
only where a display formula's height changes the pagination. `probes/words-e1-01`
and `TODO.raster-ceiling.md` both reached the text column and stopped there.

## 6. Refuted — do not re-test

1. **"LibreOffice draws OMML with no extractable text at all, so this document is
   a raster ceiling and `words` cannot be won."**
   (`dotnet/TODO.raster-ceiling.md`, *A second kind of ceiling: OMML formulas*;
   `probes/words-extra-01/results.md`.) **False against 26.2.4.2.** That was
   measured against `/usr/bin/soffice` 24.2.7.2, a distro build with **no
   `libreoffice-math` installed**, which reserved the space and drew nothing. The
   TDF tarball ships Math, and its PDF's text layer carries the formulas —
   `pdftotext` of page 8 gives
   `c L _ clean ⁡_ max = 0.85 * 1.35 = 1.15`, and PyMuPDF reports the subscript
   spans in `LiberationSerif-Italic` at 7.2 pt. The reference draws **more**
   formula text than we do, not less. Any ceiling reasoning about this document
   should be re-derived. `[bin]`
2. **"The formula counts are 87 `m:oMath` and 33 `m:oMathPara`."** (Both files
   above.) The part holds **54** and **22**. The higher figures are `grep -c` on
   the *tag* occurrences (open + close) halved wrongly, or an older revision;
   counted here by matching `<m:oMath>`/`<m:oMathPara>` open tags. `[bin]`
3. **"`m:oMathPara` can wrap a construct directly."** It cannot — 26.2.4.2 draws
   **nothing at all**, silently, for `<m:oMathPara><m:sSub>…</m:sSub></m:oMathPara>`.
   The first cut of the fixture did that and its display half was blank; the
   reference's own files always write `<m:oMathPara><m:oMath>…`. A fixture bug
   here is indistinguishable from a renderer one. `[bin]`
4. **"Our page 6 explodes to one word per line."** It does not. `pdftotext`'s
   column detection makes it look that way; read out of the content stream the
   paragraph is **3 justified lines in both renderings** (ours at y = 673.2 /
   686.7 / 700.1, the reference at 677.2 / 692.4 / 707.5). What is real is that
   our flattened `configurationc L _profile_flapped_max=2.15,` is one unbreakable
   89.7 pt token, so the justification gaps on that line are much wider than the
   reference's. `[bin]`
5. **`pdftotext -bbox` for this question.** Its `yMin` is a font-descriptor ink
   box; the whole of §3.1 would be off by a per-face constant. Every vertical
   figure here is a PyMuPDF character `origin`.

## 7. Files

```
make-fixture.py   authors dotnet/tests/corpus/features/words-formula-complexity.docx
ladder.py         the per-construct table; ladder.tsv is its output
spans.py          per-span baseline/size/font dump for one page
paths.txt         vector-drawing counts, both sides
unz/              the target document, unzipped (read-only source)
ref/ ours/        the real document rendered both ways
fx-ref/ fx-ours/  the fixture rendered both ways
png/              the pages read in this round
```

The fixture is left **uncommitted** in the working tree, as is everything here;
no C# source was touched and nothing was built.

## 8. Addendum — the display formulas on the real document's page 8

Read the same way, and cleaner than the fixture because the base size is exact:

```
187.350  258.201  12.000  LiberationSerif         '2 '          <- numerator
209.346  258.201   7.200  LiberationSerif-Italic  ' MTOM'       <- its subscript
135.900  266.851  12.000  LiberationSerif-Italic  'V'           <- main baseline
143.220  266.851   7.200  LiberationSerif-Italic  ' S'
168.150  275.101  12.000  LiberationSerif-Italic  'ρ'           <- denominator
158.800  276.701  21.606  OpenSymbol              '√'
268.800  275.751  20.552  OpenSymbol              '√'
```

* base **12.000 pt exactly**, subscript **7.200 pt exactly** — a flat 60 %, and
  **12 pt regardless of the 11 pt body**: `SmFormat::aBaseSize` is
  `o3tl::convert(12, pt, …)` (`starmath/source/format.cxx`:25) and the run's own
  `w:sz` does not reach it. `[bin]` confirming `[src]`
* numerator baseline **−8.65 pt**, denominator **+8.25 pt** from the main
  baseline; numerator→denominator **16.90 pt** at a 12 pt base.
* the radical sign is drawn from **OpenSymbol**, not from the text face, at
  20.55 and 21.61 pt — stretched per instance to its radicand. `[bin]`

## 9. Proposed patch

Not applied; no C# was modified and nothing was built.

### 9.1 The namespace constant

`dotnet/src/Paperless.Ooxml/OoxmlNamespaces.cs`, beside `WordprocessingML` (:16):

```csharp
/// <summary>Office MathML, the namespace an OOXML formula's elements live in.</summary>
public const string OfficeMath = "http://schemas.openxmlformats.org/officeDocument/2006/math";
```

### 9.2 The seat

`dotnet/src/Paperless.WordProcessing/Ooxml/DocxLayoutSource.cs`,
`RunWalker.Append(XElement element, int depth)` at **:1912**.

*Current behaviour.* The `foreach (XElement child in element.Elements())` loop
switches on `child.Name.LocalName` with **no namespace test**, so an OMML subtree
is walked as though it were WordprocessingML: `m:r` takes `case "r":` (:2235),
`m:t` takes `case "t" when !Suppressed:` (:2065), and every structural element
(`m:oMath`, `m:sSub`, `m:e`, `m:sub`, `m:f`, `m:num`, `m:den`, `m:rad`, `m:d`,
`m:nary`, `m:m`) falls to `default: Append(child, depth + 1);` (:2244). A formula
is therefore drawn as the concatenation of its `m:t` leaves at the paragraph's
size on the paragraph's baseline, and everything the construct *generates* is
absent.

*Corrected behaviour.* First statement inside the loop:

```csharp
if (child.Name.NamespaceName == OoxmlNamespaces.OfficeMath)
{
    AppendMath(child, depth + 1);
    continue;
}
```

`case "r":` and `case "t"` keep their meaning for WordprocessingML alone.
**Do not add the guard on its own**: with no `AppendMath` it turns wrong text
into no text, which loses the searchable formula content that is currently this
renderer's one advantage here.

### 9.3 `AppendMath` — stage 1, in the same file, no new layout engine

A recursive walk over the math subtree carrying `(Escapement escapement,
double scale)`, emitting through the existing `Emit`. `Escapement`
(`dotnet/src/Paperless.WordProcessing/Layout/Escapement.cs`) already carries
`Proportion` (size %) and `Percent` (offset as a percentage of the font's
*height*, ascent + descent + line gap) and is the right carrier.

| element | stage-1 handling |
|---|---|
| `m:oMath` | recurse; set the run's size to **12 pt** rather than the paragraph's (§8) |
| `m:oMathPara` | recurse, and centre the paragraph unless `m:oMathParaPr/m:jc` says otherwise — `OOXMLFastContextHandler.cxx`:185-198 defaults it to `CENTER` |
| `m:r`, `m:t` | as today (`w:rPr` inside `m:r` is genuinely WordprocessingML) |
| `m:e` | recurse at the current escapement |
| `m:sub` | recurse with size × **0.60** and baseline **lowered** by `max(0.20·em, ascent_em·0.60·em − 0.30·em)` |
| `m:sup` | same size, baseline **raised** by the superscript arm of the same rule |
| `m:d` | emit `m:dPr/m:begChr@m:val` (default `(`) before the `m:e`s, `m:endChr` (default `)`) after, `m:sepChr` (default `\|`) between |
| `m:rad` | emit `U+221A` before the radicand; emit `m:deg` first at 0.60 size raised, unless `m:degHide` is `1` |
| `m:nary` | emit `m:naryPr/m:chr@m:val` (default `U+222B`) at **1.45×** size; emit `m:sub`/`m:sup` at 0.60 lowered/raised unless `m:subHide`/`m:supHide` |
| `m:f` | emit numerator, then `/`, then denominator — until stage 2 stacks them |
| `m:acc` | emit the `m:e`, then `m:accPr/m:chr@m:val` (default `U+0302`) as a combining mark |
| `m:func` | emit `m:fName` then `m:e` |
| `m:m`, `m:mr` | emit rows separated by a line break, cells by a space |
| `m:*Pr`, `m:ctrlPr` | skip |

The subscript constant is the one thing to get right and the one a reading of the
specification gets wrong. `SmFormat` states `DIS_SUBSCRIPT = 20`
(`starmath/source/format.cxx`:41), but `SmSubSupNode::Arrange`
(`starmath/source/node.cxx`:1199-1211) then clamps the subscript's *top* to the
delimiter line at `baseline − 0.30 × em`, and **for every Latin face the clamp
wins**: with Liberation Serif's `ascent/em = 0.89111` the drop is `0.2347·em`,
not `0.2000·em`. Implementing the stated 20 % puts the subscript **17 % too
high** — 0.42 pt at 12 pt, four times a rendering comparison's tolerance.
Confirmed against 26.2.4.2 at four sites, worst error 0.07 pt (§3.1).

Expressed through `Escapement`, whose `Percent` is of the font *height* rather
than the em: `Percent = -(drop / fontHeight) × 100`, which is **−20** for
Liberation Serif and is **not** a face-independent constant — compute it from the
face rather than hard-coding it. `Proportion = 60` (StarMath's `SIZ_INDEX`), not
the 58 that `Escapement.AutomaticProportion` carries for `w:vertAlign`.

### 9.4 Stage 2 — the part that needs a box model

Stacking a fraction over a rule, drawing a radical's vinculum, putting an n-ary
operator's limits above and below it, laying a matrix out in rows and columns,
and stretching a delimiter to its content all need a 2-D math box with its own
baseline, and cannot be done by emitting into the paragraph's character stream.
§3.2 and §8 give the target geometry for each, measured; `starmath/source/node.cxx`
`SmBinVerNode::Arrange` (:831), `SmRootNode::Arrange` (:725) and
`SmBinHorNode::Arrange` (:786) are the rules, and `starmath/source/format.cxx`:30-60
the constants. The reference draws its radical sign from **OpenSymbol**, stretched
per instance.
