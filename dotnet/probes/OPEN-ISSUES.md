# Open issues register

**Every entry here ends in one of two states.** Either it is *fixed in this tree*, or it is
*established as a LibreOffice 26.2 defect we deliberately do not reproduce*. "Known and tolerated"
is not a third state; anything genuinely not worth working is recorded as **NOT WORK** with the
measurement that says so, which is a form of the second.

Rounds add to this file and change entries' states. It supersedes hunting through ninety
`results.md` files for "left with its seat".

Reference is **26.2.4.2** (`/opt/libreoffice26.2/program/soffice`) throughout.

---

## Established LibreOffice 26.2 defects — closed, do not reproduce

| # | what | evidence |
|---|---|---|
| L1 | **On the draw layer the reference measures in one face and draws in another.** `FontAttribute` has no family-class field, so `getVclFontFromFontAttribute` rebuilds at `FAMILY_DONTKNOW` while the DX array was measured with the class on. `fc-match "Helvetica:bold"` → Liberation; `fc-match "Helvetica,sans:bold"` → DejaVu. | `probes/title-font-r92`; seventh confound in `CLAUDE.md`. Our output equals the reference's own class-less branch to 0.105 pt on a 155 pt line. **And it reaches the binary formats, which r92's zip-only census could not see**: a `.ppt` states the same class in the top nibble of `lfPitchAndFamily` and `svdfppt.cxx`:410-435 maps it to `FAMILY_SWISS`/`FAMILY_ROMAN`. Round 115 rendered all **51** legacy PPT-family corpus documents twice with 26.2.4.2, as authored and with every family nibble cleared: **20 state** a splitting class-ful family and **3 have it reach drawn text** — `architecture6.ppt` (O57), `RRM-training-syllabus-…` (O15's own witness, rediscovered blind by the instrument) and `pres_ioc_phuket.ppt`, which is new and is `Times` at `FF_ROMAN`. `probes/ppt-spacing-r115`. |
| L2 | **`a:normAutofit/@fontScale` is not honoured.** Twenty one-attribute variants over two decks, drawn `Tf` sizes read from the reference's own PDF: 90000, 50000, 25000, absent, and each of those beside a stated `lnSpcReduction`, all draw identical sizes. Only removing the element moves anything; what is drawn is `constScaleLevels`' own row. | `probes/slides-ink-r94`. The trap: PowerPoint's stored value sits *near* the search's answer, so a witness measured at its own value cannot separate the hypotheses. Closed **without** code change. |
| L3 | **A bar-of-pie's bar label is wrapped into a frame one hundredth of a millimetre wide, so it is drawn one character per line.** `PieChart::createBarLabelShape` computes `nTextMaximumFrameWidth = ceil(0.8 × (m_fBarRight − m_fBarLeft))` = `ceil(0.4)` = **1** (`chart2/source/view/charttypes/PieChart.cxx`:780-782); `m_fBarLeft` 0.75 and `m_fBarRight` 1.25 (`PieChart.hxx`:267-269) are **unit-circle radii** and are never transformed to screen, while the parameter is a maximum frame width in hundredths of a millimetre. 26.2.4.2's own rendering of `028_Unit_Circle_Chart_Optimized_Graph_83d9c756.docx` draws `Branch 3 Stem 6 Leaf 15 4%` as **26 single-character lines** at x ≈ 437.6-440.1, 10.47 pt apart — a 270 pt column on a 370 pt diagram, which is what `VDiagram::adjustInnerSize` is then charged for and the whole of why the reference's of-pie is 271.29 pt square where a sane wrap gives 370.66. **Not reproduced.** | `probes/chart-inner-r110` §3; measured in the reference's own PDF, not inferred from the source. Cost of declining it: one corpus document, `028…docx`, whose summed `\|ink\|%` goes 3.47 -> 3.58 while its diagram moves 7.02 pt right and keeps its size. |
| O57 | **The extra width is INVENTED, not stated — 26.2.4.2 draws this `.ppt`'s Helvetica in Liberation Sans at DejaVu Sans' advances.** This is `L1` reaching the **binary** format, and it closes the seat: a `.ppt` cannot state character spacing at all. The reference's own reader, `PPTStyleTextPropReader::ReadCharProps` (`svdfppt.cxx`:5096-5185), reads the whole of a `TextCFException` — flags, `cfTypeface`, `cfFEOldTypeface`, `cfANSITypeface`, `cfSymbolTypeface`, `cfSize`, `cfColor`, `cfPosition` — and there is no tracking field in the record and no `PPT_CharAttr_*` for one. What the deck *does* state is `Helvetica` at `lfPitchAndFamily = 0x22` (`FF_SWISS`), which `svdfppt.cxx`:410-435 and :2186 put on the `vcl::Font`, and which `FontConfigManager::Substitute` turns into a second `FC_FAMILY` of `sans`. Per glyph, 767 of 767 adjacent-pair advances at 14 pt are nearer **DejaVu Sans Bold**'s `hmtx` than Liberation's (mean 0.00107 em against 0.07278) while every ink bbox, the embedded subset and the PDF `/Widths` are **Liberation Sans Bold**'s — the gap carried entirely in negative per-glyph `TJ`. Round 113's *heading, whole, +14.90* is withdrawn: it compared 39 characters against 40 (the reference draws a trailing space as its own `Tj`), and the heading's own adjustments are ±1/1000 em. | `probes/ppt-spacing-r115`. **Confirmed twice, on the deck itself.** Two byte-length-preserving one-attribute variants rendered by 26.2.4.2 — class `0x22`→`0x02`, name untouched; and name `Helvetica`→`Arial`, class untouched — each collapse the reference onto this tree to **0.042–0.069 pt** over five strings (`Description` 89.185 → 77.073 against our 77.031; `Disadvantages` 116.337 → 98.903 against 98.834), which is the `floor(hmtx × 1000/upem)` floor. Over all 31 pages the run-set symmetric difference goes **268 → 52** and the worst common-run origin shift **322.07 → 1.60 pt**, with run counts identical on every page. **Reach, measured rather than censused**: each of the 51 legacy PPT-family corpus documents rendered twice by 26.2.4.2, as authored and with every `FontEntityAtom`'s family nibble cleared — **3 move** (`architecture6` 0.4389 → 0.0441, `RRM-training-syllabus-…` 0.2150 → 0.0000, `pres_ioc_phuket` 0.1593 → 0.0005, as the fraction of glyph slots carrying a `\|TJ\| ≥ 20/1000 em`); **20 of 51 state** such a family, so stating one is four times commoner than its reaching drawn text. 0 page counts, 0 alphanumeric counts, no gate column can see it. **No code changed and none should.** |
| O31 | **26.2.4.2 never draws a live 3-D chart scene as vectors, in any stream, and the corpus's one apparent counter-example was not a chart.** Confirmed twice. *Source*: `ScenePrimitive2D::create2DDecomposition` (`drawinglayer/source/primitive2d/sceneprimitive2d.cxx`:219-548) caps the discrete area at `Quadratic3DRenderLimit`, runs `aZBufferProcessor3D`, and ends unconditionally in a `BitmapPrimitive2D` (:530-534) — there is no branch emitting 2-D geometry, `getGeometry2D()` (:550) is called only by the contour extractor, and no vcl processor handles `PRIMITIVE2D_ID_SCENEPRIMITIVE2D`, so a scene reaches every device through that one decomposition. Writer, Calc and Impress share it, so N29's *"why does Impress differ"* has no referent. *Binary, one variable*: `undp_presentation_revised_17_may.ppt` page 19 draws **362 and 309 vector path items** in the chart frame; the same binary rendering the reference's own flat ODP of the same file draws a **635 x 155 raster** at (40.1, 344.9)-(242.3, 397.9) and **no paths**. Both files resolve the chart to `chart:class="chart:circle"` with `chart:three-dimensional="true"` and a `dr3d:transform`, as does the DOCX witness, so the model is not the difference — the paths are PowerPoint's **stored replacement metafile** for the `Excel.Sheet.8` OLE, i.e. Excel's own drawing, and vanish the moment the chart is loaded live. So r102's *"one of 947 documents is measurable"* is **withdrawn**: the implementation target count is **0 of 947**. `021_Unit_Circle_Chart_3D_Pie_Chart`'s 3.47 `\|ink\|%` stands and is unreachable — no vector geometry matches a Z-buffer raster and no corpus witness could tell us whether an implementation was right. Closed **without** code change. | `probes/chart-3d-r110`, `page19.tsv`; supersedes `probes/chart-resid-r99` §2 and `probes/chart-smooth-r102` §2. |
| L4 | **A `TOC` field's `\t` switch names paragraph styles, and 26.2.4.2 then discards the whole direct `w:pPr` of every paragraph in those styles.** ECMA-376 Part 1 SS17.7.2 puts direct paragraph formatting last in the precedence chain, so it wins; Word honours it and this tree honours it. Isolated to a two-paragraph document differing in one field switch: with `TOC \h \z \t "Heading 2,1,Heading 3,2"` the reference leaves the style's 6.35 pt after a `Heading3` paragraph that states `w:spacing w:after="0"`, and with `TOC \o "1-3"` or a `\t` that does not name the style it leaves 0.35 pt, as we do. Not only the spacing: a direct `w:jc` and a direct `w:spacing w:before` are lost as well. **Reach 7 of 272 corpus `docx`, 134 paragraphs**, against a base rate of 50 documents carrying any `TOC` field and 19 carrying a `\t` template. **The cost of declining it** is the ten wrong-orientation pages of `24-25_FAA_Holdover_Tables.docx` — removing the `\t` template makes the reference agree with us on pages 1-142 — and the one-page displacement of everything after page 73 of that document, none of which any gate column can see. The source leg is NOT identified: the `\t` handling at `DomainMapper_Impl.cxx`:7663-7710 only fills a level-to-style map, so the loss is downstream of it. | `probes/wordsgroup-r128` SS2, `heading-gap.tsv`, `toc-template.tsv`, `faa-orientation-runs.tsv`. |
| O88 | **A field inside a text box inside a `wpg:wgp` group anchored in a table cell is not imported at all — 26.2.4.2 keeps its cached result as plain text.** Seen first as a footer page number frozen on every page: `A1. EASA Form 2.docx` prints `Page 6 of 7` on all 7 pages and `B11. TE.CAO.00129  Experience  logbook.docx` prints `Page 3 of 6` on all 6, where we print 1..N. The frozen values are the cached `w:t` results, and the reference's own `--convert-to fodt` shows **zero** `<text:page-number>` in those footers against a real `text:page-number` in the nine text-box documents that work — so there is no field to update, and this is an **import** defect, not a field-update or layout one. **Two factors, and both are necessary.** *Grouped* alone is harmless (three grouped documents work) and *in a table cell* alone is harmless (a plain field in the same footer table cell works, twice, and so does an ungrouped text box wrapped in one, twice). Established in **both** directions: moving the drawing paragraph out of the footer table unfreezes both real documents; wrapping a working grouped document's drawing in a one-cell table freezes it (`UG.CAO.00133`, `TE.CAO.00125`); wrapping an **un**grouped one changes nothing (`system_design…`, `ABCD-SDE-23-00`). A from-scratch 2×2 factorial with a real `word/settings.xml` and no VML fallback reproduces it in exactly one cell of four. **Not page-specific and not header/footer-specific**: the same two factors drop a `FILENAME` field, and drop the field in the document **body**. **Not a 26.2 regression** — 24.2.7.2 does the same. **Why it is theirs**: nothing in either file pins the field (no `w:fldLock`, no `w:dirty`, no `w:documentProtection`), and the same binary keeps the field in three of four arms that differ only in the shape's container, which no policy of honouring a cache would do. **Measurement hazard**: 26.2.4.2's own `.odt` export writes the frozen string and no field, so `/home/user/corpus-odf`'s copies of these two documents agree with us for the wrong reason — an ODF-track comparison on them is measuring the defect's own output, not agreement. **Do not reproduce.** | `probes/pagefield-r137`. Base rates: **2 of 947** banked gate-r133 reference renderings freeze a page field (the other two candidates the census threw are false positives, `issue 1 of 20/06/2019` and a `10/24` revision date); **2 of 272** words `.docx`; 97 state a header/footer page field, 11 put it in a `w:txbxContent`, 9 have a `wpg:wgp` in a header/footer, **2** have the conjunction. The brief's *96 of 272 / 9 at risk* does not reproduce under either a case-sensitive or case-insensitive reading (97/11 and 89/6); its *grouping: REFUTED* was drawn from three grouped documents that are all outside a table and so cannot see a two-factor interaction; and its *anchored in a table* test counted `PI-doc.-no.-2E-…` because that document's `w:tbl` is **inside** the `w:txbxContent` rather than around the drawing. Export-side reach **2 of 97**. Gate reach **0** — both documents are `match` rows and a frozen page number has the same glyph count as a counted one, so no gate column can ever see this. |

## Fixed in this tree — closed, kept so the mechanism is not re-derived

|---|---|---|
| O70 | **On a page drawn through a transform, the reference quantises a rule at the size BEFORE the scale and this tree quantised it after.** Seated by round 123, closed by round 132. Calc prints a zoomed sheet through a map mode carrying the zoom, so `OutputDevice::ImplLogicHeightToDevicePixel` takes the scale INTO the device pixel count and `DevicePixelToLogicHeight` takes it back OUT -- and `CoordinateMapper::ViewToLogicDistanceY`'s `llround` (`vcl/source/outdev/CoordinateMapper.cxx`:279) therefore lands on a whole logical unit of the UNSCALED page, which is multiplied by the zoom on its way to the paper. `MetricGrid` gained a `PageScale` that only `ToLength` reads; `ToPixelEm` needs none because the em it is handed is already the drawn size. **Witness, with no free parameter**: `RMP 2011-2014 and Inventory.xls` prints at 60% -- recoverable from the PDF's own stated 6.004 pt em, which is `353 x 0.6` hundredths of a millimetre for a 10 pt Arial and which no other whole percent produces -- so the device sets 60 px and the descent branch answers 3 px thick at 7 px deep. After the scale: `round(3 x 2540/720)` = 11/100 mm = 0.3118 pt. Before it: `round(3 x 2540/720 / 0.6) x 0.6` = 10.8/100 mm = 0.30614 pt, written `0.306`; the depths are 0.7086 against 0.69732, written `0.697`. 26.2.4.2 draws 0.306 and 0.697. **Impress and Writer needed no change and that is measured, not assumed**: the two scaled-set decks fit a transform of 100%, and all 338 words documents plus all 302 slides render byte-identically. | **On the 31 documents holding a scaled rule, 1418 paired rules go from 544 agreeing to 0.0015 pt to 1412**; mean |difference| 0.0051 -> **0.0004 pt** on thickness and 0.0055 -> **0.0007** on depth; 21 of 31 documents to 100%, none down. Over the whole **307-document sheets track**: 11 957 paired rules, agreeing **11 080 -> 11 950**, disagreements **877 -> 7**, zero documents down. **Confinement measured byte for byte**: base and fixed binaries each rendered all 947 corpus documents under a pinned `SOURCE_DATE_EPOCH`, 947 of 947 succeeded on both and **31 differ -- docx 0 of 272, doc 0 of 66, pptx 0 of 251, ppt 0 of 51, xlsm 0 of 2, against xlsx 27 and xls 4**. The 6 rules still out are all on documents whose bytes did NOT move, `apron-area`'s 0.1992/0.3975 among them, so none of them is this defect. `probes/rulescale-r132/results.md`, `hash-diff.tsv`, `sheets-after.tsv`, `fitzoom-page.py` |
| O64 | **Every rule the reference draws is a whole number of the PDF writer's own 720 dpi pixels rounded to a whole unit of the map mode the page is painted in — which is a hundredth of a millimetre for Calc and Impress and a TWIP for Writer.** Seated by round 118, closed by round 120. Three steps: `PDFWriterImpl` is a `VirtualDevice` asking for `RefDevMode::PDF1` = 720 dpi (`vcl/source/pdf/pdfwriter_impl.cxx`:424-428, `vcl/source/gdi/virdev.cxx`:409-411); `FontMetricData::ImplInitTextLineSize` answers a whole number of **those pixels** on both branches (`vcl/source/font/fontmetric.cxx`:200-330), which is why round 118's arithmetic missed — `post.underlineThickness` at 10 pt is not 0.7324 pt but `ceil(150 x 100/2048)` = 8 *pixels*; and `drawStraightTextLine` converts it with `HCONV` = `DevicePixelToLogicHeight` = `CoordinateMapper::ViewToLogicDistanceY`, an `llround` to a whole logical unit (`pdfwriter_impl.cxx`:6751, `vcl/source/outdev/CoordinateMapper.cxx`:278-283). **Which logical unit is the half the source gets wrong**: `PDFExport::ExportSelection` records every page in `MapUnit::Map100thMM` (`filter/source/pdf/pdfexport.cxx`:168-179), but the recording device's map mode is whatever the application sets while it paints and Writer paints in twips. `LineSpacing.ResolveRuleWidths`, `MetricGrid.TextLine` and `MetricGrid.WriterTextLine`. **Corrected by round 123, twice.** (a) *"`SheetUnderline` has the state and the Calc path is exact"* is true of the THICKNESS and false of the position: the reference's two lines are **three** thicknesses apart, because `drawStraightTextLine` centres the first and centres the second **and adds a whole further thickness** (`pdfwriter_impl.cxx`:6857), and this tree drew them two apart. (b) **This row's 810 of 810 is a median thickness and the sweep it was scored on structurally could not see where a rule sat** — `read-rules.py` reports no position. The offsets were left as fractions of the em on the strength of C16's 0.02 pt, which is a strikethrough on one document; over 126 authored rules a single underline is 0.067 pt out on average and 0.139 at worst. Round 123 puts all three kinds' offsets on the same chain and reproduces **252 of 252** authored rules and **15 062 of 15 103** of the reference's own rules over the whole corpus, thickness and position together. `probes/dblunder-r123`. | `probes/quantise-r120/results.md`, `RuleWidthTests`. Confirmed twice — in source, and against 26.2.4.2 on a purpose-built sweep of **six faces x fifteen sizes x three kinds authored identically as `.fodt`, `.fods` and `.fodp`**, each rendered **twice** (C11): **810 of 810 exact with no free parameter**, and the units do not interchange — the Writer leg is 0 of 270 against 1/100 mm and the Calc leg 0 of 270 against twips. Both branches are exercised (Liberation Mono's descent trips #i55341's clamp; Caladea is 1000 upem with `USE_TYPO_METRICS`). **The seven pairs the seat was opened on — 18, 21, 18, 21, 49, 28 and 14 hundredths — are seven of seven**, and the five distinct point values round 118 read (0.51, 0.595, 1.389, 0.794, 0.397) are exactly `llround(n x 72000/2540)/1000`, which is `appendMappedLength`'s own thousandth of a point. **Reach: 368 of 947 renderings change — words 147, slides 133, sheets 88 — 0 page counts, 0 alphanumeric characters, 0 fidelity verdicts (10 of 552 failed before and the same ten after).** A rule is ink and no gate column can see one. Single underline and strikethrough go **74 of 540 exact to 540 of 540**, mean error 0.0323 pt to 0.0002 pt, which is the channel's own floor. |
| O68 | **A double underline is two THINNER lines whose separation is floored on the device, and none of the three rules was where the reference puts it.** Seated by round 120, closed by round 123 — **for the word-processing half only; the slides half is O69, nil reach.** `PageRun.IsUnderlined` was a `bool`, so `w:u w:val="double"`, `\uldb`, `sprmCKul` operand 3 and `style:text-underline-type="double"` all drew one line at the single thickness. `TextUnderline { None, SingleLine, DoubleLine, BoldLine }` in `Paperless.Text.Fonts` now carries it through all four readers, and `LineSpacing.RuleWidths` carries the geometry. **The BOLD line came with it**, on the same reach argument and with more of it: `((descent × 50) + 50) / 100` against the single's `× 25` (`fontmetric.cxx`:289) with the equality guard at :290-291 that is implemented and **unwitnessed** — it needs a one- or two-pixel descent, which is sub-point text at 720 dpi. Reach **6 rules in 2 of 947 documents**, both reproduced to the digit, which is twice the double underline's own reach. **Two corrections to this row's own seat, both measured.** (a) *"a gap of one thickness between them"* is true on the HarfBuzz branch (`mnDUnderlineOffset2 = mnBUnderlineOffset + mnDUnderlineSize × 2`, `fontmetric.cxx`:238) and FALSE on the descent branch, where the gap is `n2LineDY = max(n2LineHeight, 1 + DPIY/150)` (:299-306) — a floor of **five pixels** on the writer's 720 dpi device, binding below about 12 pt — and the three Liberation faces are the ones on the descent branch. (b) The two lines are **three** thicknesses apart, not two: `drawStraightTextLine` centres the first and centres the second **and adds a whole further thickness** (`pdfwriter_impl.cxx`:6857, asymmetric and reproduced because it is what 26.2.4.2 draws). **And ODF's underline is two attributes of ONE item** — `style:text-underline-style` and `-type` both map to `CharUnderline` with `MID_FLAG_MERGE_PROPERTY` (`txtprmap.cxx`:177-179, handlers at `undlihdl.cxx`:114-160) — so the level has to be decided before the attribute, which is CLAUDE.md's `style:font-name`/`fo:font-family` rule inverted; `OdfStyles.ResolveTogether`. | `probes/dblunder-r123/results.md`, `RuleWidthTests` (including `ABoldUnderlineIsItsOwnThicknessAndItsOwnOffset`), `OdfUnderlineTypeTests`, `RtfDoubleUnderlineTests`, `UnderlineTests`. Confirmed twice — in source, and against 26.2.4.2 on a purpose-built probe of **six faces × seven sizes × three kinds** authored as `.fodt` and `.fods`, each rendered **twice** (C11), reaching down to 6 pt so the five-pixel floor is exercised: **252 of 252 exact on thickness AND position, no free parameter**, against 14 of 252 before. Round 120's own 810-row sweep goes **630 → 720 of 810** (Writer double 0 → 90 of 90, mean error 0.3822 → 0.0000 pt, worst 1.5000 → 0.0000, drawn rules 90 → 180 against the reference's 180); the remaining 90 are the Impress double rows, which are O69. And against 26.2.4.2's own banked rendering of the whole corpus, at the corpus's own faces and sizes, **15 062 of 15 103 rules** — single 9084 of 9125, strikethrough 5978 of 5978 — with all 41 misses accounted for. **Corpus reach of the double underline itself: 1 of 947 documents** (`RobertQ_Service.doc`, three double underlines over two of its four pages), base rate 345 of 947 documents state an underline and 2 of their 8080 statements are double. The OFFSET half reaches every underline and strikethrough there is. |
| O71 | **A word-processing underline or strikethrough does not span a TAB, and the reference paints the tab as blanks in the same font so that it does.** Seated by round 124, closed by round 126. `SwTabPortion::Paint` (`sw/source/core/text/txttab.cxx`:626-641), under the comment *"Tabs should be underlined at once"*, paints `Width()/GetTextSize(' ')` literal blanks in the current font whenever `SwFont::IsPaintBlank()`, which `SwFntObj`'s constructor sets from `(underline || overline || strikeout) && !IsWordLineMode()` (`fntcache.cxx`:106-109). **Two things the source does not settle and round 126 measured.** (a) *The rule spans the tab's WHOLE width*, not `n` blanks at their own advances: the call passes `bKern = true`, which `SwTextPaintInfo::DrawText_` turns into `aDrawInf.SetKern(rPor.Width())` and routes to `SwSubFont::DrawStretchText_` -> `DrawStretchText(aPos, rInf.GetWidth(), …)` (`inftxt.cxx`:798-808, `swfont.cxx`:1289-1345), so the blanks are set onto the portion's own width. (b) *The count decides only whether anything is drawn at all* -- `nChar == 0` gives an empty string and `DrawText_` returns on `!nLength`. `PageDrawing.TabRule`, which reuses `Rules` and therefore round 123's `ResolveRuleWidths`/`MetricGrid.WriterTextLine` chain, so a tab's rule is quantised exactly as the glyphs' own is. **Two arms deliberately NOT implemented, each with its census.** `IsWordLineMode()` (`w:u w:val="words"`, `\ulw`, `sprmCKul` operand **2**, ODF `style:text-*-mode="skip-white-space"`) is **0 of 338 words documents in all four spellings** against a base rate of 155 of 272 DOCX stating a non-`none` `w:u`; cost of declining, 8 of 171 authored probe rows. **Overline is 0 of 338** in the reference's own `--convert-to` view and three of the four formats cannot state one, so `PageRun` still has no overline field. `SwGluePortion::Paint` (`porglue.cxx`:68-75) and `SwHolePortion::Paint` (`porrst.cxx`:298) paint blanks under the identical test and are unmodelled with no witness. | `probes/wordsdec-r126/results.md` §1-3, `UnderlineTests`. **Confirmed twice** -- in source, and against 26.2.4.2 on a 171-row authored `.fodt` (4 faces x 3 sizes x 3 decorations x 4 tab widths, plus 12 word-mode rows and three discriminator groups), rendered **twice** into separate profiles (C11) with the two reads byte-identical. Scored on merged horizontal COVER and both shapes (C16): **111 of 111 exact on the two decorations this engine draws, against 4 of 111 before**. Three groups land with no free parameter. *The representation*: five Liberation Mono tabs whose residue modulo the 120-twip blank is 80, 20, 100, 100 and 100 twips -- where *stretched onto the tab* and *set at their own advances* are **1.00 to 5.00 pt apart** -- give 16.00/19.00/23.00/29.00/35.00 pt of cover at the reference, which is the tab's FULL width at **5 of 5** and the truncated reading at 0 of 5. (This is the arm a fix taken from the witness alone gets wrong, and the coordinator put it as a challenge mid-round; it is answered by that group and not by the source.) *The cliff*: the same face draws no rule at 30/90/119 twips and one at 121/180/361. *Truncate against round*: Liberation Sans at 10 pt (55.566 twips) draws none at 54 and one at **55**. **Reach, on round 124's own `gap-census.py` re-run on both legs: the reference bridges a gap of ours 10 documents / 1439 pages / 501 248 pt -> 6 / 17 / 2 460 pt**, against a mirror direction (the base rate) of 7 / 24 / 2 453 pt unchanged. Witness `150-5370-10H.docx` pages 100, 300 and 500: the header band goes from two fills totalling **120.28 pt** to four abutting fills totalling **468.00 pt** against 26.2.4.2's four strokes totalling **468.00 pt**, contiguous 72 -> 540 on both sides. The residue is **O79**. |
| O72 | **A `TOC` field's stored result keeps the file's `Hyperlink` character style -- and the cause was not a missing rule but a PARAGRAPH BOUNDARY.** Seated by round 124, closed by round 126, and the seat's own diagnosis is corrected. The suppression round 124 asked for was **already in the tree**: `DocxLayoutSource.RunWalker._indexResults` has cited `DomainMapper.cxx`:3037-3047 (*"do not add it elements in TOC: they will receive later another style references from TOC"*) and `DomainMapper_Impl.cxx`:9355-9360 (`VisitedCharStyleName`/`UnvisitedCharStyleName` = `Index Link`, `STR_POOLCHR_TOXJUMP`, an EMPTY pool style) since before round 124 opened. **A `RunWalker` is built per paragraph**, and a `TOC` field's `fldChar begin`, its `w:instrText` and its `separate` all sit in the FIRST entry's paragraph while its `end` sits in the LAST -- so the counter reset at every paragraph boundary and suppressed the style on entry one and on nothing else. On `150-5370-10H.docx` that is 1 of 43 contents lines. LibreOffice's own `m_bStartTOC` is on the document-level import state for exactly this reason. Three pieces: `DocxLayoutSource._indexFieldDepth` seeded into each walker and read back from `RunWalker.IndexFieldDepth`; `_inheritedIndexResults`, because the field STACK is per paragraph so the `end` that closes a multi-paragraph field arrives with nothing of that field's on it to pop; and `ReadFlow` SAVING and restoring the depth rather than clearing it, because a text frame's content comes through it too. **The WW8 half is unimplemented and is O81** — closed by round 130, and its rule is a different one; the O78 this row used to name is a different seat entirely. | `probes/wordsdec-r126/results.md` §4-5, `DocxIndexFieldTests`. **Round 124's control was re-derived before anything was built and it reproduces**: over the whole words track, **50 documents carry a drawn contents page** (98 pages, identified from five or more runs of five or more leader dots rather than from the markup) and on them ours drew **576 547 pt** of rule against 26.2.4.2's **82 588** -- ours more by >200 pt in **42 of 50**, the reference in 4, the reference drawing NOTHING in 16 -- while **the same test on the same documents' ordinary pages reverses**, ours higher **8 of 50** and the reference higher **18 of 50** (r124 had 5 and 19 of 40). After: contents cover **110 002 pt**, Σ|ours − reference| there **511 497 -> 48 531 pt**, documents within 10 pt of the reference **2 of 50 -> 39 of 50**, ours-more **42 -> 3**, reference-draws-nothing **16 -> 1**. Witnesses: `150-5370-10H.docx` p7 **9413.49 -> 468.00 against 468.00** (and those 468 are O71's header rule), `exhibit-06---technical-architecture-template.docx` p4 **4205.54 -> 1969.90 against 1969.40**; thirteen more documents go to 0 against 0. **70 % of the 48 531 pt residue is two `.doc` (O81), and round 130 took those two from 34 065 pt to 92.** |
| O65 | **A hyperlink cell keeps the file's own colour wherever the text under the field carries a HARD character colour, and which importer leaves one there is the whole discriminator.** Seated by round 118, closed by round 120, and round 118's *rich versus plain* reading is refuted. `ImpEditEngine::SeekCursor` applies the field's LINKS colour and then re-applies any `EE_CHAR_COLOR` over the field's position, under the comment `#i1550# hard color attrib should win over text color from field` (`editeng/source/editeng/impedit3.cxx`:2947-2957). `WorksheetGlobals::insertHyperlink` (`sc/source/filter/oox/worksheethelper.cxx`) calls `rEE.Clear()` and inserts a bare field, so **SpreadsheetML leaves nothing hard and is always navy**; `lclInsertUrl` (`sc/source/filter/excel/xicontent.cxx`:155-215) keeps the edit object and, for a plain cell, applies the pattern through `ScPatternAttr::FillEditItemSet`, which is hard **unless the colour is `COL_AUTO`** (`sc/source/core/data/patattr.cxx`:1196-1209; only index `0x7FFF` is automatic — index 64 is a hard black, `xlstyle.cxx`:140-163); and an ODF `text:span`'s own `fo:color` is hard while a cell style's is an engine default. `SheetCellFormat.ColourIsHard` and `SheetTextLayout.FieldInk`, which answers **one colour for the whole cell** because the reference collapses the cell to one field character. | `probes/quantise-r120/results.md` §5, `SheetLinkColourByFormatTests`. Confirmed twice, and the binary leg is three formats of one content: an authored `.xlsx`, **the `.xls` 26.2.4.2 itself converts it to**, and an authored `.fods`, each rendered twice (C11). The same cell stated `#FF0000` is drawn `#000080` from the `.xlsx` and `#FF0000` from the `.xls`; and the `.fods` separates the mechanism outright — a colour on the **cell style** loses to the field and a colour on a **`text:span`** beats it. **Reach: 8 of 307 sheets renderings, 0 pages, 0 characters**, and all eight now match the reference's colour census exactly (the witness `Praktikastellen_-_chinesischsprachiger_Kulturraum.xls` is 566 `#000000` / 54 `#0000FF` / 3 `#333333` / 1 `#000080` on both sides). The automatic arm is measured on `AIM_OPR_LIST.xls`, whose **2232 hyperlink cells state no colour and draw 2262 navy spans on both sides**. The ninth census candidate `CSA_CCM_v1.2.xls` does not move because its hyperlink cells are not printed at all. |
| O2 | **The head of the slides ink ranking, closed in five pieces — and the two that had no row are a shape height that another seat had already fixed and a path question whose answer is "it reaches".** Round 94 dissolved the seat (`probes/slides-ink-r94` §8) and three pieces were adjudicated elsewhere: the `.ppt` WordArt path became **O13**, WMF nibble case `0x7` became **N8**, and `NAS-…-Weather.pptx` *as an ink defect* became **N7**, refuted. (a) **`pres_ioc_phuket.ppt` p26's dark blue banner was O14 all along.** r94 left it "not characterised" at 103.38 pt in the reference against 64.88 here; at HEAD the banner is **738.00 × 103.37 pt at (−18.00, 436.63)** against 26.2.4.2's **738.00 × 103.38 at (−18.03, 436.65)** — width to 0.00 pt, height to 0.01, origin to 0.03. Both sides paint a **fill** (`f` against `f*`), so C16 is answered before the size is: no primitive mismatch. C11 is answered too — two fresh reference renders under separate profiles are identical to each other record for record on page 26 and to the reference leg gate r119 banked. The mechanism is O14's `fFitShapeToText`; **O14's own row carries this document, this page and the number 103.37 and never says which seat it closes**, which is why the seat looked open. The L1/O57 font-class confound is live on this document and does not reach this: the banner's height is an outliner *line* height, not an advance width, and it agrees to 1 part in 10 000. Page 26 is now `\|ink\|` **0.23 %**, *shifted*, against r94's **4.28 MAJOR**; the residue is O13's WordArt band, closed there. (b) **`TICAPCapability_Final.xls`'s paired `SRCAND` blits reach the reader and the pair fires** — see O66. | `probes/o2-audit-r121` §1–§2; `banner-geometry.tsv` over four renders; O14 (`probes/slides-r97` §1) is the fix |
| O1 | **The head of the sheets ink ranking was 413 conditional formats nobody read.** `TK-Syllabus-Comparison-Document-v2.xlsx`, 304.57 summed `\|ink\|%` over 1235 pages and passing the gate throughout — the seat was ink, not pages. The document states **413 `<conditionalFormatting>` blocks and 413 `<dxf>` entries and every one of the 413 rules is `type="expression"`**, a family `XlsxConditionalFormats` named in its own remarks and did not evaluate. The revision pair is what sized it: v2 states 413 expression rules and v5 states 3 — the revision baked the formatting into the cells and left 728 unused `dxf` entries behind — so per page v2 was `304.57/1235 = 0.2466` against v5's `46.55/855 = 0.0544`, a ratio of **4.53**. After the conditional-format work v2 is **0.1665** per page and the ratio is **3.06**. | `probes/sheet-ink-r94`; the readers are O20, O22, O29, O6 and O21, and N10/N11 hold what is left |
| O5 | **`017_Timeline_Templates`' missing page was a connector with no `svg:width`.** Not a print-area extent in the sense the seat guessed: `draw:line`, `draw:connector` and `draw:measure` state `svg:x1`/`svg:y1`/`svg:x2`/`svg:y2` and **neither `svg:width` nor `svg:x`**, so `OdsDrawings` sized every one of them zero and a zero-area shape widens no print area. A connector's rectangle is its two endpoints. The witness printed **2 pages against 26.2.4.2's 3** and prints **3** after, at 969 glyphs on both legs. | `probes/ods-residue-r95` §3, and its own §1 table records the seat as O5 |
| O62 | **A Calc cell's hyperlink underline: the reference's underline and the reference's link COLOUR come out of one function call, and this tree had modelled only the colour.** Seated by round 117, closed by round 118. `ScEditUtil::GetCellFieldValue` (`sc/source/core/tool/editutil.cxx`:209-244) answers a `text::textfield::Type::URL` by writing *both* `*ppTextColor = …GetColorValue(LINKS)` and `*ppFldLineStyle = FontLineStyle::LINESTYLE_SINGLE`; `ScFieldEditEngine::CalcFieldValue` passes both out, `ImpEditEngine::UpdateFields` (`impedit2.cxx`:3229-3231) stores them on the field's own attribute and `EditCharAttribField::SetFont` (`editattr.cxx`:349-350) applies them. `SetUnderline` **replaces**, so a hyperlink cell whose font states a *double* underline is drawn with one line. `SheetTextLayout.Line`, beside the existing `Ink`. **The blind reader's lead was right about the symptom and wrong about the shape**: it is one path, not a colour path and an underline path. | `probes/underline-r118/results.md` §1-5, `SheetHyperlinkUnderlineTests`. Confirmed twice — in source, and against 26.2.4.2 on a purpose-built five-cell fixture rendered **twice**, which measures every arm including the two a format-level fix would get wrong (a linked double underline draws ONE line; a hyperlink on a *numeric* cell draws none). **The reference's own `--convert-to fods` says the opposite and is the wrong instrument**: `ScXMLExport` resolves a field with both out-parameters null (`xmlexprt.cxx`:3062), so the flat file prints `style:text-underline-style="none"` and `#000000` on the very cells the PDF draws navy and underlined. **Base rate: 74 of 307 sheets documents hold a hyperlink-field cell and 73 hold an underlined cell** (fods census, 307 of 307 converted); **reach: 27 of 243 zip spreadsheets hold a hyperlink cell whose own font states no underline, 212 cells**, and the render diff finds exactly those 27 plus 2 `.xls` the XML census cannot see. **29 of 307 renderings change, 0 page counts, 0 alphanumeric characters, 0 gate verdicts — an underline is ink and the gate cannot see it.** Link-coloured rule *length* is within 2 % of the reference on **27 of the 29 after and 0 of 29 before**. **Two of round 117's three witnesses are refuted**: `002_Contextures_chart_sample`'s ten rules were already drawn (all ten cells state `<u/>`), and `TK-Syllabus-Comparison-Document-v2` has **no hyperlink cell at all** — its 7625 red rules are *strikethroughs* (4221 `<strike/>` runs against 12 `<u/>`; on page 5 the rule sits 2.30 pt ABOVE the baseline), which this tree draws as fills at the same place, 0.074 pt thinner. Its 0.800 census score is the fill-versus-stroke trap, C14's sibling. |
| O61 | **A BIFF chart's own rules are never given a width — the `CHLINEFORMAT` weight was skipped, and it is four numbers.** Seated by round 117, closed by round 118. `XclImpChLineFormat::ReadChLineFormat` (`xichart.cxx`:452-463) reads the weight as a **signed** `Int16` and `XclChPropSetHelper::WriteLineProperties` (`xlchart.cxx`:906-925) maps it to an API width in hundredths of a millimetre: hair 0, **single 35, double 70, triple 105**. `HAIR` is −1 and `SINGLE` is **0**, so a reader taking the field unsigned, or reading 0 as unstated, makes every single-weight line a hairline. An *automatic* line takes `spFmtInfos`' per-object weight (`xlchart.cxx`:420-440): twelve of sixteen object types are `HAIR`, and the exceptions are `LINEARSERIES`/`FILLEDSERIES`/`ERRORBAR` at `SINGLE` and `TRENDLINE` at `DOUBLE` — **so an automatic width is a hairline everywhere this reader models except a series.** *This also answers O63's own open question*: the unexplained 35/100 mm is `EXC_CHLINEFORMAT_SINGLE`'s API width, written down in the BIFF filter — though whether the OOXML importer reaches it by the same route is not shown. | `probes/underline-r118/results.md` §7, `XlsChartLineWidthTests`. Confirmed against 26.2.4.2's own `--convert-to ods` as well as in source: over the **7** `.xls` that carry a chart, **every** stated `svg:stroke-width` in the class is `0.035cm` or `0.07cm` and nothing else — 26 series (7 + 19), 3 chart frames, 5 walls, and **not one axis or gridline**, so those two halves of the rule are exercised by the fixture alone. **Reach: 2 of 307 renderings change**, 0 pages, 0 characters, 0 gate verdicts. On the seat's own witness `014_Contextures_chart_sample_991ecfc5.xls` the mean stroke width goes **0.000 → 0.2205 against 26.2.4.2's 0.2500** and excluding hairlines 0.000 → 1.1905 against 1.2143; on `EHEST-Pre-departure-checklist` 0.5917 → 0.6196 against 0.6235. **What is left on that witness is a count, not a width**: the reference draws 136 stroked items and this tree 108, and the 28 are the chart-area frame (whose `ChartPlot.Border`/`BorderWidth` exist and are unread from BIFF) and the plot wall (which the model has no border for at all). |
| O58 | **A turned value label reserves half its own line HEIGHT at the axis' far end, and no width at all.** Closed by round 117. `PlotAreaOf` took `((valueLabel·cos) + (valueHeight·sin))/2` off the right edge; the answer is `valueHeight·sin/2`, and upright — where `lcl_correctRotation_Bottom`'s `fAnglePositiveDegree==0.0` branch is **empty** (`chart2/source/view/main/LabelPositionHelper.cxx`:241-256) — the plain half-width stands. The piece that makes the arithmetic close is `ShapeFactory::makeTransformation`'s own comment, *"as autogrow is active the rectangle is automatically expanded to that side to which the text is not adjusted"*: a bottom-axis label is centre-adjusted, so its top **centre** is on the tick before the correction, rotating that box puts its right edge at `w·cos/2 + h·sin`, and the correction takes `(w·cos + h·sin)/2` off it — leaving `h·sin/2`, with no `w` in it. | `probes/chart-slide-r117` §2, `turn027.py`. **Confirmed twice.** Source as above (27.2 tree), and measured on `027_Simple_personal_cash_flow_statement`'s savings chart with its value axis' scale pinned so the tick set cannot move and O51's cap cannot enter: **widening every value label from 19.18 pt of ink to 40.69 by its number format alone moves the drawn plot width by 0.02 pt in 8 of 8 variants**, where half the width would have moved it 10.8, and every label's right edge stays **2.00 pt past its own tick** throughout. The reserve read against a `tickLblPos="none"` control is `h·sin(θ)/2` over **three stated sizes (6, 9, 14 pt) × four angles (22.5, 45, 67.5, 90) to within 1.6 %**. The **upright control** — the same chart with `c:majorUnit` pinned so its two labels do not collide — has the last label centred on its tick to 0.04 pt at four widths and gives up half its width, which is why the fix is two rules and not a deletion. `027`'s axis goes **108.17 → 115.664 against the reference's 115.666**, from a left edge that already agreed at 645.05. **Reach 1 of 176** chart-bearing renderings, the other 175 byte-identical with `SOURCE_DATE_EPOCH` pinned and both legs the same UTC day; **0 page counts and 0 alphanumeric counts move**, so no gate column can see it. Three tests, two failing at `88209aa4b`; `ChartValueAxisArrangementTests`' own 420 pt synthetic frame sat within a few points of its collision boundary and is re-tuned to 380 with the reason beside it. **Left open beside it and not implemented**: the same algebra puts the *first* label's left edge at `tick − (w·cos + h·sin/2)` and the bar branch reserves nothing for the value labels on the near side — no witness, `027`'s left edge is set by its category names and did not move by a hundredth across twenty variants. |
| O63 | **A chart's own rules: the theme's line styles never reached the spreadsheet or word-processing chart reader, a DrawingML width is a whole 1/100 mm, and a marker is stroked as well as filled.** Seated and closed by round 117, out of round 115's unseated residual — whose sentence *"we stroke a chart's rules at width 0 where the reference strokes them"* is right about the symptom and silent on all four mechanisms, and misleading because **the reference writes `0 w` too** (108 times on the first witness). (1) `XlsxDrawings.Plot`/`ExtendedPlot` passed `styles: null` and `DocxPictures.LoadChart` passed none, so `DrawingChartAutoFormat.AutomaticLineWidth` had no `a:lnStyleLst` to read and every automatic axis line and gridline resolved to **zero**; only `PptxSlideLayoutChart` joined the two halves. The colour was the same defect — with the matrix the theme's own `shade`/`satMod` resolve and `008_Contextures`' gridlines land on `#878787` exactly. (2) `LineProperties::getLineWidth` is `convertEmuToHmm`, `(n+180)/360` — an integer 1/100 mm — so 9525 EMU is 26 and 0.73701 pt, never 0.75. (3) `TypeGroupConverter::convertMarker` gives a symbol a fill *and* a border and the border is the fill colour (the `tdf#124817` branch only reaches for the line when there is no fill); the reference emits two operations per marker and this tree emitted one. (4) And a series stating an `a:ln` with no `w` takes neither the stated nor the automatic width but a flat **35/100 mm**, while a `c:dPt`'s own `a:ln` with no `w` is a **hairline** — one fixture with a single data point added draws that wedge at `0 w` and the three taking the series' identical `a:ln` at 0.99036. | `probes/stroke-resid-r117/results.md` §3-4, `SheetChartLineWidthTests`, `FrameChartLineWidthTests`. Every arm confirmed against 26.2.4.2's own `--convert-to ods` as well as in source; (4) by seven one-attribute-at-a-time measurements, among them raising the theme's subtle line to 38100, which moves every gridline from 26 to 106 hundredths of a millimetre and leaves the series at 35. **This model has no per-point line width**, so (4)'s point rule is applied to the whole series — exact where every point states an `a:ln`, which is the shape that occurs, and wrong for a mixed series. **Reach: 26 of 307 sheets and 1 of 338 words documents hold a rule with no stated width** (the 17 slides documents are the control and were already right), 169 of 947 carry an OOXML chart at all. Over the sheets track the ink census goes **14 → 4 documents more than 10 % lighter, 3 → 1 more than 10 % heavier, and mean |ratio−1| 0.0571 → 0.0218**, over the 191 documents comparable on both legs. **Where 35/100 mm comes from is not established** and is recorded as such on `DrawingChartPlot.SeriesLineWidth`. |
| O59 | **A cell border's dash lengths are absolute, and this tree derived them from the line width.** Closed by round 117. A border's dashing is `svtools::GetLineDashing(type, PatternScale() * 10.0)` (`sdrframeborderprimitive2d.cxx`:599-601) over a table of small integers — 1/2 dotted, 6/2 fine-dashed, 16/5 dashed, 16/5/5/5 and 16/5/5/5/5/5 (`svtools/source/control/ctrlbox.cxx`:248-283) — and a Calc cell's pattern scale is `fColScale`, the twips-to-1/100 mm factor, so **one table unit is ten twips, half a point**, and the print zoom multiplies it afterwards. Nothing in it is a function of the width. Confirmed against the binary twice: round 115's thirteen-style fixture gives `[0.49999 0.99998]` and the other four at 100 % and the same five times 0.42 at `scale="42"`; and page 1 of `079_Org_charts_visual`, a real corpus document at a fitted scale of 0.40002, comes out `0.29481 w [ .20001 .40001 ] 0` against `26/100 mm × 0.40002` and `[0.5 1.0] pt × 0.40002` — two quantities, five figures each, one shared parameter read off the first. **The dash lengths are not rounded to a whole logic unit and the width is** (`vclmetafileprocessor2d.cxx`:1818-1874). | `probes/stroke-resid-r117/results.md` §1-2, `SheetBorderDashTests`. **Reach: 12 of 307 sheets documents (3.9 %) state a patterned border at all** — two independent censuses, the XML statement and 26.2.4.2's own `--convert-to ods`, name the same twelve. `dotted` reaches 9 of them and `fine-dashed` 4; **`dashed`, `dashDot` and `dashDotDot` have zero corpus reach** and are exercised only by the fixture. No gate column can move and none did. |
| O56 | **A spreadsheet's cell border takes the sheet's print scale, and this tree drew every one at full size.** Closed by round 115, and the seat is not the style table the census implied: `--convert-to fods` on the worst witness prints `fo:border="0.74pt solid #bfbfbf"` for a `thin` border, so the reference's *model* width and ours already agreed. What is missing is the zoom. `ScDocument::FillInfo` hands `svx::frame::Style` the width in twips times `fColScale` (`fillinfo.cxx`:1144-1147), which for a print or PDF export is the constant twips→1/100 mm factor (`printfun.cxx`:2627-2628), and the print zoom is the fractional scale of the `Map100thMM` map mode the page is drawn through (`:2639`) — so it multiplies the widths along with the coordinates. Measured at 26.2.4.2 rather than read off that: **drawn width = `max(0.1 pt, stated × scale)`**, over one workbook of thirteen border styles at **nine stated scales**, as `.xlsx` and again as `.xls`, five figures at every one. The floor is one pixel of the export's 720 dpi device (`PDFPage::appendLineInfo`, `PDFPage.cxx`:497-505) and is measured as a floor, not a constant — `hair` is 0.1 at 100 % and 0.19956 at 400 %. A **patterned** width first rounds to a whole 1/100 mm (`vclmetafileprocessor2d.cxx`:1818-1819): 0.737 against a solid 0.75004. Three stated widths were also wrong and are now measured: SpreadsheetML `double` is **10/15/10** twips (lines 1.248 pt apart), BIFF `double` is **10/30/10** (1.984 pt apart), and `mediumDashed`/`slantDashDot` were swapped. | `probes/sheet-border-r115`, `scale-sweep.txt`, `class-before-after.txt`. Re-censused the way round 113's was, over the sheets track's 191 comparable documents against the banked 26.2.4.2 leg: **≥1.5× heavier 31 → 0**, within 10 % **111 → 172**, mean \|ratio−1\| **0.3047 → 0.0571**, 79 better / 4 worse / 108 level; the worst witness `079_Org_charts_visual` **4.759 → 0.989**. The four that worsen are the mean-ratio instrument, and both witnesses agree with the reference *at width level* after the change. **Reach 120 of 307 sheets renderings, 0 page counts and 0 alphanumeric counts** — an ink seat, as the entry always said. `SheetBorderWidthTests`, 8 cases, 7 failing at the base. |
| O54 | **A `.ppt` table's row heights are re-derived by `TableLayouter`, twice — and the second pass is a shrink.** Closed by round 113. `CreateTable` (`svdfppt.cxx`:7569) seeds the rows from the members' tops and measures the last to the group's bottom (`CreateTableRows`, :7339); filling the cells lays the table out at `max(stated, Cell::getMinimumHeight())` (`tablelayouter.cxx`:724, `cell.cxx`:686 — the text at the cell's inner width, plus one unit, plus the two vertical distances); then `SetSnapRect` at :7712 reaches `NbcSetLogicRect` (`svdotable.cxx`:1925-1938), which sees the height change and passes **`bFitHeight` true**, so `distribute` (`tablelayouter.cxx`:499-547) runs with a negative budget it **does not reset between passes** — and a deficit one row cannot absorb takes every row to its own minimum. `Thailand17` 11 is 39.20 + 8 x 36.20 and `2015-Civil-Rights-Website-training` 48 is four rows of 1108 and one of 5024, both exactly 26.2.4.2's own flat ODP. | `probes/slide-table-r113`. Over the **10 corpus `.ppt` holding a table group**, on the 34 pages that move: Σ worst row-edge |Δy| **249.04 → 129.19 pt**, of which 127.71 is `architecture6`'s text (O57) and the rest is **1.48 pt over 17 pages**; `Thailand17` 11's 78 text baselines go 2.72 → **0.03 pt** worst. Confined: 10 of 604 slide renderings move, exactly the census's ten, 0 page counts and 0 alphanumeric counts anywhere. |
| O55 | **A `.ppt` table's rule is a cell border, and its width goes through three integer steps.** Closed by round 113, on the `.ppt` side only — this is **not** O56's flat-default class, because the widths this tree drew were the file's own. `floor(emu / 360)` into hundredths of a millimetre, truncating (`msdffimp.cxx`:1048 via `ScaleEmu`, :3193); then `max(1, that / 4)` for the `BorderLine2` (`ApplyCellLineAttributes`, `svdfppt.cxx`:7519-7521); then that number handed to `svx::frame::Style` scaled by `o3tl::convert(1.0, twip, mm100)` — **read as twips** (`viewcontactoftableobj.cxx`:183-184). One unit is a twentieth of a point, so the chain is `max(1, floor(emu/360)/4) / 20` pt. Confirmed twice: `Thailand17` states 12700 and 28575 EMU, its flat ODP states `0.23pt`/`0.54pt` (8 and 19 hundredths), and its PDF strokes **0.40 and 0.9499**. The 2.50 and 2.37 round 112 could not reconcile are the truncation at two of the three steps. | `probes/slide-table-r113`, `pptlines.txt`, `widths.txt`. **377 line members in 34 groups in 10 documents**, five stated widths, all predicted. On the 34 moving pages, stroke widths matching 26.2.4.2's go **67 of 416 → 412 of 416**. |
| O51 | **The interval cap's numerator is the rectangle the MAXIMUM-LABEL pass leaves, not the axis as drawn — and it is wider than either side's plot area.** `estimateMaximumAutoMainIncrementCount` divides `nTotalAvailable` by `m_nMaximumTextWidthSoFar`, and `nTotalAvailable` is `get2DAxisMainLine`'s length at the moment `doAutoScaling` runs the second time — which `ChartView::impl_createDiagramAndContent` (`ChartView.cxx`:556-604) places between `reduceToMinimumSize()` (available/2.2) and the *first* `adjustInnerSize(aConsumedOuterRect)`, where the consumed rectangle is the reduced diagram plus three labels per axis laid out overlap-allowed, line-break-forbidden and never auto-rotated (`canAutoAdjustLabelPlacement` refuses while `m_bOverlapAllowed` is set, `:539-556`). The 2.2 cancels, so the numerator is **the available width less what those labels hang outside the diagram**, with nothing off the far end — the value labels the maximum pass drew are `{0, 1, 2}`, all at the near end. **Confirmed at 26.2.4.2 on the axis at right angles to O53's**: widening `027`'s *widest* category label — index 3, which `MaxLabelTickIter` resets past — leaves the value axis at `$0 $2,000 … $14,000` while the drawn axis collapses **115.67 → 75.10 pt**, and index 4 likewise; widening index 0, 1 or 2 coarsens it to 5,000. The fine sweep pins `nTotalAvailable` at **[129.7, 131.1) pt** where the model predicts 130.7, the drawn axis is 115.7 (reference) and 107.2 (ours), and r112's bracket was [127.4, 138). **And "the longest label" is a CHARACTER COUNT** — `getIndexOfLongestLabel` (`VAxisBase.cxx`:195-212) compares `getLength()` under its own `//todo`, first of two equal ones winning; reading it as a width costs `026`'s expenses chart a whole interval, eight where the reference draws four. `ChartLayout.MaximumPassWidth` and `WidestMaximumCategoryLabel`; the **horizontal** branch only, and a chart stating its own inner rectangle (`mbUseFixedInnerSize`) keeps the drawn plot area. **Reach 1 of 176 chart-bearing renderings and 0 of 182 chart-bearing converted-ODF files**, `SOURCE_DATE_EPOCH` pinned: `027` goes from 7932 to **7949 against the reference's 7949, exact**, no page count anywhere. | `probes/chart-cap-r113` §1, `cat-set-027.py`, `cat.tsv`. `ChartIntervalCapAreaTests`, 5 cases, 2 failing at the base. The **vertical** branch is measured and deliberately left (§2, `colcap.py`): four variants over a 120 pt frame fit both readings, which differ by at most half a label's height. Residual **O56**. |
| O50 | **A data label's `[CATEGORY NAME]` is written through the CATEGORY AXIS' number format, and the source cell's decides nothing — and shipping it makes `055`'s gate column worse.** `VSeriesPlotter::getCategoryName` (`VSeriesPlotter.cxx`:2213-2224) is `getSimpleCategories()[n]`, built by `ExplicitCategoriesProvider::convertCategoryAnysToText` (`ExplicitCategoriesProvider.cxx`:186-227), which resolves ONE format before its loop — `getAxisByDimension2(0, 0)` through `AxisHelper::getExplicitNumberFormatKeyForAxis` — and writes every numeric category through it; a string category is passed straight out. **Measured at 26.2.4.2 with seven one-attribute variants of `055`**: rewriting the axis' `formatCode` to `yyyy`, `mmmm` or `0.00` moves all 51 category spans (38 ticks + 13 data labels), while rewriting the *cells'* format to `yyyy` or `0.00` — or setting `sourceLinked="1"`, which for an axis `ObjectFormatter::convertNumberFormat` (`objectformatter.cxx`:1143-1147) ignores outright — leaves the rendering byte-identical. `ChartLayout.CategoryTextAt` at the five `Compose` sites; it takes the serials off `plot.DateAxis.CategoryValues` because an OOXML chart in a workbook resolves its `c:cat` against the live sheet and `Categories[n]` arrives already written through the cell's format. **Census: 35 chart parts in 25 of the 167 zip chart-bearing documents state such a label; exactly ONE has a numeric category cache, and it is the same one that has a date axis.** **Reach 1 of 176**, and **the gate cost is stated**: `055` goes 960 → 934 against a reference reading 1045, so column 9 is 111 short where it was 85 — the verdict does not move, the column gets worse, and the reason is C12's +111 with the opposite sign. What says it is right is the frozen pair: on `055_static2023` and `055_static2026` this tree now reads **934 against 26.2.4.2's 934, exact**, where it read 960. | `probes/chart-cap-r113` §3 and §5, `catname-055.py`, `catname.tsv`, `catname-census.tsv`. `ChartCategoryNameFieldTests`, 4 cases, 3 failing at the base. |
| O53 | **A value axis' automatic interval is capped on the widest of its FIRST THREE tick labels, and every later one is never measured.** `estimateMaximumAutoMainIncrementCount` divides by `m_nMaximumTextWidthSoFar` (`VCartesianAxis.cxx`:1559-1618), which is filled by `createMaximumLabels` — and that pass iterates a **`MaxLabelTickIter`** (`:455-511`, built at `:1517-1530`) seeded with `getIndexOfLongestLabel` for a text axis and with **zero** for a value axis, holding indices `{0, 1, 2}`. `ChartLayout.IntervalsThatFit` measured every tick. **Measured directly at 26.2.4.2 by making the two halves of one axis disagree**: `027`'s savings chart, `c:numFmt` the single variable, is labelled `$0 $2,000 … $14,000` as authored; with every tick from 6,000 up given an eight-character suffix it draws **the same eight labels at the same 2,000**, and with only `$0`/`$2,000`/`$4,000` given it, it collapses to **`$0 $10,000 $20,000`**. One extra character on the first three moves the cap from ≥7 to ≤6. **Only the horizontal branch changes** — a vertical axis is capped on the label *height*, where three digits and all of them are the same number, which is why r87's two calibration witnesses are untouched. **Reach 2 of 176**, both toward the reference: `045_Check_register_with_chart`'s longest bar 47.56 → **63.41** pt against 26.2.4.2's 79.24, and `026_Monthly_cash_flow_statement` gains the six-label axis the reference draws (see O52). | `probes/chart-axis-r112` §2, `boundaries.tsv`, `label-set-027.py`. Two tests in `ChartValueAxisArrangementTests`, one failing at the base. |
| O52 | **A value axis is wired to `ChartAxisLabels.Resolve`, so it turns its labels 45° when they collide.** Round 111 corrected the reader — a `c:valAx` never enters `axisconverter.cxx`:324-326's `case CATEGORY/SERIES/DATE`, so it keeps chart2's `TextBreak` **false**, which is what `canAutoAdjustLabelPlacement` (`VCartesianAxis.cxx`:539-556) requires — and measured nil because nothing that draws read the flags. `ChartLayout.ArrangeValueLabels` is the half that draws: a value axis running **along the bottom** goes through `Resolve` with its own stated flags, `AddValueAxis` takes the rotation and the rhythm, and `PlotAreaOf` takes the same rotation so the band and the last label's overhang follow it. **Confirmed a second time against the binary rather than the tree**: `--convert-to fods` on `027` gives all four of its value axes `text:line-break="false"` with **no `chart:label-arrangement`** at all, against `text:line-break="true" chart:label-arrangement="side-by-side"` on the category axes beside them, and the same holds for `055`'s `c:dateAx`. **Reach 1 of 176** and it changes no character: `026_Monthly_cash_flow_statement` page 7's six value-axis labels go from upright to 45°, which is what 26.2.4.2 draws there — it holds **39 glyph-sized filled paths** in that band against our 38 characters and no text at all, so the reference turns the same axis and outlines it (C7). Scope: the **horizontal** branch only; a vertical value axis cannot collide with itself and needs `LabelPositionHelper`'s `_Left`/`_Right` corrections, and a scatter's domain axis is `AddDomainAxis`'s. | `probes/chart-axis-r112` §3, §4.1. Five tests in `ChartValueAxisArrangementTests`, one failing at the base. |
| O10 | **The chart category-axis hyphenator, shipped — and it does NOT close the gate row r105's arithmetic predicted.** `hyph_en_US.dic` (106,414 bytes, BSD-style) and `README_hyph_en_US.txt` are vendored into `Paperless.Core/Globalization/Hyphenation`; `HyphenationPatterns` is the Hunspell reader and Liang's algorithm, with `LEFTHYPHENMIN`/`RIGHTHYPHENMIN` read out of the file and floored at 2 as `hyphenimp.cxx`:435 floors them; `Hyphenators` is the search path (`PAPERLESS_HYPHEN_DICTS`, one level of subdirectories, so a LibreOffice `share/extensions` resolves `dict-fr/hyph_fr.dic`), the `Register(language, stream)` seam and the `0`/`false`/`no` off switch. **`fr` and `es` are supplied at runtime and deliberately not committed** — LGPL and a GPL/LGPL/MPL tri-licence — and their tests skip where absent. `IHyphenator` moved from `Paperless.Text.Layout` to `Paperless.Core.Globalization`, contract unchanged, because `ChartAxisLabels` is in Core and Core may not reference Text. **Established twice**: the four hyphens 26.2.4.2 draws on corpus charts (`Gov-ernance`, `Con-sumption`, `Pes-simistic`, `Pas-senger`) are reproduced 4 of 4, and 22 authored one-word fixtures rendered four ways — reference with and without its dictionary, ours with and without — agree **19 of 22 against a base rate of 13**. **Reach: 3 renderings of 568 scored, all chart-bearing, all three better against 26.2.4.2 and none worse** (summed |ink|% 57.00 → 54.72 over r105's eight candidates), **0 gate rows**, and **0 of 399 non-chart documents**. *`038`'s column 9 stays 1585: r105's 1585 = reference-with-hyphenator-off identity is between the reference's two states, and the reference reads 1449 because it OUTLINES its turned labels where we draw text. That row belongs to the shear rule, not here.* Residual: `Service`, one word of 22, hyphenated here and not by 26.2.4.2, cause not established, corpus reach zero. | `probes/hyphen-r106` |
| O13 | **Escher WordArt is drawn as Fontwork, and the two halves were one change after all.** A shape is WordArt on one bit and not on its type — `bIsFontwork = ( GetPropertyValue( DFF_Prop_gtextFStrikethrough, 0 ) & 0x4000 ) != 0`, `filter/source/msfilter/msdffimp.cxx`:4424-4425 — and the type only chooses the preset, `svx/source/customshapes/EnhancedCustomShapeTypeNames.cxx`:171 and :179 over the contiguous 136-175 band of `include/svx/msdffdef.hxx`:412-451. `PptFontwork` decodes `gtextUNICODE` (192), `gtextAlign` (194, default `mso_alignTextCenter`, `msdffimp.cxx`:4467-4484), `gtextSize` (195, 16.16 points), `gtextFont` (197) and the bits of 255, and divides adjustment 0 by 65536 for the eight polar-handle types 144-151 (`msdffimp.cxx`:2163-2174 and :2594-2599). `PptSlideLayout` then returns the warped outlines *instead of* the shape, because `EnhancedCustomShapeEngine::render2` does exactly that (`EnhancedCustomShapeEngine.cxx`:250-259), with the shape's own resolved fill, because `CreateSdrObjectFromParagraphOutlines` copies the shape's whole item set onto the path (`EnhancedCustomShapeFontWork.cxx`:1123-1126) and an Escher WordArt has no run to have copied a character fill from. **Half (b) needed no new paint kind**: `PptFills.Resolve` already answers `fillType` 5 with a `GradientPaint`. Censused twice with different failure modes — a record-tree walk and a brute-force `msofbtOPT` scan over 15 032 candidate records — **2 shapes in 2 of 51 `.ppt`**, both in the text-path band. **Two corrections to the r103 sizing this entry carried**: the phuket shape is on page **26**, not page 1, and is worth **1.97 `|ink|%`**, not 0.01 — page 1 measures 0.01 in both legs and does not move. Reach measured base against head: AOD 20.34 -> 4.56 sum `|ink|%` (page 59, 16.04 -> 0.26, MAJOR cleared), phuket 4.70 -> 2.96 (page 26, 1.97 -> 0.23, MAJOR cleared), **no page worse in either**. Half (b)'s DrawingML sibling is separately **nil reach**: 0 of 251 `.pptx` state a warped body whose first run carries a non-solid fill (21 warped bodies in 3 documents, 15 `solidFill` and 6 stating none). | `probes/slides-r107` §1; `ink.txt`, `fg2.txt`, `fgtext.txt`, `wordart-props.txt`, `warpfill.txt`; `PptFontworkTests` |
| O29 | **`iconSet` is read and painted, and the artwork is seven vector glyphs rather than twenty-two icon sets.** The reach was 20 rules in 10 documents; what 26.2.4.2 actually *paints* over those ten is **60 icons drawn from 7 distinct assets**, because fourteen of the twenty rules are `custom="1"` and most of their buckets are `NoIcons` — `077` and `078` name `3TrafficLights1` and never draw a traffic light, `066` names `3Stars` and never draws a star. The seven were extracted from the reference's own PDFs **with their soft masks applied** and matched to `images_colibre.zip` at 0.007-0.064 mean absolute channel difference, against 19.7-45.1 for the best any other theme manages, so the theme is established rather than assumed. They are authored as vector paths (`SheetIconArtwork`) transcribed from `icon-themes/colibre_svg/sc/res/icon-set-*.svg`, which is the source the reference's own bitmaps are rasterised from — no assets, no raster path, no licensed binary in the tree. Reader is `XlsxIconSets`: both spellings, last matching bucket, `reverse` before `custom`, `NoIcons` as no icon at all, and a single-cell formula threshold resolved. Geometry is `drawIconSets` — the cell's own font height, square, two device pixels in from the bottom left, clipped to the cell. | `probes/iconset-r103`. **Reach 7 of 947 renderings**, 5 improving, 0 worsening, no page count moved; 300 of the 307 sheets renderings byte-identical. **9 of the 10 stating documents agree with 26.2.4.2 glyph for glyph and count for count** (`icon-census.txt`); the tenth, `066_Agile_Gantt_chart`, differs by 9 diamonds and that is its `TODAY()` recalculation, not the reader. Four fixtures, four tests, each shown to fail under a one-line mutation of the rule it pins. |
| O17 | **`alle einzeln.xlsx` 225.44 was a pivot table's generated cell formatting, and it is now generated: 225.44 → 0.07 over 186 pages, 36 MAJOR → 0.** Calc does not read the formatting Excel put on a pivot's cells; it clears the range and regenerates the output through `ScDPOutput`, ruling it in two widths of plain black (`SC_DP_FRAME_INNER_BOLD` 20 / `OUTER_BOLD` 40, `sc/source/core/data/dpoutput.cxx`:76-79) and applying the three `Pivot Table *` styles that carry anything (`lcl_SetStyleById`, `:264-296`) plus a 13 px per-level row-header indent (`:1135-1137`). **The member tree does not have to be recomputed**: SpreadsheetML's `rowItems`/`colItems` are that tree already laid out, and an `<i>`'s `@r` is exactly `MemberResultFlags::CONTINUE`, the fields it states `HASMEMBER`, a non-`data` `@t` `SUBTOTAL`. Three rules the round had to settle: the indent is the drill-down step and nothing else, so `showDrill="0"` has none (`:1136` with `pivottablebuffer.cxx`:1366, and `DynamicBubbleChart` is the witness); a **single** compact row field is not a different layout (`GetColumnsForRowFields` `:854-868`, `bLast` `:1135`, `FieldCell` `:1087-1090`), worth thirteen pivots in three workbooks; and Excel's `ref` excludes the filter rows while Calc's does not (`pivottablebuffer.cxx`:1421-1426 against `dpoutput.cxx`:892-907). | `probes/pivot-gen-r107`. **Reach over the eleven pivot-bearing documents: 265.25 → 24.62 of summed unsigned ink, 55 MAJOR pages → 4** — `alle einzeln` 225.44 → **0.07** (the reference's own `.ods` of it scores 0.02), `Keywords_Mapping` 10.79 → 1.68, `DynamicBubbleChart` 4.42 → 0.94, `049_Expenses_calculator` 2.18 → 0.90, `007_Contextures` 0.95 → 0.13, `037_Personal_money_tracker` 3.45 → 2.88, and 0.00 on the other five. Checked against 26.2.4.2's own `.fods` **in both directions** — an edge the reference states and we do not counts — over all sixteen accepted pivots: **31596 edges, 6324 styles and every indent agree, 0 MISS, 0 EXTRA, 0 WRONG**, of 38772 edge slots in those rectangles. Seven tests on a hand-written fixture, six of them failing at the base. Sweep: all **307** sheets-track documents at both binaries, **6 moved and they are exactly the six with an accepted pivot**, 64 `.xls` unmoved as the control. |
| O44 | **Both declined pivot layouts are modelled, and the one that was *not understood* was an oracle bug rather than a layout.** `check-grid.py` walked `table:table`'s direct `table:table-row` children, and a sheet with print titles puts rows inside `<table:table-header-rows>` in document order among them — so every row after one was read a place too early. 11 of the 58 sheets in the eleven pivot-bearing workbooks hold such a row and the three r107 declined are exactly the three it touches. Read correctly, **`mnHeaderSize == firstHeaderRow`** for the two values the OOXML path can produce (`SetHideHeader`, `pivottablebuffer.cxx`:1368; `dpoutput.cxx`:884-889) and `033`'s table lands on the stated `B5:N11` exactly. (b) A run of compact row fields is packed into one column by `GetColumnsForRowFields` (`:854-868`); besides the count, the button cell then takes `MultiFieldCell` and is **not boxed** (`:1087-1090`, `:794-812`), and each packed field adds an indent step to the fields inside it (`:1135-1137`, applied outside the `nField + 1 < size` branch, which r107's model had it inside). (c) The clearing is implemented: a pivot cell keeps nothing the workbook **hard**-states and everything its **Default cell style** states — `049_Expenses_calculator` states its justification and indent in `cellStyleXfs[0]` and the reference keeps all 47 such cells. | `probes/pivot-res-r108`. **19 of 19 worksheet-cached pivots accepted, 32012 edges and 6425 styles at 0 MISS / 0 EXTRA / 0 WRONG** against 26.2.4.2's own `.fods`, of 39880 edge slots in those rectangles (80.3 % stated; 2622 of the 32012 are the 40-twip rule). Clearing scored the same way over the 9970 cells: 81 alignment and 98 indent disagreements before, **0 and 0** after. Reach **24.33 → 21.01** over the eleven — `037` 2.88 → 1.25, `033` 12.36 → 11.27, `035` 1.75 → 1.13, `049` 0.90 → **0.92**, the rest level; **no MAJOR verdict moves**. The clearing itself costs 0.09 and that is recorded rather than hidden. Eight tests on a hand-written three-pivot fixture, six failing at the base. Sweep: all **307** sheets documents at both binaries, **5 moved and all five hold an accepted pivot**, 0 failures. ~~`<format>`/`dxf` is now a nil-reach census: 0 of the 28 pivot parts state one.~~ **That census was wrong and r110 retracts it**: it searched for `<format>` as a direct child of `<pivotTableDefinition>`, where it is a child of `<formats>`. There are **286 records across 12 of the 28 parts**, they are read now, and reading them takes `033_Event_planning_tracker` from 11.27 to 0.15 of summed unsigned ink — see O45. |
| O37 | **A BIFF sheet's shapes were dropped by three defects and none of them was the shape-to-`OBJ` pairing.** (a) Once a sheet's Escher stream passes the 8224-byte record ceiling Excel writes the rest as bare `CONTINUE` records interleaved between the `OBJ` and `TXO` records, and `BiffRecordReader.MoveNext` joined them into the `TXO` — LibreOffice turns continuation joining **off** for the whole drawing block (`XclImpDrawing::ReadMsoDrawing` calls `ResetRecord(false)`, `sc/source/filter/excel/xiescher.cxx`:4021-4052 in this tree) and `ReadTxo` takes its two by name at `:4242-4269`. (b) **A shape inside a group states no client anchor**, only an `msofbtChildAnchor` in the group's coordinate space, so every grouped shape in every `.xls` was dropped; the map is `SvxMSDffManager::ImportShape`'s at `filter/source/msfilter/msdffimp.cxx`:4318-4340 over `GetGlobalChildAnchor`'s union at `:5029-5045`, and a group's *own* shape takes no fill or line because `ImportShape` branches on `ShapeFlag::Group` before the branch that builds an item set (`:4376-4386`). (c) An embedded chart's substream carries a drawing of its own, anchored in quarter-thousandths of the chart's rectangle (`XclImpChartDrawing::CalcAnchorRect`, `sc/source/filter/excel/xichart.cxx`:4242-4290) and `ReadEmbeddedChart` stepped over it. A fourth rule came with them: a shape setting `fAutoTextMargin` (Escher 188) gets **20000 EMU on all four sides** (`EXC_OBJ_TEXT_MARGIN`, `sc/source/filter/inc/xlescher.hxx`:140) and one that does not gets its own stated `dxTextLeft`, which 26.2.4.2's own Excel 97 filter writes as zero. | `probes/sheet-pivot-r103` §1. `EHEST-Pre-departure-checklist.xls` draws **33 of the reference's 33** colour-stating shape spans against **0** at the base, within **0.13 pt in x and 0.92 pt in y**; ink **8.65 → 8.06**, 0 MAJOR both. Reach **9 of the 64 `.xls`, 0 failures**, and 60 `.xlsx`/`.ppt`/`.doc` renderings byte-identical. Two tests on two fixtures, both failing at the base. **The ranking column nets +0.14 over the movers and §3.4 says why**: `2012-GA-Survey-Chapter-6`'s one moving span lands on 26.2.4.2's x *exactly* while its `|ink|%` rises, and `apron-area`'s +0.63 is the pattern fill of O38. |
| O8 | **Frame capture, all three of its halves.** `bConsidered` is `bWrapThrough && !bTextBox` for a fly and `bWrapThrough \|\| !bTextBox` for a draw object, so a shape with **no text box** is never captured whatever its wrap; the area narrows from the sheet to the page **body** under `compatibilityMode` 15 at every relation but `PAGE_FRAME`/`PAGE_PRINT_AREA`; and **only where `mpAnchorFrame->FindBodyFrame()` finds one**, which a header, a footer and a footnote anchor do not. That last is what r85's wide rule was missing and what cost it `b053-19` and `Case-Study-Heathrow-Airport`. | `probes/words-close-r95`. 93 one-attribute fixtures at 26.2.4.2: **79 → 91 of 93** (2 unscoreable). Corpus reach **4 of 947 renderings and 0 of 676 converted words**; no page and no glyph moves on any of the four. |
| O9 | **`Body Text` and `caption` inherit the document's own `Normal`.** `PoolFormattingOf` answered a constant, which cannot express *Standard*; `PoolParentOf` answers `{None, Heading, Standard}` and the `\sbasedon` walk continues into style 0. | `probes/words-close-r95` §2. **Reach nil**: `caption` 0 of 338, `Body Text` 2 of 338 and neither moves, because both entries state their own `\fs` and a style's own `\fs` reaches no paragraph — 8 of 8 probes at the reference. |
| O20 | **The text and blank conditional-format families.** `containsText`, `endsWith`, `containsBlanks`, `notContainsBlanks` and `duplicateValues` are evaluated now, beside r94's `expression` and `cellIs`. Four arms are not what the specification would have you write, each read out of 26.2.4.2: `containsBlanks` is no mode at all but the substituted formula `LEN(TRIM(#B))=0` under `ScConditionMode::Direct` (`condformatbuffer.cxx`:860-866, :929-931), so three spaces are blank and a number never is; a text rule's `<formula>` is dead markup because only the `text` attribute is interned (:943-948); case is folded for a text cell (`IsValidStr`, `conditio.cxx`:1222-1229) and not for a numeric one (`IsValid`:1133-1145); and a multi-range `sqref`'s base cell is `ScRangeList::GetTopLeftCorner` over an ordering of `(tab, col, row)` (`rangelst.cxx`, `address.hxx`:396-399) — the smallest **column**, ties by row, not the componentwise minimum Excel writes the formula against. Precedence is document order between blocks (`ScDocument::GetCondResult` walks a `sorted_vector` of format keys) then priority within one (`CondFormat::insertRule` keys `maRules` by priority). | `probes/cond-format-r96`. Five fixtures under `tests/corpus/features/`, every expectation read out of 26.2.4.2's own rendering; all five fail at the base and pass after. Corpus reach **3 of 947 renderings**, sum `|ink|%` 27.62 → 27.02, no page count anywhere; `wiley-cancelled-title-list` 0.42 → 0.01 with its fill and font-colour operators 0/0 → 2/2 against the reference's 2/2. See N11 for why 1215 rules move three renderings. |
| O14 | **A `.ppt` shape resizes itself around its own text, and three rules had to land together.** Escher's `fFitShapeToText` — bit 1 of `DFF_Prop_FitTextToShape` (191) — becomes `SdrTextAutoGrowHeightItem` on every horizontal text object `svdfppt.cxx`:1049-1110 builds, and the height is `SdrTextObj::AdjustTextFrameWidthAndHeight` (`svdotxat.cxx`:44-236, and its custom-shape twin `svdoashp.cxx`:2249-2421): the outliner's height **plus one unit of tolerance**, clamped to the minimum frame height, and only then the two vertical distances. **The minimum is what decides whether a shape may shrink**: `makeSdrTextMinFrameHeightItem` is set only on the `SdrRectObj` branch (`svdfppt.cxx`:1116-1119, :982), so a real placeholder can only grow and a `TextInShape` custom shape shrinks to fit. Two reader rules had to land with it or the shape grew to the wrong size: a trailing return leaves an **empty paragraph** behind (`PPTStyleTextPropReader::Init` appends one more portion after its loop, `svdfppt.cxx`:5403-5409) and that paragraph is measured in the run **before** it, not in the extra character run a `.ppt` always states (the banner's are 40 pt then 8 pt and the reference measures two lines of 40); and a `PageTitle`'s returns are **line breaks**, not paragraph ends (`:5241-5246`, `:5261-5266`). | `probes/slides-r97` §1, **every figure re-measured after a container restart against a fresh clean build** (§6). Witness `pres_ioc_phuket.ppt` p26: 64.88 → **103.37** against 26.2.4.2's 103.38, both re-read out of the two content streams. Reach is the **whole `.ppt` population** — exactly 51 in 963 tracked corpus files, none outside `slides/` — of which **1691 shapes in 38 state the bit**, 1348 with text; the reference's own flat-ODP disagrees with the stated anchor by over a millimetre on **101 shapes in 22 documents**, 83 taller and 18 shorter. Gate `match` **49 of 51 before and after**, 0 page counts and 0 alphanumeric counts moved, 20 renderings changed; the gate re-run at the fresh build is **identical on all 51 rows including the md5s**. Sum `|ink|%` over the 20 **118.08 → 103.08**, MAJOR pages 37 → 29, 11 improved / 2 worsened / 7 level. Confinement **90 of 90** non-`.ppt` renderings byte-identical. **Three** tests fail at the base and 1045 of 1045 pass after. Two gaps recorded rather than closed: the growth census matches only **878 of 1667** grow-flagged shapes, and `MinimumFrameHeight` discriminates on the placeholder where `svdfppt.cxx`:1053/1118 discriminates on which branch built the text object, so a text-bearing **non-custom-shape** with the bit and no placeholder diverges — unmeasured. |
| O21 | **A cell's drawn text and its compared value are two different strings.** `RichStringPortion::setText` is `maText = AttributeConversion::decodeXString(rText)` and nothing else (`sc/source/filter/oox/richstring.cxx`:60-63), so every control character survives into the document model and the eliding `XlsxCellText.Of` describes is the *drawing* layer's. The chain to the comparison is `ScConditionEntry::FillCache` (`conditio.cxx`:804-859) keying on `lcl_GetCellContent` (`:766-802`), whose line 793 is `rCell.getSharedString()->getString()` — the raw string, **lowercased** into the cache at `:842`. So `"\tD5758620001301"` and `"D5758620001301"` are two `duplicateValues` keys, and `containsBlanks`' `LEN(TRIM(#B))=0` reads a lone `U+0009` as **not** blank because `TRIM` takes spaces and not tabs. `XlsxCellText.Stored` and `XlsxSharedStrings.StoredAt` keep the second spelling only where it differs from the first. | `probes/cond-format-r97` §1. Fixture `sheet-cf-stored-vs-drawn.xlsx`, seven cells, every expectation 26.2.4.2's own PDF; re-derived as failing at the base (`painted.Fill(0) should be null but was "#FFC7CE"`) and passing after. Reach **1 cell in 1 of 947 renderings, 2 spans**: `ST Capability List Rev.16` goes 18 `#9C0006` spans → **16** against the reference's 16, on the reference's own four pages, 217 pages throughout, `\|ink\|%` 26.85 → 26.80. |
| O22 | **`dataBar` is read**, in `XlsxDataBars` beside the colour scale. Five arms are not what 18.3.1.10 would have you write, each read out of 26.2's source and confirmed twice at 26.2.4.2 — against `--convert-to fods` of the real corpus documents and against an authored fixture. `minLength`/`maxLength` reach the bar only in the `NONE` arm (both) and the `MIDDLE` arm (`maxLength` alone) of `GetDataBarInfo` (`colorscale.cxx`:1008-1049 is the `AUTOMATIC` arm and contains neither), and `DataBarRule`'s constructor leaves the axis at `NONE` (`condformatbuffer.cxx`:351-356) — so an `x14:dataBar` with no `axisPosition` turns them off, because anything but `none`/`middle` becomes `AUTOMATIC` (`:1630-1643`). The extension's own `minLength`/`maxLength` are **never read**; `importDataBar` (`:1710-1715`) takes `gradient` and `axisPosition` alone, and all nine corpus rules say `minLength="0" maxLength="100"` while 26.2.4.2 writes `min-length="10" max-length="90"` for every one. Its `x14:cfvo` types overwrite the main namespace's, which is the only way `autoMin`/`autoMax` arrive (3 of the 9), and a `num`'s `<xm:f>` is taken only when it parses whole (`:1665-1703`, **no witness on either leg**). And **`mbNeg` defaults to *true*** (`colorscale.hxx`:107) — the OOXML importer only ever sets it true again (`:1658-1664`) and only the ODF importer clears it (`xmlcondformat.cxx`:483) — so `COL_LIGHTRED` (`colorscale.cxx`:1082) is not unreachable from OOXML but the **usual** answer, taken by any bar whose extension omits the element. `showValue="0"` takes the cell's value off the page rather than hiding it (`output2.cxx`:1691-1698) and does not change the row's height. | `probes/cond-format-r97` §2 and §4. Six fixtures, every expectation 26.2.4.2's own PDF; the geometry reproduces its bars to 0.03 pt on width and 0.12 on position. Corpus reach **7 of 947 renderings** (shared with O21's one), sum `\|ink\|%` 34.90 → **34.19**, six improve and none worsens, `088_To-do_list` 0.29 → **0.04** with its only MAJOR page gone, no page count anywhere. 940 of 947 byte-identical. **The `COL_LIGHTRED` arm is nil reach** — 75 numeric cells under a corpus `dataBar`, 0 negative. The `iconSet` half is **not** closed — it is re-seated as O25. |
| O6 | **An ODF conditional format replaces the whole of a cell's font, and only where a condition holds.** The applied style is a cell *style sheet*, and `lcl_populateresult` (`patattr.cxx`:608-637) asks `pCondSet->GetItemIfSet`, whose `bSrchInParent` defaults to **true** (`itemset.hxx`:201) — so the search walks the style's parents to `Default` and a rule stating only a colour hands back `Default`'s face and size. SpreadsheetML is exempt because `createDxfStyle` calls `ResetParent()` (`stylesbuffer.cxx`:3225-3234), which the same workbook in both spellings shows at the ink: the `.ods` draws its `#N/A` cells in Carlito 11 and the `.xlsx` in LiberationSans 9. **Round 95's two contradicting measurements were both right** — its clean probe's condition never fired, because `IsValidStr` is false for a numeric operand against a string cell; an *error* result is the number zero, which is why `#N/A` fires where `E` does not. `OdsConditionalText`; sistem 22 pages of 26 → **26 of 26**, one gate row gained and none lost over the 60 `.ods` that state a conditional format, 31 of 31 byte-identical outside them. | `probes/sheet-rows-r98` |
| O11 | **tdf#48041's half of the automatic interval cap, which `probes/chart-axis-r87` did not port.** `VCartesianAxis::estimateMaximumAutoMainIncrementCount` (`chart2/source/view/axes/VCartesianAxis.cxx`:1578-1600) formats every tick of the pass just finished through the axis' number format, counts the longest run of consecutive equal strings, and lowers the estimate to `m_aAllTickInfos[0].size() / (nMaxSameLabel + 1)` (:1611-1615). **Its comparison is seeded with an empty `OUString` (:1582) and the first tick is compared against it**, so an axis whose first tick draws nothing scores a repeat although no two labels are alike. `048_Expense_trends_budget` states `formatCode="#,##0;;"` over 0…500: eleven ticks, one repeat, `11 / 2 = 5`, and five intervals is the 1/2/5 ladder's 100 where ten is its 50. **The seat's own proposal — lay out at model size and scale the drawing — is refuted**: with the sheet's `fitToPage` and `pageSetup/@scale` alone removed the reference still steps by 100, at 180.29 pt of axis against 13.42 pt labels, where the length cap saturates exactly as it does at 120.80/9.04. | `probes/chart-fit-r97` §0 and §2. Four one-attribute variants at 26.2.4.2; three tests, one failing at the base. Reach **1 of 947** renderings: `048`'s axis goes `500 450 … 50` → **`500 400 300 200 100`**, pitch 15.102 → **30.204** against the reference's 30.199, `\|ink\|%` 0.24 → 0.23. The corpus's one other empty-section axis, `034_Personal_net_worth_calculator`, does not move and cannot be scored — it draws no labels at all. |
| O23 | **A fly bigger than its page is cut down to the page, and the frames did *not* coincide.** `SwFlyFreeFrame::CheckClip` (`sw/source/core/layout/flylay.cxx`:471-706, from `MakeAll`:251) gives up the position first (:514, :534) and squeezes only if that did not help, which for a frame that fits it always does — so the frames whose *size* it changes are exactly those bigger than the sheet. :571-585 cuts the two axes independently and :598-627 makes it proportional for a picture or an OLE object, the bigger change deciding when both axes are over. The escape is `SwAnchoredObject::IsDraggingOffPageAllowed` (`anchoredobject.cxx`:790-801) — `DisableOffPagePositioning` **and** wrap-through — and that setting's only setter in `sw/` is `WriterFilter.cxx`:333, the **OOXML** filter; `RtfFilter::setTargetDocument` (`RtfFilter.cxx`:191-195) sets nothing, so it is DOCX's alone. Two kinds never reach it: an as-character object (`SwFlyInContentFrame` is not a `SwFlyFreeFrame`, `flyfrms.hxx`:212 against :150) and a **drawing object, text box or not** — the fly of a TextBox pair holds the text and the shape that is drawn stays an `SwAnchoredDrawObject`, which is the opposite answer from O8's `bConsidered` and cost three documents before it was caught. | `probes/chart-fit-r97` §1 and §4. `023` states 682.10 x 493.50 pt on a 595.30 x 841.89 pt page and 26.2.4.2's own `.fodt` resolves **595.30 x 430.70**; five more extents and four rendered wrap/node variants agree. Fourteen tests, nine failing at the base. Reach **5 of 338** word renderings, 333 byte-identical, no page count moved: sum `\|ink\|%` 9.50 → **8.07**, `023` **2.83 → 0.59** with its eight pie labels going from 28.6-66.2 pt out to **0.22 pt**, `fleetfastfacts16nov2023` 0.74 → **0.04**, `b053-19`'s header picture 612.5 → **612.0** on a 612 pt page against the reference's 612.0. **`021` is worse, 1.84 → 3.47**, and is re-seated as O31. |
| O24 | **The six names O9 left out, and two nobody had named.** `hasByName` on the *unconverted* name is the second route into Writer's pool and reaches `Figure`, `Heading` and `Text` (`COLL_LABEL_FRAME`, applied to 17 paragraphs of `bulletin.rtf`); it accepts a pool style's **programmatic** name only, because `SwStyleNameMapper::FillUIName` appends `" (user)"` to a UI-only spelling (`SwStyleNameMapper.cxx`:306-335). Every paragraph pool style roots at `COLL_STANDARD` (`GetPoolParent`, `poolfmt.cxx`:169-298), so the question is what the *intermediate* adds, and that is decided by **when the style was created**, not which it is: `DomainMapper.cxx`:141 sets `StylesNoDefault` for the whole import and `GetTextCollFromPool`:676-677 then skips the per-style `switch` while keeping the parent linkage — so `COLL_HEADERFOOTER`, `COLL_HEADER`, `COLL_FOOTER`, `COLL_COMMENT` and `COLL_SIGNATURE`, created during the import, add **nothing** (measured, tab stops included), while `SwDocShell::InitNew`'s four (`docshini.cxx`:224-289) keep everything. `COLL_LABEL` therefore adds **italic, 12 pt and 6/6** — 12 pt because `InitNew` writes `FONTSIZE_DEFAULT` 240 over the block's own `PT_10` (`DocumentStylePoolManager.cxx`:978) — and `COLL_REGISTER_BASE` adds nothing although it is one of the four, because the size it wants equals the one it already inherits. **And *Caption* is a reference, not a constant**, whenever the document declares a `caption` of its own; both corpus documents applying a `COLL_LABEL_*` name do. | `probes/rtf-style-r97`. 116 one-name probes at 26.2.4.2 read out of the flat ODF, 36 test cases transcribed from them. **Reach 4 of 338 renderings, of which 3 change a pixel** — measured by rendering the whole `.rtf` column at `61bc19e03` and at HEAD under `SOURCE_DATE_EPOCH` and comparing hashes; 334 byte-identical. Twelve documents *apply* a name this round adds and eight of them do not move, because their entries state what the pool parent would have supplied (`whichproperty.txt`). |
| O30 | **A heading-parented style inherits the document's own `Normal` in every property *Heading* does not state.** `COLL_HEADLINE_BASE`'s pool parent is `COLL_STANDARD` — `poolfmt.cxx`:279-289, the `COLL_DOC_BITS` arm, verified line for line, where `COLL_HEADLINE1`…`COLL_HEADLINE10`, `COLL_DOC_TITLE` and `COLL_DOC_SUBTITLE` take the `default:` branch to `COLL_HEADLINE_BASE` and that takes the case above it to `COLL_STANDARD`. `FormattingOf` now adds `HeadingPool` to the chain and **continues the walk into style 0**, and `PoolInheritance` answers `HeadingPool.Over(Normal)`, so the merge is `entry.Over(HeadingPool.Over(Normal))` exactly as *Caption*'s already was. **The structural citation is not what settles it**: the intermediate states a font, `PT_14`, `PT_12`/`PT_6` and keep (`DocumentStylePoolManager.cxx`:769-820, the keep item at `:813`) and each of those shadows `Normal`, so which properties survive had to be measured one at a time. Bold, italic, underline, strike, capitals, colour and alignment pass through; size, space above, space below and keep do not. The face is modelled by omission, because no style's face reaches a run. | `probes/rtf-heading-r98`. **75 probes at 26.2.4.2** — twelve heading-parented names × five arms plus three controls — read out of `--convert-to fodt`; all twelve resolve as `Heading_20_N>Heading>Standard` and take `bold@Standard`, `italic@Standard`, `#ff0000@Standard`, `solid@Standard` underline and strike, `uppercase@Standard` and `center@Standard`, against `14pt@Heading` under a `Normal` of `\fs36`. **Reach 1 of 338 renderings, 337 byte-identical** — both legs swept in full under `SOURCE_DATE_EPOCH`. Twelve documents apply an affected name and **eleven do not move**: three gain nothing (their entries state everything `Normal` does), five gain only `\ql`, `\cf0` and `\lang`, which are the values the paragraphs already had, and three gain justification but apply their heading styles only to one-line paragraphs, where a justified line is also a last line. The one mover is scored by a pagination-independent instrument, `flushlines.py`, because its pagination already differs from the reference's (15 pages against 17) and a page-aligned ink score there is noise: **18 paired lines change flushness and 18 of 18 end up agreeing with 26.2.4.2**, agreement 256/294 → 274/294. Eleven test cases, seven failing at the base and four that must not. |
| O25 | **The BIFF `CONDFMT`/`CF` pair is read**, and the census it was seated on was short by a whole document. Round 95's `CONDFMT` walk reported 4 of 64 `.xls` and round 97 inherited that witness list; walking the same substreams again finds **`EHEST-Pre-departure-checklist` with 120 `CONDFMT` and 144 `CF`**, so the reference resolves **149 conditional formats and 187 conditions in 5 of 64**, 175 of them naming a style that carries ink — not 29 / 43 / 43 in 4. Four arms are BIFF's own and each is confirmed twice: the anchor is **`maRanges.front().aStart`** (`xicontent.cxx`:657), the *first* range and not `GetTopLeftCorner`, which the corpus discriminates (`EHEST`'s twelve-range block starts at `E46` and its top-left corner is `E1`; 26.2.4.2 writes `base-cell-address="E46"`); the area block's used-flags are **inverted**, a set bit meaning *leave it alone*, and a stated background with no pattern becomes a solid fill of it (`XclImpCellArea::FillFromCF8`); the 118-byte font block has **four different sentinels** and `EXC_CF_FONT_STYLE` means *unused* in one word and *italic* in another; and a formula is an RPN token array whose class fold is `(opcode & 0x1F) \| 0x20` — `opcode & 0x3F` turns `tRefV` 0x44 into 0x04 and rejects every value-class reference — with `tRefN`'s **column offset a signed byte** (`0xFE` is −2, not 254) and its row a signed 16-bit. The predicate itself is shared with the OOXML reader through `Layout/SheetConditions.cs` rather than written twice. | `probes/biff-reader-r98` §1-2. Two fixtures, every expectation read out of 26.2.4.2's own PDF; 5 tests fail at the base and pass after. Corpus reach **4 of 64 `.xls` renderings**, sum `\|ink\|%` over the 64 **463.84 → 324.36**, MAJOR pages 137 → 121, **no gate verdict and no page count moves**. `CF12` is not read and neither does the reference read it — 42 records, all matched by a `CF` that is. |
| O18 | **`Background_Declaration_Template.xls` 136.07 → 0.07**, MAJOR pages **15 → 1**, and the mechanism is measured rather than assumed. The base painted the cells' *stated* `#FFFFCC` where the reference paints the conditional format's `#969696`: on page 8, 208 840 pt² of it against the reference's 208 870, with the filled **area** already agreeing to 0.02 % and only the colour wrong, on thirteen of the twenty-five pages. So the conditional fills are the whole of the residual. | `probes/biff-reader-r98` §3. What is left is 0.07 on page 2 — one fill we draw and the reference does not, 0.51 % of the page, plus four displaced-glyph regions — and is recorded rather than re-seated. |
| O16 | **`TK-Syllabus`' row heights were one rule with fifteen witnesses, and the wrap was never a second cause.** Row for row against 26.2.4.2's own resolved heights, **82909 of 82924 already agreed** at the base — the brief's "534 missing lines over sixty pages" was a *drawn-line* count over a two-row page offset, not a height. The fifteen that disagreed are all +1 line, and thirteen are one rule: a **trailing empty paragraph** is a line of the row, which `LineCount` counted for a plain cell and the layouter dropped for a rich one (`"a\n"` lays out as one line). `RichLineRanges` appends the empty range; 15 → 2 disagreeing rows, `.xlsx` ink **205.57 → 158.42** and MAJOR **66 → 34**. Reach **10 of 63 candidates**, 0 of 61 controls; over the ten, summed unsigned ink **759.30 → 665.13** and MAJOR pages **318 → 252**, with **no gate column moving anywhere** — 10 of 10 identical page counts and alphanumeric characters before and after. | `probes/sheet-wrap-r99` §1-2 |
| O19 | **A BIFF `TXO`'s formatting runs are read.** They are in the *last* `CONTINUE` after the record — *second* was right for 98 of the corpus's 108 shape-path boxes and wrong for the ten `EHEST` ones whose characters need three, and the reader was always correct because it joins continuations and reads straight through (r100 §3.1) — eight bytes each — a character index and a `FONT` index — and the last names the index one past the string. Three rules came with them, all from `lclCreateTextObject` (`sc/source/filter/excel/xihelper.cxx`) and `XclImpString::AppendFormat`: a run index is into the **whole string, newlines included**, so the paragraphs must be cut and the runs distributed in one pass; a **repeated index replaces the font** rather than adding a run; and **text before the first run takes no font at all** (the loop starts with an empty item set), as does a `TXO` with no run array, because `XclImpTextObj::DoPreProcessSdrObj` branches on `IsRich()` = `!maFormats.empty()` and takes `NbcSetText` otherwise. | `probes/biff-reader-r98` §4. Fixture `sheet-shape-runs-xls.xls`, its 16 pt bold and 8 pt regular spans read out of 26.2.4.2's own PDF. **The reach is 9 documents, not the 17 that hold a text box**: pairing each `TXO` with its `OBJ`'s `ftCmo` type, 47 of the 155 boxes in 9 documents are **cell comments**, whose text goes through the note path and not the shape path — six of those documents' comments are hidden and drawn by neither renderer. Sum `\|ink\|%` over the 64 `.xls` **324.36 → 320.96**; `EHEST` 8.89 → 6.88 with its last MAJOR page gone, `TICAP` 5.39 → 4.93, `apron-area` 1.23 → 0.87. No gate verdict moves; the largest character movement anywhere is 3. |
| O27 | **A BIFF chart states its plot area and we were computing one.** `XclImpChChart::Convert` (`sc/source/filter/excel/xichart.cxx`:4030-4048) hands the **primary** `CHAXESSET`'s `CHFRAMEPOS` (0x104F) straight to `setDiagramPositionIncludingAxes` whenever `IsManualPlotArea()` holds and both modes are `EXC_CHFRAMEPOS_PARENT` — so what the file states is the **outer** rectangle, the one the axes' labels come out of, and no layout heuristic runs. The units are 1/4000 of the frame through `CalcHmmFromChartRect` (`:320-327`), `unit * n + gap` with `unit = (frame - 2*gap)/4000` — **and the gap is added to the width as well as to the position**. `gap = 250` hundredths of a millimetre, because `XclRootData`'s screen-pixel width keeps its **50.0** default when there is no active frame to ask (`xlroot.cxx`:105 against :150-163) and headless conversion has none; solved out of 26.2.4.2's own resolved model it is 249.7 and 249.4. Which `CHFRAMEPOS` is the plot area is decided entirely by where it sits — the corpus writes the same record under `CHTEXT` and `CHLEGEND`. | `probes/chart-resid-r99` §1. Ten tests, **five failing at the base**. Reach **7 of 307** spreadsheet renderings, 300 byte-identical, no page count moved; the census predicted 7 and the sweep found 7. Sum `diff%` **534.45 → 507.16** and `\|ink\|%` 81.15 → **78.96**; `Template Pilot Logbook` 4.66 → **1.72** with its plot area going from 36.36 pt too narrow to **0.25**, `014_Contextures` 1.11 → 0.50, one MAJOR page gone. `EHEST` rises on `\|ink\|%` alone (6.88 → 8.65) while every edge of its wall moves closer and its disagreeing pixels fall 12 % — the signed-mean cancellation of `clip-seats-r97` §2.2. The pie and 3D-axis-title branches of `:4040-4046` are **nil reach**: 0 of 16 BIFF chart substreams is a pie and the 2 that are 3D state no axis title. |
| O34 | **The overflow was never the defect: 26.2.4.2 draws an overflowing block and lets the page clip it, and round 99's cost was a hard line break sizing the line it ends.** The premise is refuted by census: over the 33 549 text shows the reference draws in the 51 `.ppt`, **91 have a baseline off the page — 10 above the top and 81 below the bottom, 0.27 %, in 10 of 51 documents** (ours 45 in 7), which no renderer that clipped a block to its shape could produce. `Inducement-to-Insurance-Business.ppt` p14 is the worked case: 26.2.4.2's own flat-ODP gives the title frame `svg:height="7.509cm"`, `draw:textarea-vertical-align="bottom"`, `draw:auto-grow-height="false"`, and the reference draws its topmost baseline **107.1 pt above the text area's top edge and 15.2 pt above the top of the page**. **The real cause**: EditEngine's line-metrics loop skips exactly one portion kind, `PortionKind::LINEBREAK` (`editeng/source/editeng/impedit3.cxx`:1498-1516, whose own comment names this case), the break being the line's last character (`:1441-1449`), while a line holding nothing but one is sized by the break's own character through the zero-height fallback (`EditLine.cxx`:69-71, `impedit3.cxx`:1478-1491). Round 99's `VisibleEnd`→`End` move is right and brought this in with it, because a line separator is trailing whitespace and `VisibleEnd` had been excluding it by accident. **Fixed in this tree** (`SlideTextLayout.MeasuredEnd`; `SlideLineBreakLineHeightTests`, 3 of 5 fail at the base). Measured at 26.2.4.2 on `make-break-probe.py` — five boxes, six swept sizes, `.ppt` converted by the reference itself — **base 15 of 30 cells beyond 0.10 pt, head 0 of 30**; the trailing-blank and visible-glyph boxes move identically at every size, which is round 99's own rule re-measured. Page 14's 29 shows go from 0 to **29 of 29 within 0.03 pt**, its alphanumerics 12 812 → 12 892 of 12 899 and `2015-Civil-Rights` 34 846 → **34 816, the reference's count exactly**. Column cost: `match` 49 → 49, page counts 0 moved, sum |glyph distance| **1417 → 1307**, sum |ink|% **252.82 → 250.10**, MAJOR 66 → 65, 9 renderings moved — which is 2.11 below the 252.21 round 99's debt was measured from. | `probes/slides-r100` §1; `offpage.txt`, `witness-p14.txt`, `break-probe.txt`, `gate-summary.txt` |
| O28 | **The baseline half closes: a fitted paragraph centres a small bullet in the box it would have had unscaled.** `Outliner::ImpCalcBulletArea` takes the box's height from `ImplGetBulletSize`, which **caches it on the paragraph** (`editeng/source/outliner/outliner.cxx`:1315-1355), and the autofit search formats the outliner once unscaled before walking `constScaleLevels` (`impedit3.cxx`:303-333) — so the call that fills the cache is the unscaled one, while `StripBullet` asks `ImpCalcBulletFont` again for the font it paints with (`:851-855`, :906-919) and that one does scale. **This checkout would not do it**: here `IsBulletInvalid` compares a stored `ScalingParameters` (`include/editeng/outliner.hxx`:154-172) and the checkout declares `27.2.0.0.alpha0+`, so C8 is live and the measurement is what decides. **Measured at 26.2.4.2 before the source was read**: `make-bullet-fit-probe.py`, one 24 pt bulleted body per slide in a box swept 60…420 pt so the search answers a different row each time — base **6 of 16** cells beyond 0.10 pt, head **0 of 16**, and the residual on the six scaled ones is `(1 − fontScale) × unscaledBulletHeight / 2` at every one (231, 203, 144, 115, 58, 58 hundredths of a millimetre against 231.0, 202.1, 144.4, 115.5, 57.8, 57.8 over five distinct scales) with the drawn sizes and body pitch agreeing throughout. The **control that re-aimed the round** is `make-bullet-probe.py`: over four stated sizes × six stated line-spacing percentages with `a:noAutofit`, **0 of 24 out at either leg on either format**, so neither the stated size nor the stated line spacing is where it lives. **Fixed in this tree** (`SlideTextLayout.BulletBoxHeight`; `SlideFittedBulletBoxTests`, the fitted case fails at the base and its unfitted control passes). **Reach, on the densest instrument the corpus has**: on the 263 pages whose show sequence agrees with 26.2.4.2's and whose every one of 3926 body shows sits within 0.10 pt of the reference's baseline, bullets beyond 0.10 pt go **315 of 1239 → 0 of 1243**, mean −0.2117 → +0.0071, range now [−0.057, +0.086]; over all 310 agreeing pages 426 → **73 of 1462 (5.0 %)** against the body's 388 of 4769 (8.1 %) on the same pages, so the bullet is now placed better than the text beside it. **Cost:** 50 of 51 renderings move and summed |ink|% moves **+0.18** (all of it two documents), `match` 49 → 49, sum |glyph distance| 1307 → 1307, MAJOR 65 → 65 — no gate column can see a bullet's baseline, which is why the census is what it is scored on. **Round 99's own §2.2 sample is withdrawn** — see C10. | `probes/slides-r100` §2; `bullet-fit-probe.txt`, `bullet-probe.txt`, `bullet-census-{base,head}.txt`, `gate-summary.txt` |
| O32 | **A merged cell's background is painted one column past the printed block, and not across the whole merge.** `ScOutputData::DrawBackground` accumulates the run's width over the merged columns and breaks at `nCol > mnX2 + 2` while adding the width of column `nCol - 1`, so the last column it can add is `mnX2 + 1` (`sc/source/ui/view/output.cxx`:1148-1172 **in this tree**, 27.2.0.0.alpha0+, not the reference binary's source). r99 read it as *the whole merge, clipped by the paper*, which its witness could not disprove — `TOGAF9`'s merge is two columns wide on a one-column block. Only the **origin** extends: it carries `ATTR_MERGE` and a covered cell carries `ATTR_MERGE_FLAG` and no count, so a merge beginning off the block to the left extends nothing. | `probes/sheet-last-r100` §2. Fixture `sheet-merge-fill-overflow.fods`, every figure 26.2.4.2's own PDF: a 4-column merge and a 3-column merge on a one-column block are painted at **the same 5 cm**, where the whole merge would be 11 cm and 8 cm and both fit the paper. `TOGAF9-Tool-ConfReqts-CSQ.xls` **16.75 → 12.92**, MAJOR **4 → 1**, pages 1/4/24/26 to **0.00** and none worse. Corpus reach **84 of 614 sheets renderings and 530 byte-identical, 0 failures**: over 4487 pages sum `\|ink\|%` **783.39 → 700.41** and MAJOR **346 → 275**, 51 improving, 27 level and **6 worsening** — three documents in both spellings, where the reference stops at the block edge and we go one past. **The obvious restriction to a merge beginning at the block's first column was built and measured and is worse**: over the six worseners and the ten largest improvements it gives 197.92 against 182.19 (base 237.08), because it hands back `043_Buy_a_car`, `ODs-February` and `DOE-C2M2` entirely. What separates the three worsening documents from the fixture is not established. Getting the origin rule wrong costs 0.65 on `TOGAF9` page 27, which is measured rather than argued. |
| O33 | **A BIFF shape run's colour is read and painted**, and the seat's arithmetic is corrected with it. `SheetShapeRun` carries a `Colour?`, `XlsCellFormats.StatedColour` resolves a `FONT`'s `icv` through the workbook's palette and answers null for `0x7FFF`, and `SheetShapePainter` draws `piece.Colour ?? Colour.Black` instead of always black. **r99's 43-against-119 was not the `CONTINUE` assumption**: correcting that moves the record count to **48** — the five it lost are in the ten `EHEST` boxes that use four `CONTINUE`s where the corpus's other 98 use two — and the rest of the gap is that a formatting run is not a span. The posture stays uncarried on N23. | `probes/sheet-last-r100` §3. Fixture `sheet-shape-run-colour.xls`, which is the `.fods` beside it put through 26.2.4.2's own *MS Excel 97* filter; four spans, two of them black controls, all four matched within 0.35 pt. Corpus reach **2 of the 64 `.xls` and 62 byte-identical**: `PC1000` 1.92 → **1.59** with its non-black span profile now the reference's exactly (43 of 43), and `TOGAF9` gaining the one `#ff0000` span the reference draws — which refutes r99's *0 non-black shape spans* reading of that workbook. **113 of the seat's 119 spans were never colour**: see O35. |
| O35 | `ChartSpline` is the flattening: a natural cubic spline parameterised by *point index* (not by x), sampled twenty times per interval, with a periodic spline where the ends coincide. Reproduces 26.2.4.2's own polyline to **0.0495 pt over 262 vertices** — 1.75 hundredths of a millimetre, the grid the reference's geometry sits on — against 0.170 for clamped ends, 1.822 for an x-parameterised spline and 1.818 for no spline at all, on a fixture the reference itself supplies. **The entry's reach figure was wrong in both directions.** By markup it is **12 of 947**, not 11: `mbSmooth( !bMSO2007Doc )` (`seriesmodel.cxx`:124) makes an *absent* `c:smooth` mean smooth, which is why the ODF-export leg said 12, and the three corpus documents with an absent one split exactly on `AppVersion 12.` with the reference's export agreeing on all three. In drawn geometry it is **2 of 947**: nine of the eleven witnesses state arithmetic-progression data, whose cubic spline **is** the chord (the reference's own flattening departs from its chord by ≤0.048 pt on all nine), and `055_Project_timeline`'s smoothed series is `a:noFill`. On the two that move, the mean distance from the reference's curve to ours falls on **6 of 6** series and roughly halves once the constant offset is removed; `\|ink\|%` at 512 px does not move, because the residual there is a 1-2 pt *position* error and a 44.7 pt axis error on one page, which is a different seat. | `probes/chart-smooth-r102` §1, §3 |
| O36 | **A chartex (`cx:`) chart is read and drawn.** `DrawingChartex` reads a `cx:chartSpace`: every `cx:series` naming a `cx:dataId` becomes a **line** series over the *first* data block's categories (`rTypeGroups.front()->createCategorySequence()`, `axisconverter.cxx`:290 — both witnesses' second block is one cell longer, the header row, and taking it spreads eleven points over twelve slots), a series naming none is dropped, and **no axis, tick, label, legend or title is drawn** because 26.2.4.2 draws none. `XlsxChartRanges` now expands a workbook-scope `definedName` whose value is one sheet-qualified area, which chartex needs and no `c:f` in the corpus does. **What the reference draws is far less than what it resolves**: its `--convert-to fods` gives a `ClusteredColumnChartType` diagram of four series (two with an *eleven*-cell range and two with none), eleven categories, two axes, wall and floor — and its page carries a white plot rectangle and two eleven-point polylines and nothing else, 1,964 and 2,029 ink pixels at 120 dpi. *(`chart-types-r101` §6's "4 data points" is corrected to eleven.)* | `probes/chart-rest-r104` §2. Reach **2 of 947**; `\|ink\|%` **0.14 → 0.00** and **0.50 → 0.03**, the drawn polylines within **0.05 pt** of 26.2.4.2's at every corner and their stroke colours identical; chart-region ink at 120 dpi 0 → 1,823 against 1,964 and 148,032 → 1,937 against 2,029. **Confinement: **2** of the 243 corpus `.xlsx`/`.xlsm` renderings move — the two witnesses — and **241** are byte-identical.** 8 new tests. |
| O38 | **The pie-of-pie has no second condition — the gate is `nPointCount >= 4` for both forms, and this tree already implements it.** Round 102 §4.5 is **retracted**: its instrument clustered filled marks by x and could not tell a pie-of-pie's two rings from one ring's wedges. `PieChart::createShapes` draws exactly two two-point connectors through `ShapeFactory::createLine2D` for `PieChartSubType_BAR` and `_PIE` and for no other case (`PieChart.cxx`:1073-1150), so the question is binary in the reference's own output. Over **28 one-attribute variants** of `029_Unit_Circle_Chart_Pie_Theme` at 26.2.4.2: three points give 0 connectors and one plain pie in **both** forms, four through twenty give 2 — and the wedge counts are **n + 1** for `pie` at every split position and **n − split + 1** for `bar` (9 against 8, 7, 6 and 4 at n = 8, splits 1, 2, 3, 5), which is `OfPieDataSrc::getNPoints` exactly. `ChartLayout.Plots.cs`:560 and :608 already hold it. | `probes/chart-rest-r104` §3, `o38-ofpie.tsv`. **No code changed**; `AnOfPieDrawsTheReferencesOwnWedgeCounts` is the measurement, 9 cases. |
| O40 | **An Escher pattern fill is a two-colour stencil, and painting its foreground colour over the shape is a solid block where the file states a hatch.** `EscherInk.Read` resolved `mso_fillPattern`, `mso_fillTexture` and `mso_fillPicture` to `fillColor`; they are the three `ApplyFillAttributes` resolves to `FillStyle_BITMAP` (`filter/source/msfilter/msdffimp.cxx`:1335-1339 in this tree). The tile is the `fillBlip` blip recoloured: guarded on the bitmap being 8 x 8 (`:1404-1406`), each pixel tested against `Color(0)` and written `fillBackColor` there and `fillColor` everywhere else (`:1432-1435`), both defaulting to `COL_WHITE` (`:1408-1414`); `fillOpacity` is a 16.16 fraction and becomes an `XFillTransparenceItem` for every style but a gradient (`:1367-1376`). A pattern states no tile size, so the tile is the bitmap's own eight pixels at the device's 96 dpi (`vcl/headless/svpgdi.cxx`:44) - 6 pt. `EscherPatternFill` builds it, `XlsDrawing.TextureOf` resolves the two colours through the workbook's palette, `SheetShapeInk` composes the `BitmapPaint`, and `SheetDrawing.HasInk` counts it - without that last hunk the shape is culled before it is placed and the rendering merely loses the wrong grey block. | `probes/sheet-draw-r107` §2. **The tile is confirmed against the reference's own resolved view rather than a rendering**: `--convert-to fods` writes each recoloured fill out as a `draw:fill-image` PNG, and our four 8 x 8 image XObjects match 26.2.4.2's four **on all 64 pixels of each**, at alphas 0.349/0.4/0.502/0.6 against its `draw:opacity` 35/40/50/60 % (`tile-check.txt`). Tile drawn 6.0000 pt against the reference's 5.9943 (`tile-pitch.txt`). Census over all 64 corpus `.xls`: **562 shape containers, 4 stating a bitmap fill, in 1 document** - and **none of the four is grouped**, so the stale *"2 shapes"* count is not explained by r103's group fix and no other explanation was measured. Reach **1 of 307 sheets renderings**, 306 byte-identical, 160 of 160 words/slides/`.ods` byte-identical: `apron-area.xls` `|ink|%` **1.51 -> 0.44**, MAJOR **1 -> 0**. Five tests. |
| O39 | **A `showValue="0"` cell was hidden at cell-build time and the row was measured as though it were empty.** The reference clears `bDoCell` in `ScOutputData::DrawStrings` (`sc/source/ui/view/output2.cxx`:1691-1698 in this tree), which is paint time, long after `ScColumn::GetOptimalHeight` has settled the row. The tree had two answers: `SheetDecoration.HidesValue`, asked by `SpreadsheetPages.DrawCell`, which is right and was already in the base, and `XlsxHiddenValues`, which dropped the cell's text in `XlsxSheetReader`. The second shortens the row for a reason unrelated to the string: a cell with no text is skipped before the pattern is looked at (`column2.cxx`:98-102), so it never reaches the branch that measures a **conditionally formatted** cell through the EditEngine rather than the arithmetic (`:936-941`) - and a `showValue="0"` cell is by construction covered by a conditional format. `XlsxHiddenValues.cs` is deleted; `XlsxIconSets` and `XlsxDataBars` already cover strictly more than it did. | `probes/sheet-draw-r107` §4. Drawn row pitch on `sheet-cf-icon-set-x14-custom.xlsx` **13.7764 -> 14.8819 pt** against 26.2.4.2's own `style:row-height="0.2071in"` = 14.9112 on all five rows (`row-pitch.txt`); base was 1.135 pt short, head is 0.029. **Corpus rendering reach nil and measured**: **3** documents state the attribute, not the 2 the seat named - the third on a main-namespace `dataBar` - and all three are byte-identical at both legs in the 307-document sweep. The coverage of the survivor is controlled by `SheetHiddenValueTests`, seven pre-existing cases over five fixture pages, all passing with the reader gone. What does move is **extraction**, towards the reference: `077_Inventory_list_with_highlighting` 1545 -> 1557 characters, the twelve added being the `1`s 26.2.4.2's own csv export has always written. Two tests. |
| O41 | **`EHEST-Pre-departure-checklist.xls`'s gauge plot rectangle was never `adjustInnerSize`, and the band arithmetic was never wrong: the chart was being composed in the sheet's *print-scaled* coordinates.** A BIFF or ODF chart is an OLE object with a page of its own — `<chart:chart svg:width>` in 26.2.4.2's own resolved view — and Calc lays it out on that page and then scales the whole object into the anchor the sheet gives it; `SheetChart` laid it out **inside the already-zoomed box** with the type sizes multiplied to compensate. Everything proportional to the zoom cancels under that. Three families do not: the BIFF unit grid's flat **250** hundredths of a millimetre off each edge of the *chart's* page (`XclChRootData::InitConversion`, `sc/source/filter/excel/xlchart.cxx`:1245-1251), which at 65 % becomes 385 and is **3.83 chart pt** too much on the left edge and on the top; every absolute constant in `ChartLayout` — `AXIS2D_TICKLABELSPACING`'s 100, `AXIS2D_TICKLENGTH`'s 150, a pie's flat 350; and the **96 dpi whole-pixel em** a chart's text is measured on, for which `ChartPlot.TypeScale` had been added as a one-consumer patch and which is now 1 everywhere. **The control that says the bands were right all along is an unscaled sheet**: on `014_Contextures_chart_sample_991ecfc5.xls` this tree's own outer and inner rectangles already reproduced 26.2.4.2's `<chart:plot-area>` and `<chart:coordinate-region>` to **0.35 pt on the worst of eight numbers**, with nothing changed — so no band model was ever at fault, and implementing `adjustInnerSize` literally on that path would have changed nothing, because it converges to `available − bands` in one step. `SheetChart.Draw` now composes at `box / scale` and maps the finished `ChartDrawing` — positions, sizes, stroke widths, dash patterns and type — onto the box. The page-15 gauge, drawn: **409.00 × 155.02 → 412.55 × 159.38** against 413.75 × 160.04, the left edge from 3.87 pt out to **0.01**, and the worst of the eight value-axis gridlines from 2.95 pt to **0.50**. In the chart's own points its outer rectangle goes 12.91, 39.25 → **9.10, 36.20** against the reference's 9.10, 35.55. | `probes/chart-inner-r110` §1. Reach **42 of 947 renderings move and 905 are byte-identical**, sum `\|ink\|%` 93.57 -> 92.64 over the 42 (20 better, 11 worse, 11 level), and **42 of 42 movers have the identical page count and the identical alphanumeric-character count on both sides**, so no gate verdict can move. The zoom half reaches spreadsheets only, and 34 of the movers are `.xlsx`. Residual: the last category label's half-width overhang, 6.74 chart pt against the reference's 4.98, which is the whole of the remaining 1.20 pt of width; and a chart's text is now *measured* at the chart's own size and *drawn* at the zoomed one, so the 96 dpi quantisation does not commute — `057_Simple_balance_sheet` goes 0.14 → 1.63 `\|ink\|%` while its plot rectangle lands on 26.2.4.2's coordinate region to **0.03 pt** (§1.6). |
| O42 | **The pie's second pass runs on the chart type — every pie and doughnut, nothing that states its own inner rectangle — and the of-pie's residual is an established LibreOffice defect this tree deliberately does not reproduce.** The gate is `if( bIsPieOrDonut )` (`chart2/source/view/main/ChartView.cxx`:682) over `xDiagram->isPieOrDonutChart()` (`:339-346`), with every call inside `impl_createDiagramAndContent` guarded by `!rParam.mbUseFixedInnerSize`; this tree gated it on `HasBestFitLabels`. Three of 26.2.4.2's own resolved views refute that: `027_Unit_Circle_Chart_Graphical_Chart`, whose labels are all `outEnd` and none best-fit, has a `<chart:coordinate-region>` **0.876** of its plot area's inscribed square and a *drawn* pie of **350.40 × 352.10 pt** where this tree drew 406.28 × 390.45; all **fifteen** corpus doughnuts have one of **0.9999**, so the pass runs on a ring and consumes nothing, because `AVOID_OVERLAP` becomes `CENTER` there and a ring's labels never leave the wall; and `003_Contextures_chart_sample`'s pie states `c:layoutTarget val="inner"` and is drawn at its stated 179.86 × 169.68, where running the pass on it gave 142.69 square. `PieConsumedRect` also gained the **of-pie's own drawn composition**, −1.4167 to +1.25 unit radii across (`PieChart.hxx`:258-269), which overflows the diagram square by design. Measured over the 37 corpus pie/of-pie/doughnut documents, 70 matched reference charts: **6 closer, 1 further, 63 unchanged** — and the one further is `pie-chart-template.docx`, which 26.2.4.2 draws as **a single image XObject**, so neither rendering can be scored against its coordinate region at all. **The of-pie is the LibreOffice defect and is not reproduced.** `createBarLabelShape` computes `nTextMaximumFrameWidth = ceil(0.8 × (m_fBarRight − m_fBarLeft)) = ceil(0.4) = 1` (`chart2/source/view/charttypes/PieChart.cxx`:780-782) — the two constants are unit-circle radii never transformed to screen, so a bar-of-pie's bar label is wrapped into a frame **one hundredth of a millimetre wide**; 26.2.4.2's own rendering of `028_Unit_Circle_Chart_Optimized_Graph` draws its caption as **26 single-character lines** at x ≈ 437.6-440.1, 10.47 pt apart. That column is 270 pt of the 267.6 pt of consumed height `adjustInnerSize` needs to reach the reference's 271.29 pt square from a 168.4 pt first pass; with bar labels wrapped the way a renderer not carrying the defect wraps them, both deltas are positive and the pass hands the whole 370.66 pt square back. So the pass now runs on `028` and moves its diagram **7.02 pt right** without changing its size, and the document's summed `\|ink\|%` against 26.2.4.2 goes **3.47 -> 3.58**, which is the measured cost of not reproducing the defect. | `probes/chart-inner-r110` §2 and §3; `pie-refrects.tsv` (96 reference charts), `pie-shrink.tsv` (70 matched). Reach shared with O41's, in the same 42-of-947 sweep: `027_Unit_Circle_Chart_Graphical_Chart.docx` **3.22 -> 0.84** `\|ink\|%`, `005_Contextures_chart_sample_6e279b08.xlsx` 1.91 -> 0.75, `bitesize-writing-a-report.pptx` 1.89 -> 1.68, `003_Contextures…xlsm` 0.32 -> 0.20, and against them `3495.pptx` 9.80 -> 10.25 (four pies whose *available* rectangle is already 100 pt too small, a legend defect this does not touch) and `021_Unit_Circle_Chart_3D_Pie_Chart.docx` 3.47 -> 5.18 **while its square goes 325.14 -> 274.30 against the reference's 279.41** -- that chart is a single image XObject in 26.2.4.2, which is r102's O31 ceiling and not a geometry regression. No page count and no glyph count moves anywhere. |
| O45 | **The pivot's own `<format>`/`dxf` records are read, and clearing was only half the rule.** `ScDPOutput::Output` ends with `maFormatOutput.apply` (`dpoutput.cxx`:1190), which lays those records over the generated styles — and **r108's census of them as nil was an instrument artefact**, a direct-child `findall` for an element that is a child of `<formats>`. There are **286 across 12 of the corpus's 28 pivot parts**. `XlsxPivotFormats` is `FormatOutput` transcribed: the `dxfs` table, the `<pivotArea>` and its references, the line matcher and the placement. Beside it the clearing is now **total** — the face, its declared generic class, the size and the colour from the `Normal` `cellStyleXf` (not `cellXfs[0]`, and not the full-width `<col>` format `SheetCellFormats.SheetDefault` answers on two of these workbooks), and `SheetFormatting.ClearBackgrounds` for the fill. **Three arms of the C++ tree read here are not in 26.2.4.2** and each was settled by applying one of `033`'s 84 records at a time and reading the reference's own `.fods`: `tryHandleGrandTotals` does not exist there, an `<alignment>` in a `dxf` reaches no cell, and `PivotAreaType` is parsed and never used. | `probes/pivot-fmt-r110`. Census over the 19 generatable ranges' **9878** cells, base rate beside every count: face **1**, declared class **93**, size **101**, colour **79**, fill **0**, border **0**. Scored **by value** against 26.2.4.2's own `.fods` — merge is what r108 shipped, clear is clearing with no `dxf` half: face 9877 / 9878 / **9878**; size 9872 / 9783 / **9878**; colour 9876 / 9873 / **9878**; fill 9806 / 9752 / **9869**. So clearing alone is *worse than merging* for the size and the colour, and only both halves match; the nine residual cells are one workbook's field-button row and corner. Reach: all **307** sheets documents at both binaries, **4 move** (`033`, `035`, `037`, `049`), 303 byte-identical, 0 failures; **no page count and no alphanumeric count moves anywhere**, so no gate verdict can; ink over the eleven **21.01 → 9.56**, `033` **11.27 → 0.15** with **its MAJOR page cleared** — that page's missing fill, 17.4 % of it, was r108's unexplained residual and is a `dxf` whose `<patternFill>` states a background and no `patternType`. Two authored fixtures under `tests/corpus/regression/`, nine tests, six failing at the base. *The sheet-wide half this row handed on is **O47**, landed in r111.* |
| O47 | **The sheet default is the `Normal` `cellStyleXf`, not `cellXfs[0]` — and unlike the pivot half of O45 the corpus can witness it.** A cell element states its format as `@s`, and LibreOffice reads an absent one as *no XF at all* — `rAttribs.getInteger(XML_s, -1)` (`sc/source/filter/oox/sheetdatacontext.cxx`:371) then an immediate return on a negative id in `SheetDataBuffer::setCellFormat` (`sheetdatabuffer.cxx`:721) — so the cell keeps what the sheet already carries: the row's pattern, the column's, else the document's **Default cell style**, which is the `cellStyleXfs` entry the `builtinId="0"` `cellStyle` names. `XlsxSheetFormats` used `cellXfs[0]`; it now interns `XlsxCellFormatTable.StyleDefault`, and a rich cell's runs resolve over the same base. **The rule is not a substitution**: `SheetDefault` is the `Normal` style *or*, on a sheet stating a `<col>` spanning to the last column, that column's format (`ScTable::ApplyPatternArea`'s `aDefaultColData`, `table2.cxx`:2980-2999) — so `SetSheetDefault` had to stop ignoring pool index 0, which is a legitimate answer there and means *not stated* only for the cell, row and column setters. **And it makes two readers of one question agree**: `XlsxStyles.DefaultFormatId` has taken the `Normal` route for the *number format* of an unstated cell since `probes/numfmt-r68`, while the drawn format took `cellXfs[0]` — a workbook separating the two entries by a `numFmtId` would have been extracted in one format and drawn in another. | `probes/sheet-default-r111`. Base rate **243 of 307** sheets-track documents (`.xlsx`/`.xlsm` carrying both tables; the other 64 are `.xls`). **6 differ in raw markup, 3 effectively** — the other three state an `<alignment>` whose every attribute is the schema's own default, which `applyAlignment` then applies to no effect. The three: `jobs-bulletin-51-22-december-2025` (declared generic class, unstated against *swiss*, on two otherwise identical Calibri 11), `sectors-defense-and-aerospace` (`vertical="top" wrapText="1"` against nothing; its four fonts are byte-identical, so its `fontId` difference is nil) and `Published_Issuances_2024` (`horizontal="left" vertical="top"` against nothing, and a full-width `<col>` over it anyway). **26.2.4.2's own `.fods` builds the `Default` cell style from the `Normal` entry in all six** — `bottom`/no-wrap where `cellXfs[0]` says `top`/wrap, `font-family-generic="swiss"` where it declares none — and on `regression/pivot-default-style.xlsx`, re-measured this round, `Plain` `A13:C13` (no `s`) comes back the `Normal` font while `A7:C7` (`s="0"`) comes back `cellXfs[0]`'s. **Reach on the page is nil and it was known before the code was written**: in the reference's own resolved view of all six, across all 24 sheets, **0 non-empty cells resolve to `Default`** — every cell carrying text states an `s` or sits under a `<col>` that does. Confinement over all **307**: 0 failed, **0 moved, every PDF byte-identical**, so no page count and no glyph count moves and no gate verdict can. Six tests, **two of them fail at `a362d41f9`**; suite 6706 + 542/552 fidelity (the known ten), 0 skipped. Left open with reasons: the same omission in `XlsxCellDecoration`'s fill/border (nil on this corpus, no witness), and the rich-text base still skipping the `<col>`. |
| O15 | **The residual holds no live seat, and round 112 took the last one out of it.** The block arithmetic and the line breaking stay exact (`fitedge.py`, 84 of 84 and 164 of 164) and the empty-line bullet-box floor is unchanged. Round 111 divided the eleven page by page; round 112 closed four of what was left and reclassified a fifth. **Register at head: 6 pages, 47.11 pt over 1534, 6 documents**, and every one of the six is already filed *not ours* or *terminal*: the **raster ceiling, three pages** -- `Thailand17` 8, `W3_Case_Study` 10 and `Fundamentals_Module_1_basics` 6, where the reference draws a PICTURE and this tree decodes the pasted metafile and draws the real characters, so ours is the searchable output; the **seventh confound, one page** -- `RRM-training-syllabus-...` 16, where 26.2.4.2 draws DejaVu Sans at DejaVu Serif's advances; and **two pages the instrument cannot reach** -- `2015-Civil-Rights-Website-training` 22 and `gfopportunitiesforlinkagespres` 27, where 26.2.4.2's own round trip moves its answer a full `constScaleLevels` row. **`ws_prod-...-France` 14 was NOT a layout page** and round 111's reading of it is withdrawn: both legs agreed at 20.01 because the reference's own ODP states the same label width the `.ppt`'s ruler does, so the round trip could not discriminate -- it is 18.99 at head, from the `.ppt`, on the ruler of O46. **No seat.** | `probes/slides-bullet2-r112`; `size-summary.txt`, `fourleg.txt`. Round 111's own record is `probes/slides-final-r111`. |
| O46 | **Closed. The `.ppt` picture bullet is read, it lands where the reference puts it, and it is sized from the height the reference sizes it from.** Round 111 implemented the master's `ExtendedParagraphMasterAtom` and the `ExtendedBuGraContainer` (`svdfppt.cxx`:3316-3349, :3255-3300), the merge's asymmetric arm and the box `round(fontHeight x 0.2540 x bulletHeight)`, and fixed pages 8 and 15 while losing `ws_prod-g-doc-Events-Part-M-presentation` 21 and leaving `ws_prod-...-France` 16. **Round 112 closed both, and one reader defect closed them together**: a shape whose text is an `OutlineTextRefAtom` keeps its own `TextRulerAtom` in its client textbox, and the reference remembers that record's offset BEFORE it patches the textbox header over to the referenced text (`svdfppt.cxx`, the `nTextRulerAtomOfs` block, and its use at :6713). This tree returned before it reached the ruler on that branch, so **121 rulers in 17 of the 51 `.ppt` were never applied** -- 54 of 54 in the France deck and 3 of 3 in Part-M, every one of them in a textbox that refers out. That is directly the label width (the ruler states `textOfs[0]` = 336 master units = 1.482 cm where the Body master states 216 = 0.953 cm; page 8 and page 16 go 70.09 -> **85.09** against 85.10) and, through `nHardCount`, the bullet's box: `ReadParaProps`' tail sets `PPT_ParaAttr_TextOfs`' mask bit when the ruler speaks for a level (`:5062-5068`), so France's paragraphs are hard and take their portion's height while Part-M's page-21 paragraphs are soft and keep their master char level's. `PptTextBody.NumberingIsOwn` is that count, with `BulletHeight` gated on `BuHardHeight` in both the mask and the flags word and the ruler counted for the two offsets. **Round 111's `size-source.txt` is superseded**: its three variants are one rule under the two branches of `nHardCount`, and its reading of `0x1801` as stating none of the seven is wrong -- `PPT_ParaAttr_BulletOn` is bit zero and is the first of them. **Confirmed twice**: against 26.2.4.2's own flat ODP of the Part-M deck, whose 25 `text:list-level-style-image` heights take four values at level 1 (28/24/20/18 pt at 155 %) and **one** at level 2, 0.671 cm = 24 pt at 110 %, on all thirteen list styles that have one although its depth-1 paragraphs state a hard 18 pt portion; and against its PDFs, where head reproduces every drawn bullet on France 8/14/15/16/44 and Part-M 21 exactly -- 17.86, 22.31/15.85, 22.31/15.85, 20.10/14.26, 20.10 and 31.24/19.02. **All four O46 pages fixed, no page open.** What is left unimplemented is one term of `GetAttrib` and it is narrower than round 111 recorded: the source-instance-against-destination-instance master level comparison, reachable only by a HalfBody or QuarterBody text (`:5954-5958`, `:1021-1024`), which no page of this corpus's picture-bullet set is. The other term round 111 named, a destination instance of `TSS_Type::Unknown`, is resolved: `ProcessObj` always produces one (`:999-1028`). | `probes/slides-bullet2-r112`; `fourleg.txt`, `rulercensus.txt`, `confine.txt`, `ink-ppt.txt`. `PptPictureBulletTests` 22, `PptOutlineRulerTests` 22. |
| O48 | **Closed. A `.ppt` table's cells never autofit.** A `.ppt` writes a table as a plain group of rectangles, each an ordinary Body-kind text shape that `svdfppt.cxx`:1030-1099 gives `TextFitToSizeType_AUTOFIT`; the reference then throws the group away and builds one `SdrTableObj` (`:2913`, `CreateTable` at `:7569`), copying each member's `OutlinerParaObject` into a cell and **nothing else of its item set** -- `ApplyCellAttributes` (`:7412`) carries the four text distances, the two adjusts, the writing mode and the fill, `SDRATTR_TEXT_FITTOSIZE` is not among them, and `svx/source/table` mentions autofit nowhere at all. **Confirmed at the binary too**: in 26.2.4.2's own flat ODP of `Thailand17.ppt` the deck's autofitted bodies export `style:shrink-to-fit="true"` and none of page 11's 54 `table:table-cell` styles does, all 42 of its spans stating a flat `fo:font-size="12pt"`, and in its PDF all 77 body spans are drawn at 11.99 pt. This tree drew the 35 spans whose cell wraps to two lines at 11.00 -- `round(12 x 0.925)`, `constScaleLevels`' second row -- and now draws **548 alphanumerics at 11.99 against the reference's 548**, exactly. `PptShapeGeometry.TableProperties`/`TableRowProperties` (927/928, read from the group descriptor's tertiary table, `nTableProperties & 3` beside a non-empty row array), `PptSlideLayout.IsTableGroup`, `Context.InTable` and one arm of `Autofits`. Reach censused in advance: **34 table groups in 10 of the 51 `.ppt`, 1089 members, 391 of them Body-kind in 5 documents**, and all five move. **1 page fixed.** **What is left is the row heights and it is a separate, smaller thing**: the reference re-derives them through `TableLayouter` (39.20 pt header and eight rows of exactly 36.20, filling the frame) where this tree draws the group's own rectangles (38.75 and 35.87, leaving 3.05 pt unused at the bottom) -- 0.33 pt per row, cumulative, moving no character's size and no page. | `probes/slides-bullet2-r112`; `tablecensus.txt`, `fourleg.txt`. `PptTableCellFitTests`, 16 tests. **1 page fixed, 0 open; the row-height residual is named and measured in `results.md` §5.** |
| O73 | **An Escher `adjustValue` is discarded for every preset but `roundRect` and `triangle`, so a `.ppt` shape is drawn at the DrawingML preset's own default however the file states it.** Seated by round 124, closed by round 127. The conversion is **per preset and is not a constant**, and which presets it is sound for is now a measurement rather than a reading: all 148 name-mapped `MSO_SPT` values were expanded by **26.2.4.2 itself** -- a flat ODF naming each type and stating only `draw:modifiers`, which `SdrObjCustomShape::MergeDefaultAttributes` (`svx/source/svdraw/svdoashp.cxx`:814-900) fills from `GetCustomShapeContent`, exported as SVG -- and by this tree, at three IN-RANGE values of `adjustValue` (2000/5000/9000, inside the 0-10800 an Escher one-handle preset's own `SvxMSDffHandle` declares) and at two aspect ratios. `value x 100000/21600` reproduces the reference at all six points for **18** types and is **wrong for 82 of the 105** that have an adjustment at all; applied blanket it takes the count of types drawing the same shape from 92 to 68. Three more -- `parallelogram`, `hexagon`, `trapezoid` -- need the extra factor **`w/ss`**, because Escher measures across the WIDTH and DrawingML across `ss = min(w,h)`; with it they are right at 1:1, 2:1 and 1:2 alike. `PptShapeGeometry.AdjustmentInViewBox` and `AdjustmentAcrossWidth` are those two sets and `Adjustment` now takes the shape's size. **Testing outside the stated range tests the clamp**: a first pass at 15000 reported `roundRect` -- correct for twenty rounds -- as wrong. | **Reach re-scored, and 80 of round 124's 158 was never real.** `straightConnector1` (28), `curvedConnector2` (50) and `bentConnector2` (2) carry an EMPTY `SvxMSDffCalculationData` span and a null `pDefData`, so 26.2.4.2 ignores property 327 on them too -- **nil reach**. A further **57** are `bentConnector3/4` and `curvedConnector3/4`, which pass the unit test and are still declined: `msdffimp.cxx`:4391, 4792-4888 throws the custom shape away and draws an `SdrEdgeObj` with both ends reset to the bound rectangle, and honouring the value moved the interior-vertex residual against 26.2.4.2 from **6.63 pt to 8.73 pt** over 11 paired polylines (whole-page ink worse on all three decks). **14** are measured `rescale-wrong` (`wedgeEllipseCallout` 10, `wedgeRoundRectCallout` 2, `leftArrow` 1, `rightBrace` 1), **2** are WordArt that `PresetOf` does not map, and **5 are the trapezoids, fixed** -- see O74 for the before/after. The other twelve decks stating an adjustment render identically before and after (mean ink 0.0000, worst 0.0001). `probes/pptgeom-r127/results.md` SS2-3, `adjust-conversion.tsv`, `connscore.py`. |
| O74 | **Escher's `mso_sptTrapezoid` and DrawingML's `trapezoid` are VERTICAL MIRRORS of each other, and this tree mapped a binary preset onto the DrawingML one of the same name, so a `.ppt` trapezoid was drawn upside down.** Seated by round 124, closed by round 127. `PptShapeGeometry.MirrorsVertically` names the preset and `PptSlideLayout.Mirrored` turns the expanded geometry over **in the shape's own coordinates, before the placement matrix** -- which is where `fFlipV` lives, and is the whole of the fix: the flag was always read correctly and was being applied to a base that was upside down. Witness `ws_prod-...-European-Safety-Strategy-Initiative.ppt` p8: before `284.625 403.125 m 375.219 40.750 l 611.906 40.750 l 702.500 403.125` (wide edge at the page TOP, narrow edge **236.688 pt**), after `284.625 40.750 m 455.373 403.125 l 531.752 403.125 l 702.500 40.750` against 26.2.4.2's `284.627 40.791 m 702.539 40.791 l 531.751 403.172 l 455.386 403.172` -- narrow edge **76.378** against **76.365**. All **four** drawn trapezoids of that deck (pp 7, 8, 9x2) now agree corner for corner to **0.047 pt**, and pages 7 and 8 are drawn the opposite way up from each other on both sides, which is the proof the flag was never at fault. Document ink against the reference **8.936 -> 6.045**, improving on exactly the three pages that hold a trapezoid (15.45->2.22, 22.32->14.36, 22.34->14.62) and on no other page. | **THE CENSUS round 124 left open is done and the answer is ONE.** Of the **148** types `PresetOf` maps by name, 147 have an Escher definition; expanded by 26.2.4.2 and by this tree in a square box and compared under the identity and all three reflections: **92 identical, 50 differ in a way no reflection accounts for, 5 fit a reflection, 1 has no Escher geometry at all**. Four of the five are the `callout3` family, whose path templates are IDENTICAL -- all eight coordinates are adjustments -- and whose defaults happen to be reflections (`mso_sptCalloutDefault4` puts the tail at x=+1.083..+1.167, DrawingML at -0.083..-0.167), **and whose adjustment pairs are transposed: Escher states (x,y), DrawingML (y,x)**. So **1 of 147 is a mirrored table**. Reach correction to this row's own figure: the 5-in-2 is a declaration count -- `outlook_of_nigerian_pension_sector.ppt`'s single type-8 shape is drawn by NEITHER side, so the drawn reach is **4 shapes on 3 pages of 1 document**. `probes/pptgeom-r127/results.md` SS1, 4, `preset-census.tsv`. |
| O80 | **Closed. A picture's own `a:prstGeom` never reached the clip, so a photograph Word put in a circle was drawn as a square photograph with a circle round it.** `oox`'s `Shape::createAndInsert` turns a `GraphicObjectShape` into a `CustomShape` whenever `representsDefaultShape()` is false — any preset but `rect`, or an `a:custGeom` — and the bitmap becomes that shape's fill; 26.2.4.2's own PDF emits the preset outline as a clip path immediately before the image operator, and a `rect` picture's as a plain `re`. `PageDrawing.DrawPicture` clipped only where there was a crop, and only to the rectangle. `SlideDrawing.DrawPicture` has clipped to the outline all along, so this was a cross-format asymmetry. **Reach 4 of 272 `docx`, 13 shaped pictures**; 70 more in 7 `pptx` were already right, and the Escher and `custGeom` channels are 0. `079_Storyboard_Template_Simple_Format` 1.67 -> **0.16** |ink|% and leaves the MAJOR set; `075_Storyboard_Template_Fillable_Format` 0.71 -> **0.38**. A before/after byte comparison over all 35 candidate documents moves exactly those 4. | `probes/wordsgroup-r128` SS1, `pic-preset.tsv`, `shaped-picture.tsv`, `self-vrt.txt`. `FrameShapedPictureTests`, 5 assertions, 3 failing at the base. |
| O82 | **A horizontal edge two table cells share is ONE border and both of them get it, and this tree gave each cell only what it stated -- so at the top of a table's continuation page the line came out drawn in pieces.** Seated by round 126, closed by round 131. A cell stating `w:top w:val="nil"` under a cell stating a bottom border is still separated from it by that border; on a page where both rows are drawn the row above's bottom covers the line, so the only place it shows is where the two rows are on different sheets. Measured at 26.2.4.2 on an authored ten-arm probe rendered three times into three profiles, identical: the winner is the **wider** of the two facing edges, `nil` on both sides is nothing, the resolution is **per column** (a row whose two cells disagree gets half a line), and a table's page cut carries a full-width line whatever the cells state. `PageTable.RowsWithSharedEdges`, read by `PageDrawing.DrawBorders` alone -- the row heights deliberately stay on the stated rows, which is **O84**. | Words-track gap census **2 605 -> 1 094 pt**, 7 documents -> 6, with the mirror direction unchanged at 3 201 pt; `review-welsh-…-mandelson.docx` **1 511 pt over 8 pages -> 0**. **31 of 338 renderings move, 0 page counts and 0 alphanumeric counts.** Base rate: 183 of 272 corpus DOCX hold a table, **63 state 17 656 shared edges one side nils**, and the defect reaches the continuation pages alone. The residue is re-classified rather than left: 626 pt is **O85**, 272 pt is a text rule of O71's family, 145 pt is a document 77 pages against 78, and 92 pt is the census pairing two objects 1.9 pt apart. `probes/wordstable-r131/results.md` |
| O81 | **Closed. The WW8 half of O72 -- and the rule is NOT the DOCX one: 26.2.4.2 refuses exactly one character style inside a `TOC` field's cached result, the built-in `Hyperlink` one, keyed on its `sti`.** `SwWW8ImplReader::Read_CColl` is the `sprmCIstd` handler and returns without applying the style when `m_bLoadingTOXCache && m_vColl[nId].GetWWStyleId() == ww::stiHyperlink` (`sw/source/filter/ww8/ww8par6.cxx`:4135-4160), under a comment saying why -- *"for the hyperlinks inside TOX in MS Word is not same with a common hyperlink Character styles: without underline and blue font color"*. So `FollowedHyperlink` (sti 86) and every style of the document's own are KEPT, and the run's direct sprms are kept in all cases; modelling `DomainMapper.cxx`:3037-3047's drop-every-style rule here would have been wrong three ways. **The three `ww8par5.cxx` lines the seat cited (2334, 3362-3363, 3653) are the `Index Link` pool style going on the links the reference BUILDS and are not the suppression.** Three pieces: `Ww8Style.Sti` (the low twelve bits of the STD's first word, which the stylesheet reader was discarding), `Ww8DocumentReader.IndexResultRanges` + `_indexResults` (the result's cp ranges, paired ahead of the walk and saved/restored around a nested story), and an `inIndexResult` flag on `ApplyCharacterException`. | **Σ|ours − reference| of rule cover on the 10 contents pages of the 5 `.doc` that have one: 34 650 -> 677 pt.** `150_5335_5a.doc` **29 251 -> 0 against 0**, `361400CSLegislation1RF01PUBLIC1.doc` 4 960 -> 238 against 146 (the residue is O84, not this); the three controls byte-identical. Reach **2 of 66 corpus `.doc`**, against a base rate of **2 of the 4 TOC-bearing `.doc` where the change could fire and correctly does not** (267 and 256 characters inside a result, naming no character style at all) and 62 carrying no such field. 0 page counts and 0 alphanumeric counts move. `probes/ww8toc-r130/results.md` §1-2, `toc-cover-{base,final}.tsv`, `toc-census.tsv`, `Ww8IndexLinkStyleTests`. |
| O79 | **Closed. The WW8 walk dropped the anchor character of EVERY frame it made, so two adjacent as-character pictures reached the layout with one offset between them -- and an inline object's offset is a boundary, so there was nothing to break at.** Writer inserts an anchor character for `FLY_AS_CHAR` and no other anchor (`SwFormatFlyCnt`/`GetCharOfTextAttr`, `sw/source/core/txtnode/thints.cxx`:3633-3652); `CollectFrame` now reports whether its frame is as-character and the walk emits the anchor for that case alone. It costs no width -- `ShapingControls.IsRemovedBeforeShaping` drops the whole C0 range, and `word-features.doc` draws *"After the box."* at 304.06 pt either way, which **withdraws** `DocShapeFieldTests`' standing 18.67 pt-of-`.notdef` claim. **A third change was needed and a fixture found it, not the corpus**: a line holding nothing but anchors must take no text ASCENT either, because the anchor character IS the object in Writer (`SwTextFormatter::NewFlyCntPortion`), and without that a `SHAPE`-field frame -- whose ascent is nought -- put the paragraph font's 11.2 pt above a logo-only running head, costing `picture-furniture.doc` two lines and 21 words of page 1. `MeasuredParagraph.MeasureLine` already zeroed the descent under `HoldsNoText` and now zeroes the ascent under the same test; `InlineAscent` is non-null in exactly three places, so nothing but a WW8 `SHAPE`-field frame can reach it, and slides and sheets pass no inline objects at all. | **`RMI_…GettingOffOil.doc` p2: ours y 115.10-373.95 and 99.60-373.95 (sharing a bottom edge) -> 99.60-358.45 and 358.45-632.80, against 26.2.4.2's 99.70-358.35 and 358.50-632.70** -- within 0.10 pt on both. |ink|% **10.85 -> 0.83**, MAJOR pages 2 -> 0. The defect's own drawn signature (two images sharing a bottom edge and overlapping) goes **1 of 66 `.doc` -> 0 of 66** against a reference base rate of **0 of 66 `.doc`** -- and **2 of 338 words documents, 10 pairs, both `.docx`**, which corrects r124's *"base rate 0"* to a population statement. Confinement: **6 of 66 `.doc` and 3 of 272 `.docx` renderings move, 0 of 338 converted `.odt` and 0 of 338 converted `.rtf`**, 0 page counts and 0 alphanumeric counts anywhere. `probes/ww8toc-r130/results.md` §3-5, `InlineObjectPairTests` (a `.doc` case that fails at the base). |
| O86 | **Closed. A WW8 toggle sprm is resolved against the style the SAME CHPX names, resolved on its own -- never the accumulated value and never the paragraph style when a character style is named.** `SwWW8ImplReader::Read_BoldUsw` (`sw/source/filter/ww8/ww8par6.cxx`:3117-3156) starts at `pSI = GetStyle(m_nCurrentColl)` and replaces it outright when the CHPX carries a `sprmCIstd`, then inverts on operand 0x80/0x81 only if that style's `m_n81Flags` bit is set -- and `m_n81Flags` is seeded from the base style (`ww8par2.cxx`:3825) and set by the style's own CHPX, so it is that chain walked from NOTHING. A style stating no weight therefore answers *not bold*. `Read_BoldUsw` never asks whether the style was applied, so it resolves against `Hyperlink` on exactly the runs O81 declines to apply `Hyperlink` to. `ApplyLayoutSprms` takes a `toggleBase`, `StyleToggleFormat` is `m_n81Flags`, and the same rule is on the content path. **The confound was ruled out first**: 26.2.4.2's own `--convert-to fodt` states `fo:font-weight="bold"` on those runs and `"normal"` on the two tabs beside them in the same paragraph, so it is an attribute and not L1/O57 face resolution. | **`150_5335_5a.doc` page 3 goes from 0 glyphs in `LiberationSerif-Bold` to 521 against the reference's 523**, and the whole document 924 -> 4224 against 4244; its |ink|% **19.52 -> 17.26**, MAJOR 6 -> 5. Reach with its base rate over all 66 corpus `.doc`, every story: **15 documents, 580 CHPX, 6822 characters** carry a toggle relative to a style AND a `sprmCIstd`, of which the two readings **disagree on 2 documents, 472 CHPX, 3665 characters**; the base rate is **65 of 66 documents and 12 804 CHPX** stating the same toggles with no `sprmCIstd`, where the two readings must agree. **Confinement: 7 of 66 `.doc` and 0 of 272 `.docx` move, 0 page counts and 0 alphanumeric counts of 338.** `probes/ww8char-r135/results.md` SS1, `toggle-census.tsv`. |
| O87 | **Closed, and the mechanism is established rather than assumed. A character attribute still open where a `TOC` or `INDEX` field begins is not drawn by 26.2.4.2 -- it is the whole CHPX that goes, not the underline, and only the two field types that reach `Read_F_Tox` do it.** `Read_F_Tox` moves the insertion point backwards into the index section it has just inserted (`ww8par5.cxx`:3531-3533) while every attribute of that CHPX is still on `m_xCtrlStck`, so each closes in a node it did not open in. **Six one-field variants of `361400CSLegislation1RF01PUBLIC1.doc`, each read back through 26.2.4.2's own `--convert-to fodt`**: as authored, no underline; field type 13 -> 9 (no handler), underline; the CHPX FKP's `rgfc` entry 2207 -> 2206 so the run closes at the paragraph mark, underline; `sprmCKul` replaced by `sprmCIco` 6, no colour; that colour with the retyped field, `fo:color="#ff0000"`; type 8 (`INDEX`) loses it and types 3 (`REF`) and 88 (`HYPERLINK`) keep it. The paragraph style's own character half survives throughout. Reproduced deliberately, as `Read_CColl`'s `Hyperlink` suppression is. `IndexFieldStarts` + `StrippedExceptionRanges`. | **Rule cover on that document's contents page 238 -> 146 pt against the reference's 146**, so the 92 pt round 130 left is nil and SS|ours - reference| over the ten contents pages of the five contents-bearing `.doc` goes **677 -> 585 pt**; |ink|% 1.12 -> 1.08. Reach: **4 of 66 `.doc` hold an index field, 3 of the 4 have a CHPX run ending exactly at its begin and all three carry a drawable sprm** (18, 1 and 1 characters), of which 2 move a rendering. Base rate the rule must NOT touch: **1526 CHPX runs end exactly at a non-index field's begin, in 38 of 66 documents, 1294 of them drawable** -- and arms F and G show 26.2.4.2 keeps every one. `probes/ww8char-r135/results.md` SS2, `variants.md`, `field-census.tsv`. |


| O89 | **A worksheet shape whose own fill is a picture was drawn with no fill at all — FIXED in round 138.** `XlsxDrawings.ReadAnchor` returned early for a shape because its `picture` is the `xdr:pic` element and there is none, so an `a:blipFill` in the shape's own `xdr:spPr` reached nothing; `XlsxShapeInk` carries colours and deliberately would not approximate a picture fill. The shape's own `blipFill` is now the source for the existing picture path (the blip choice, `a:alphaModFix` and `a:srcRect` are already done there). **`a:stretch` only** — a stretched bitmap fills the shape's rectangle, which is what that path draws; `a:tile` is a different geometry and is still left unfilled rather than painted stretched. All **10 of 240 `xlsx`** now match the reference's image count, worst placement delta **0.11 pt** over the six numbers of the `cm` matrix. Confinement: the `xlsx` track rendered twice under `SOURCE_DATE_EPOCH` moves exactly those ten and leaves **230 byte-identical**, with **0 page counts and 0 alphanumeric counts moving** — no gate column can see it, before or after. Pinned by mutation; fixture `features/sheet-shape-picture-fill.xlsx` validated against 26.2.4.2's own placement. *The seat said 10 of 241; the denominator is 240.* | `probes/sheetfill-r138/results.md` |

| O92 | **A number format's colour clause was discarded, so a negative in `#,##0 ;[Red](#,##0)` was drawn black — FIXED in round 139 for the OOXML readers.** `NumberFormatSection` dropped every bracketed body it did not recognise, under the comment that a colour *"changes appearance rather than the text this extracts"* — true of the text, false of the ink. The section now carries a `Colour`, and `SheetTextLayout` takes the colour of the subformat **the cell's value selects** in preference to the cell's own. The ten keywords and their values are LibreOffice's, in its order (`ImpSvNumberformatScan::StandardColor`, `svl/source/numbers/zforscan.cxx`:98-111, 145-147) — note **`[Blue]` is `COL_LIGHTBLUE` `#0000FF`, not `#000080`**, and `GREY` is the spelling it keys on. Confirmed at the binary: `[Red]` draws `#ff0000` and `[Blue]` `#0000ff`. Reach reproduced exactly on the figure the seat said to quote — **8 documents, 137 cells** (RED 115, COLOR10 18, BLUE 4); the sheets track rendered twice under `SOURCE_DATE_EPOCH` moves **7 of 307** and leaves 300 byte-identical, and the eighth selecting document does not move *because it should not* — its red is explicit cell font colour and this tree already matched 26.2.4.2 there at 4838 red spans, 686 black, 343 pages. **`[COLOR n]` is deliberately refused**: it resolves through `GetUserDefColor(n-1)` to the running installation's own `share/palette/standard.soc` (`sc/source/core/data/documen9.cxx`:246-259), whose index 9 is the `#dddddd` 26.2.4.2 paints — vendoring that would pin us to one installation's UI palette. Gap 18 cells in one document. 23 tests, pinned by two mutations. *No gate column can see any of it: a colour adds no glyph and no page.* | `probes/numfmtcolour-r139/results.md` |
| O90 | **A `w:object`'s replacement picture was drawn twice, because the filter meant to stop it could never be false — FIXED in round 140.** `DocxVmlFrames.TopLevel` selected with `IsShape(child) is not false` against a predicate that answered `true` or `null` and **never `false`**, so the only condition doing any work was the group test beside it: every descendant of the element was offered to `One`. The class' own remark said `v:shapetype` "is excluded deliberately" and described a filter that did not exist. Inside a `w:pict` that costs nothing — `One` wants a size and a property element states none — and inside a `w:object` it costs a duplicate of everything, through two independent fallbacks: `One` takes the *object's* `w:dxaOrig`/`w:dyaOrig` when the element states no box of its own, which is the same pair for every descendant, and `DocxPictures.ReadVml` searches `DescendantsAndSelf`, so the `v:imagedata` resolves *itself*. The fixture reproduces both rectangles exactly — `77.25 x 49.5` from the `v:shape`'s style and `77 x 49.85` from `dxaOrig`/`dyaOrig` over twenty — where 26.2.4.2 draws **one**, at `77.3 x 49.5`; after the fix one, at `77.25 x 49.5`. Reach on the words track, both legs pinned with `SOURCE_DATE_EPOCH`: **8 of 337 renderings move, 329 byte-identical, no page count moves**, and five of the eight lose characters with **every one of the five moving towards 26.2.4.2** — `A1. EASA Form 2` 11561 -> 11547 against 11527, `EHEST-SMS` 105466 -> 105317 against 105180, `UG.CAO.00006` 42116 -> 41545 against 40732 — so what went was drawn twice; on the witness the image placements go 16 -> 15 against the reference's 15. 3 tests, pinned by mutation. ***And the round REFUTED its own first reading of the other half.*** The predicate now names ten shape elements rather than the original five, and the claim that naming them is load-bearing — "150 top-level `v:line`, 72 of them sized" — is wrong: **all 150 state `from`/`to` attributes and none states a `style` width or height**, all 150 are `position:absolute`, so `Floating` returns null for each under either predicate, and rendering the 53 corpus documents that hold one of the five added elements with each gives **53 byte-identical PDFs**. The whole reach is in answering `false` for the property elements. *No gate verdict can move: no page count moves and the characters lost were drawn twice.* | `probes/vmldup-r140/results.md` |
| O96 | **An ODF number style's subformats were not assembled, so a `.ods` compiled one section and applied it to every value — FIXED in round 141.** LibreOffice writes a multi-section format as *separate* `number:*-style` elements linked by `style:map`, with the style the cell names carrying the **last** section: `N153` carries `(`, the number, `)` and a `<style:map style:condition="value()&gt;=0" style:apply-style-name="N153P0"/>`, the positive form living in the volatile `N153P0`. Nothing followed the links, and the colour — which ODF states as a `style:text-properties` child rather than as a keyword in the code — was skipped by the same `number:`-only loop, so round 139's fix could not reach the ODF path. `OdfNumberFormat.Code` now takes a resolver and assembles the sections the way `SvXMLNumFormatContext::CreateAndInsert`/`AddCondition` do (`xmloff/source/style/xmlnumfi.cxx`:1588-1602, :2130-2186), with all four of their rules: a condition must begin `value()` or it contributes **nothing at all**; a single `value()>=0` map writes **no** condition into the code; in a `number:text-style` — which is how an accounting format arrives, the owner holding `@` and its three maps holding the numbers — the **last** map is unconditional too; and `!=` becomes `<>`. `AddColor` (`:2196-2216`) turns the RGB back into a keyword, inserts it at the **front** of the code, and matches only `aNumFmtStdColors` — the same ten in the same order as `ImpSvNumberformatScan::StandardColor` — so a colour outside the ten is dropped by the reference as well and is dropped here. ***The instrument is the reference's own assembled code, not a rendered page***: `soffice --convert-to html` writes each cell's format as `sdnum="<lang>;<system>;<code>"`, and that third field is what `SvNumberFormatter` holds after the conditions are prepended — **five of the fixture's six agree with this tree character for character**, and the sixth (accounting) agrees in structure and differs only in `_` and `*`, which is O99. Reach, censused by what a style *paints*: of the 307 converted `.ods`, **307 state a `style:map`** (LibreOffice writes its built-in format list into every file, so that figure means nothing), 92 have a cell naming one, and **64 hold a cell whose value selects a mapped section** — 128 964 cells; 8 documents and 110 cells take one of the ten keyword colours. Sweeping the column twice under `SOURCE_DATE_EPOCH`: **21 renderings move, 286 byte-identical, no page count moves**, and **two gate verdicts are gained and none lost** (`044_Cash_flow_forecast` and `006_Contextures_chart_sample`, the second landing within 1 alphanumeric of the reference). Coloured-span distance 1158 → 1127 with five documents better and none worse. *Alphanumeric distance goes 1659 → 1880, and all three worsening documents are ones where this tree already draws chart content the reference does not — vector path counts identical before and after on all three, and `057_Simple_balance_sheet` is in this project's own raster-ceiling list, where 26.2.4.2 outlines that page's turned labels (361 paths against our 84) so they score zero in the text layer.* 25 tests, pinned by three mutations. | `probes/odfnumfmt-r141/results.md` |
| O99 | **The two padding directives an ODF number format states as something other than itself were both unread, and a third attribute in the same family with them — FIXED in round 142.** `_x` means *leave the width of `x` blank* and **ODF spells it nowhere**: the exporter writes the spaces into a `number:text` and records what they stood for in `loext:blank-width-char`, a sequence of `<char>[<position>]` groups separated by `_` whose positions are counted in the unquoted text. `*x` becomes a `number:fill-character` holding the character. `OdfNumberFormat.Append` handled neither — the fill element fell through to `default:` and the attribute was never looked at — so each blank became the literal space the element carries, an accounting format's currency symbol ran into its digits and its columns did not line up. Found while measuring: **`loext:max-blank-integer-digits`**, the count of integer digits written `?` rather than `0`, which is what makes a zero row `-??` and not `-00` (`xmlnumfi.cxx`:681-684, :801-802, :1828-1844). Three details decide the rewrite and all three are the reference's: `lcl_InsertBlankWidthChars` (`:879-926`) is **positional** and is followed exactly, down to leaving the index un-advanced when a position runs to the end of the spec; a literal carrying a spec is **always** quoted (`lcl_EnquoteIfNecessary`'s don't-quote branch is guarded on the spec being empty, `:539`, tdf#170670); and how many characters a blank replaces is the character's own width from `cCharWidths` (`svl/source/numbers/zformat.cxx`:71-105) — **`€` is above ASCII and is two**. ***The instrument states the answer***: `--convert-to html` writes each cell's format as `sdnum`, whose third field is what `SvNumberFormatter` holds, and after this round **all four of the fixture's and all four of a corpus witness' are reproduced character for character**, the euro accounting format — `[>0]_-* #,##0.00" "_€_-;…;_-* -??" "_€_-;_-@_-`, a two-space blank and a multi-group spec with positions — included. Reach: **122 549 cells in 55 of the 307 converted `.ods` state a `blank-width-char` and 102 680 in 29 a `fill-character`**. Sweeping the column against round 141's leg: **27 move, 280 byte-identical, no page count moves**, and the confinement control is exact — **every mover states one of the three attributes and no document stating none moved**. Position, measured about each document's own median because several sit a whole margin out for unrelated reasons: **16 better, 2 worse, 9 same, summed spread 120.84 → 108.79**, with five landing essentially exactly (`Global_Market_Forecast` 4.877 → **0.019**, `airports_6` 1.573 → **0.048**, `ecopy of 2016 statistical bulletin` 1.497 → **0.025**). Alphanumeric distance flat, 579 → 577. 4 tests, pinned by three mutations. | `probes/odfpad-r142/results.md` |
| O94 | **A character width stated by a paragraph style reached the layout and was then dropped three times over — FIXED in round 143, and the seat's own diagnosis was wrong.** The seat said `w:w` "is read from a run and not from a paragraph style's `w:rPr`". **It is resolved correctly through the style chain and always was**: a probe over the witness' own styles answers `WidthPerCent=60` for a paragraph naming a style that states it and stating no run properties at all. What loses it is that such a paragraph is **uniform** — every run carries its style's formatting, so the run splitter folds them away and it reaches the layout with no runs — and **three separate fallbacks then rebuild one run from the paragraph's own face, size, shaping and tracking, none of which carried a width**: `PageParagraph.Measure` (the measured run), `PageDrawing.RunsIn` (the drawn run), and — the one that actually decided the line breaks — the **shortcut in `Paginator` and `FlowLayouter`**, which hands the breaker the paragraph's *text*, a face, a size and its shaping and measures from those alone. Tracking survives that overload because `EffectiveShaping` carries it; a scale has nowhere to ride. `PageParagraph.WidthPerCent` and `IsHorizontallyScaled` are the fix. ***Fixing only the first two is worse than fixing none***, which is why the fixture has an arm that wraps: with the pen scaled and the breaker not, every line is drawn at exactly `scale ×` its unscaled width and broken in the unscaled places — a state that passes every one-line measurement, and which this round was in for an hour before the wrapping arm caught it. Witness `Regulations Governing the Status…docx`, title style `SL` at `w:w=96`: `Experts on Mission` **227.56 → 218.09 against 26.2.4.2's 218.06**, and the title's five lines now break on the same words — the reference pulls `Duties` onto line 2 and so does this tree. Whole document: mean |Δx| of 780 matched spans about their median **0.365 → 0.205 pt**, 18 pages both sides, alphanumerics unchanged. Reach: the whole 337-document words track rendered twice moves **1 rendering and leaves 336 byte-identical**. ***And the census had to be corrected against the sweep***: its first cut counted a paragraph uniform when its runs agreed *with each other* and predicted 4 documents and 1138 paragraphs, where `RunsOf` compares each run against the **paragraph mark** — so a run stating 99 under a mark resolving to 100 makes the paragraph vary, keeps the runs, and was always right. Corrected, the census predicts 1 document and 166 paragraphs, which is exactly what moved. 3 tests, pinned by three mutations. | `probes/charscale-r143/results.md` |

## Confounds — closed as measurement hazards, not defects

| # | what | note |
|---|---|---|
| C1-C5 | Five tarball font confounds | `CLAUDE.md`; move aside before measuring, never while a round is live |
| C6 | **`TODAY()`/`NOW()` recalculated on load.** Not reproducible: the delta changes daily. | `CLAUDE.md`; census before reading any glyph delta |
| C7 | Raster ceiling and outlining ceiling — the reference rasterises or outlines where we emit searchable text | `probes/odt-split-r82/overdraw.py` screens it |
| C8 | **The C++ tree is not the reference binary's source.** `configure.ac`:21 declares `27.2.0.0.alpha0+` and the whole checkout is one bulk import dated **2026-07-29**, while `/opt/libreoffice26.2/program/soffice` is **26.2.4.2**. So every citation in this session phrased as *"read out of 26.2's source"* was in fact read out of a 27.2 alpha, and no hunk can be assumed identical between them. Per-file dating cannot bound the risk — all 1230 commits carry the import date. **What this does not touch:** anything confirmed against the *binary* — `--convert-to fods/fodt/fodp`, a rendered PDF, a fixture scored against 26.2.4.2's own output. Those are behavioural measurements of the actual reference. The habit of confirming each arm twice, once in source and once against 26.2.4.2's own view, is what keeps the behavioural leg standing on its own. **Remedy, deferred:** fetch the upstream `libreoffice-26-2` branch and cite from it; there is no 26.2 tag or branch in this checkout, and a fetch was not attempted at 3.2 GB free with three rounds building. | `probes/clip-seats-r97`; found by that round, verified here |
| C9 | **A corroborating census with no base rate beside it is not evidence, and two of them in one round were both at chance.** r97 supported a reading with "16 of 19 pages carry a `style:shrink-to-fit` shape" and "18 of 19 admit a `constScaleLevels` row pair". Measured afterwards: **79.3 %** of the pages whose size *agrees* carry a shrink-to-fit shape, and **71 %** of arbitrary integer size pairs within 4 pt admit such a row pair. Both statistics were at their own null and neither was doing any work. The test is cheap and takes one script: **run the census over the population that does *not* show the defect before quoting it over the population that does.** | `probes/slides-r97` §6.2, `shrink-baserate.py`, `rowfit-null.py` |
| C10 | **`slides-r99/tfy.py` miscounts pages, on both legs, and every per-page pairing built on it is between pages that need not be the same page.** It finds pages by regex-scanning every `N 0 obj` and testing a **700-byte window** for `/Type /Page`; the window runs past `endobj` into the next object wherever objects are packed tightly, so a dictionary that is not a page is read as one and takes the *following* object's `/Contents`. Measured: **45 of the 51 banked 26.2.4.2 reference PDFs** are miscounted by 1 to 3 pages, and **13 of 13** renderings of ours by up to 18 — `Inducement-to-Insurance-Business` is 25 against 22 on the reference side and 28 against 22 on ours. It also ignores `cm`, which 26.2.4.2's own PDFs use to place an underline. Consequence: round 99 §2.2's census reports **42 pages in 5 documents**; the same filter on a correct enumeration finds **310 in 16**, and its 14× bullet-to-body ratio is 3.6× on the wider sample. The direction and the existence of the residual survive; the sample does not. Replaced by `slides-r100/tfz.py`, which enumerates through the real page tree and tracks the CTM. | `probes/slides-r100` §0, `tfy-page-count.txt` |
| C11 | **The reference is not reproducible run to run on a handful of documents, the handful is not a fixed roster, and the movement is not small.** Three full-corpus gates, same binary, same corpus, same timeout, all one UTC day. The *reference* leg should be identical between them. Pair 1 (r109→r111) and pair 2 (r111→r112) each found **4 of 947** differing — but **not the same four**, and the union is **5**. Persistent in both pairs: `SIL_TDB648`, which oscillates between exactly two glyph counts (30896 ↔ 30899), and `047_Date_tracker_Gantt_chart`, which does the same between 3367 and 3441 — round 116 saw it move **back** on a same-UTC-day pair, so its 27 `TODAY()` calls are not what makes it move and two of the five are oscillators rather than drifters, settled by one re-render, and `PBN Matrix NAAs (V01)` and `ans_mappings_of_eccairs_terms`, whose text layer differs with **no** change to the character count. In one pair only: `alle einzeln` (+2 — it wrote `Janßen,` in one run and `Janssen,` in the other, twice, a font resolving differently, the same class as C1) and `047_Date_tracker_Gantt_chart` (**+74**, the PDF itself 105167 against 105422 bytes, with `pdftotext` emitting `addadd`/`thisthis` in one run and clean text in the other). **A verdict can be at risk.** `047`'s 74 exceeds its own gate band — max(2 % of the reference's 3367…3441, 15) is 67.3…68.8 — so on that document the reference's run-to-run variation alone is wider than the band the verdict is decided in. Nothing actually moved, because it already fails on pages (5/8); that is luck, not a property of the gate. It carries 27 `TODAY()` but both runs were the same UTC day, so the sixth confound does not explain it. **What this still does not support is a corpus-wide noise floor**: at least 942 of 947 reproduce their character count exactly in any given pair, so a one-character delta on an ordinary document is real and discarding small deltas everywhere would throw away findings. What it does mean is that **a per-document delta is only trustworthy once the reference has been rendered twice for that document** — the roster of five is a lower bound from three runs, not a checklist. **And the character count is itself a lower bound on the instability, because some of it is length-preserving.** Round 122's gate moved one comparable row, `alle einzeln` again, +2 in the reference only; re-rendered twice on the same UTC day it gives 278868 and 278866, the gate's own two values, so the row is this confound and not ours. But diffing the two extractions token by token shows **two** oscillations, not one: `Mess,`/`Meß,` **twice** (the +2 the gate sees) and `HYP`/`Hyp` **eight times** — a *case* swap of the same length, which no character-count comparison can detect. So a document can be character-count-stable and still not reproducible: where a round needs the reference stable in what it *says*, **diff the extracted text, not its length**. *Counting caveat:* `tr -cd '[:alnum:]'` under the C locale drops both UTF-8 bytes of `ß` and reports that same pair as 4; the 2 is right and the 4 is the locale. **Corrected at gate r129: `alle einzeln` is NOT a two-state oscillator and neither r111 nor r122 had bounded it.** Six fresh renders on one UTC day give **three** totals — 278866, 278868 and **278870** — because there are **two independent ß/ss sites**, not one: `Janßen`/`Janssen` (which this row already named) and `Meß`/`Mess` (which r122 found and silently treated as the same site). Each carries two occurrences and is worth 2 characters, so both-ß, one-each and both-ss are the three values. Two runs agreeing on `Meß`, `Mess`, `HYP` and `Hyp` still differed by 2, which is what exposed the second site. **The generalisation, and it is the part that matters: a document's reference instability is a PRODUCT OF INDEPENDENT BINARY SITES, so the reachable values go as 2^n rather than 2. "I rendered it twice and saw both states" bounds nothing — two agreeing renders prove only that you sampled the same corner twice.** Whether `047_Date_tracker`, called a two-state oscillator above, is likewise more than two-state was not re-measured and is not claimed. `probes/gate-r129/results.md`, with three of the six extractions banked. | `probes/gate-r111/ref-reproducibility.tsv` (pair 1), `ref-reproducibility-pair2.tsv` (pair 2), `probes/gate-r111/results.md` |
| C12 | **A recalculated volatile cell reaches the *chart's axis scale* as a UNION with the import-time cache, not as a replacement — so a date axis can span years the data never covers.** `055_Project_timeline` states thirteen `DATE(YEAR(TODAY()),m,d)`; 26.2.4.2 draws its axis from `[min(cached, recalculated), max(cached, recalculated)]` = `[2023-04-05, 2026-08-31]`, 38 labels at 30-day steps, where the data it actually plots spans 148 days. **Proved by changing only a cached `<v>`** and leaving the formula alone: C20's 45021 → 40000 moves the reference's axis minimum to 6 Jul 2009 and its interval to 5 months, both of which `ChartDateScale`'s existing rules then reproduce exactly from that union range. So a chart on a volatile sheet is not merely *dated wrongly* — its **scale** is unreproducible without keeping the stale cache alive beside the recalculation. | `probes/chart-axis-r112` §1.3, `freeze-055.py`. This is C6's chart half and is why `055`'s column-9 gap is not a scaling defect. |
| C13 | **A gate compared across a UTC date boundary is not comparable on the volatile-date rows, and it can flip a verdict.** The sixth confound (`TODAY()`/`NOW()` recalculated on load) was recorded as moving counts; round 114 caught it **changing a gate verdict**. `066_Agile_Gantt_chart.xlsx` went `match` → `words` between two gates while **our own count was identical at 3021** — the reference moved 3062 → 3086, which alone crossed its band of max(2 % × 3086, 15) = 61.7. The gates ran at 2026-09-12 21:26 and 2026-09-13 02:54, and that workbook holds 21 `TODAY()`. Base rate **32 of 243** zip spreadsheets hold a volatile call, a floor because `.xls` is not scanned that way. **Two rules follow**: run both legs of any comparison on the same UTC day, or re-render the reference for any row that moved before reading anything into it; and **a verdict change is not a regression until our own column is shown to have moved** — the headline 916 → 915 reads as a regression to anyone looking only at the totals. **The confound is SYMMETRIC, and gate r133 caught the other half on the same document**: `066_Agile_Gantt_chart` flipped back `words` → `match`, our column at 3021 for the fifth gate running, the reference moving 3083 → 3080, and the headline went **915 → 916**. *The flattering direction is the more dangerous one* — a regression gets investigated and an improvement gets reported, so read off its totals that gate would have credited three rounds with a match they did not earn, and durably: the next gate to read 915 would then look like a regression from a number that was never real. State the rule in both directions — **a verdict change is not attributable, either way, until our own column is shown to have moved.** | `probes/gate-r114/results.md`, `volatile-xlsx.txt` |
| C14 | **"Mean stroke width" scores a hairline as zero, so the ink census reads *which items are hairlines* rather than how heavy the strokes are.** LibreOffice's PDF export writes a hairline as `0 w` and draws it at `72/DPIX` = a tenth of a point (`PDFPage::appendLineInfo`, `vcl/source/pdf/PDFPage.cxx`:497-505) — real ink the statistic counts as none. On round 115's seventeen residual sheets documents, dropping every zero-width item moves **ten of them inside 6 % of 1.000**, including all three that read *heavier*: the light ones were where we made hairlines of rules the reference strokes and the heavy ones where the reference made hairlines we did not draw at all. Neither is a weight difference. | `probes/stroke-resid-r117/results.md` §6; `census2.py` adds `hair_ours`, `hair_ref` and `ratio_ex_hair` and any residual should be read off the last of those. This is the *other half* of the fill-versus-stroke trap that excluded 584 of 947 documents from the original census — that guard catches a rule drawn as a fill and does not catch a rule drawn as a hairline. |
| C15 | **`--convert-to fods` cannot see a hyperlink cell's colour or its underline, and prints the opposite of what the reference draws.** `ScXMLExport` resolves a field with `ScEditUtil::GetCellFieldValue(*pField, &rDoc, nullptr, nullptr)` (`sc/source/filter/xml/xmlexprt.cxx`:3062) — **both out-parameters null** — so neither the `LINKS` colour nor `LINESTYLE_SINGLE` reaches the flat file. Measured on `084_Service_invoice_Use_this_template`: the fods gives the `jordan@example.com` cell `style:text-underline-style="none"` and `fo:color="#000000"`, and 26.2.4.2's own PDF of the same file paints that text `#000080` and strokes a `#000080` rule 0.51 pt thick under it. **The resolved view is the project's default second leg and it is silently wrong for exactly these two attributes.** Use the file's own hyperlink markup and the reference's ink instead. *And a second, milder one on the same instrument: a flat ODF collapses a run of identical cells with `table:number-columns-repeated`, so any per-cell count taken from it is a floor* — `hdss-bulletin-index-2019-2022.xlsx` has 350 underlined cells by its own markup and 25 in the fods. | `probes/underline-r118/results.md` §1.3 |
| C16 | **This tree FILLS every text rule and 26.2.4.2 STROKES every one, so a stroke census scores all of ours as absent.** `SheetTextLayout.Rule` emits `FillPath`; `PDFWriterImpl::drawStraightTextLine` (`vcl/source/pdf/pdfwriter_impl.cxx`:6658-6760) emits a stroke centred on the metric's own band. The ink is the same to within O64's thickness — measured on `TK-Syllabus-Comparison-Document-v2`, 7449 red fills against 7625 red strokes at the same x and within **0.02 pt** of the same top edge. **That 0.02 pt is a STRIKETHROUGH on one document and it was wrong to read it as a statement about rule position in general**, which round 120 did when it declined to quantise the offsets: over round 123's 126-rule probe a strikethrough is 0.030 pt out and a single underline 0.067 pt, worst 0.139 — and a double underline's second line was out by a whole thickness. All three are now exact; see O64's correction. That document was nevertheless filed against O62 as *missing* several hundred rules. **This is C14 from the other side**: C14 says a mean stroke width scores a hairline as nothing; this says it scores a filled rule as nothing too. *And the matching trap on the count*: a hyperlink cell is one field and one stroke at the reference and as many fills as it has rich segments here, so `fm-provider-service-measures.xlsx` reads 332 rules against 185 while the covered lengths are 38 053.6 pt against 38 036.3. **Rank on length, never on object count.** | `probes/underline-r118/results.md` §3.3, §5.1; `rule-check.py` measures both shapes and both statistics |

---

## Open — being worked now

| # | what | round |
|---|---|---|

## Open — seated, not yet dispatched

| # | what | seat |
|---|---|---|
| O78 | **`FAA 2025-26 Holdover Tables.docx` prints ten pages in the wrong paper orientation on a 1.5 pt margin, and it is ours.** The displacement begins at the empty half of the `<w:br w:type="page"/>` paragraph at body index 1226: our last table chunk on page 82 ends at 546.7 pt against the reference's 545.2 on a text area ending at ~573.3, so we have 26.6 pt of room where the reference has 28.1, the empty half does not fit, and our page 83 comes out blank. **Not** the `TOC \t` defect L4 -- removing the template from this document changes the reference not at all. **Round 131 attributed the 1.5 pt and it is two faults that nearly cancel**: pairing the 34 grid lines of page 82 with the reference's, **eleven rows are short by 5.25 pt in total** -- each one a row all of whose cells state `w:bottom w:val="nil"` under a row stating a 1 pt top, so it charges half a border the reference charges whole, which is **O84** -- and **two rows are tall by 6.90 pt**, 6.60 of that one cell holding `Not Available19` that we break onto two lines and the reference sets on one. Both sides measure that string identically at 52.2 pt in an inner width of 52.3 (ours) against 52.2 (the reference's), so the fit turns on **a tenth of a point in fifty-two** and is not the padding, the column width or the metric. **Fixing O84 alone makes this worse**, which is why it is not shipped. | `probes/wordsgroup-r128` §2, `faa-orientation-runs.tsv`; `probes/wordstable-r131/results.md` §4, `faa-p82-rows.tsv`. Reach 1 of 329 on this knife-edge; neither term is censused as a class. |
| O83 | **A table row split across a page has both its parts placed half a border too high, so the grid line at the split is drawn twice, 0.25 pt apart.** On `review-welsh-…-mandelson.docx` page 10 the row above ends at 721.850 and the row that continues onto page 11 starts at **721.600**; page 9 is the same the other way, 120.900 against 121.150. The cut itself carries it too -- our outer line at the foot of page 10 is at 768.50 where the reference's is at 768.75, and at the top of page 11 ours is at 72.00 against 72.25. `TableLayouter` already insets the *first* row of a table by half a border and does not do it at a page cut, and round 131's `PBNN` arm shows 26.2.4.2 drawing a full-width line at a cut whatever the cells state. | Visible as an extra whole rule: after O82 closed, `review-welsh-…`'s merged rule cover goes from 1 207 pt short of the reference to **472 pt long** while its bridges go to zero. Not censused. `probes/wordstable-r131/results.md` §5 |
| O84 | **A row's height charges the border its cells STATE and the reference charges the RESOLVED one, so a row whose cells all nil an edge is half a border short.** O82's own authored probe says so with no free parameter -- its `nil`/`nil` arm is a whole point shorter than the other three, and its two asymmetric arms are exactly as tall as the arm that states both. Implementing it as measured **over-charges a `w:trHeight` row**: on `review-welsh-…-mandelson.docx`'s first table, twenty rows go from 1.45 pt short of 26.2.4.2 to 6.05 pt long, right on rows 0, 4, 11 and 19 and wrong on ten others, and no linear function of the two facing widths fits the charges 26.2.4.2 adds to a stated height. | The cost of leaving it is **5.25 pt of O78's own 1.5** -- eleven rows of FAA page 82 -- plus whatever the same shape is worth elsewhere, which is not censused. `probes/wordstable-r131/results.md` §4 and §5 |
| O85 | **A row whose cells state borders of different widths has its bands aligned at their TOP edge by the reference and centred on the grid line by this tree.** `150_5335_5a.doc` page 39: the row 8/9 boundary carries a 1.5 pt double on two cells and a 0.5 pt single on six. 26.2.4.2 draws a continuous 0.5 pt line across the whole table at **278.75** and a second 0.5 pt line at 279.75 under the two double cells alone -- the double's outer rule and the single share one y, so the bands share a top edge at 278.50. This tree centres each band on 279.375, putting the double at 278.875/279.875 and the single at 279.375, and the census then reads the double's outer rule as a rule with a 266 pt hole in it. `PageDrawing.DrawBorders`' `Rule((bands.Outer / 2) - half, …)`. | **626 pt in 4 bridges of 1 document**, 26 % of O82's residue, and probably also the 14 pt on `TE.CAO.00125 … OJT Logbook.docx` that O82's fix does not touch. Not censused; `sharededge-census.py`'s 18 134 disagreeing edges bound the class from above. `probes/wordstable-r131/results.md` §5 |
| O91 | **A `<w:br/>` line in a shape's text body takes the PARAGRAPH MARK's size, not the size of the run carrying the break -- and when the body then exceeds the shape this tree draws none of it.** `086_Printable_Graph_Paper_Template_Gray_Theme.docx` puts four runs holding nothing but `<w:br/>` at `w:sz="4"` -- two points -- ahead of the one line of a 32.65 pt `wps:wsp` whose `bodyPr` is `anchor="ctr"`, `tIns`=`bIns`=3.6 pt. 26.2.4.2 spends **10.8 pt** on all four, 2.7 pt each, which is 2 pt at the paragraph's 1.35 spacing; this tree spends **14.8 pt each**, which is 11 pt at the same spacing and 11 pt is what the paragraph mark states. Measured by keeping k of the four and nothing else: k=0 puts our line's ink top at 37.1, k=1 at 44.5 (so half a 14.8 pt line), k>=2 draws no text at all. **Both sides draw the box itself** -- `fs [49.0, 27.1, 319.9, 59.8]`, same fill, same 1 pt grey stroke, to a tenth of a point -- so only the text is lost, and lost entirely rather than clipped. **The drop is the height and nothing else**: with all four breaks kept and the shape's `cy` raised from 414655 to 1414655 EMU, one attribute, the text is drawn. The shape's own `wps:bodyPr` says `vertOverflow="overflow"`. Two rules: **(a)** the break line's height, **(b)** draw an overflowing shape body rather than discarding it. | Population where the two readings differ: **7 of 272 `docx`, 278 breaks**, against 13 documents and 353 breaks holding such a break at all -- on the other 6 documents and 75 breaks the break run states no smaller size and the readings necessarily agree. **Text actually lost is 2 documents and 86 characters**: `086` draws 10 against 95, the largest relative character disagreement in the corpus in either direction, and `084_Printable_Graph_Paper_Template_Editable_Layout` 10 against 21. The five `Work_Breakdown_Structure` templates carry 263 of the 278 disagreeing breaks and lose nothing. **(b)'s reach outside this family is not censused** -- a static census cannot say whether a body overflows -- and that is what this row leaves open. `probes/coverage-r136/results.md` §6, `txbx-br.tsv`, `br-threshold.txt`. |
| O93 | **A `cfRule type="expression"` whose formula is anything but `<operand> <op> <operand>` is evaluated as nothing, and a Gantt chart drawn entirely out of conditional formats then comes out blank.** `XlsxConditionalStyles.ConditionOf` sends an `expression` rule to `Comparison.Parse`, which scans for one of six operators and requires both sides to be a cell reference or a literal (`SheetConditions.cs`:207-230); a bare defined name, a function call, or a comparison whose right-hand side is a defined name all return null. `072_Gantt_project_planner` states ten such rules over `H5:BO30` -- `Plan`, `Actual`, `ActualBeyond`, `PercentComplete`, `PercentCompleteBeyond`, `H$4=period_selected`, `MOD(COLUMN(),2)` and `MOD(COLUMN(),2)=0` -- and holds no drawing part at all, so its entire chart is conditional formatting. Found blind: a reader given that page reported *"the entire chart region is blank white in ours: all 60 period columns x 26 rows"*, and supplied the discriminator -- the amber `Period Highlight` input box IS filled in ours, so it is not a blanket fill failure. Measured: **7 filled paths against the reference's 1 453**, 30 path constructions against 1 532, with identical character counts on both sides. | **23 of 241 `xlsx` hold at least one expression rule this parser refuses, 118 rules covering 60 341 cells**, against **483 rules it accepts** in 34 workbooks -- so the refusal rate is 118 of 601. The drawn consequence is a large fill deficit on **2** of the 23, both never examined by any write-up: `072_Gantt_project_planner` (7 fills against 1 453) and `VOR Candidate Discontinuance List 2026-01-08` (9 against 152, its refused rule being `MOD(ROW(),2)=0` over 1 419 cells). On several of the other 21 we draw *more* fills than the reference, which C16 covers and which this round did not chase. **This is the largest of the round's seats and the only one that is not small**: honouring it needs a formula evaluator with defined names, `COLUMN()`/`ROW()`/`MOD` and relative-reference shifting. `probes/coverage-r136/results.md` §10.2, `cfexpr.tsv`. |
| O97 | **A top-level `v:line` states its extent in `from`/`to` and nothing reads them, so this tree draws none of the corpus's 150.** Round 140's census established it as a by-product: all 150 top-level `v:line` in a `w:pict` are `position:absolute` and state no `style` width or height, and `DocxVmlFrames.Floating` requires both — so every one returns null and no rule is drawn. 36 documents. `Flatten` has the group-space arithmetic for a member's `left`/`top`/`width`/`height` and would need the same treatment from `from`/`to`; `IsStraightConnector` and `VmlPaint.IsLine` already know how to paint a box's diagonal, so the missing half is the box. **Unmeasured against the reference** — nobody has yet rendered one of the 36 through 26.2.4.2 to see how much ink it is. | `probes/vmldup-r140/census-vml.tsv`, `vline-docs.txt` |
| O100 | **One column of `084_Service_invoice` is drawn 213 pt left of where 26.2.4.2 draws it, and round 142 is what made that legible.** Banding every span's distance from the reference rather than averaging it (`probes/odfpad-r142/invoice-bands.py`), that document went from **eight** scattered offsets — `-213×7, -143×4, -136×1, -124×12, -38×31, 0×12, +32×2, +39×1` — to **three**: `-213×25, -38×34, 0×12`. The middle band is a whole-page constant of 37.7 pt this document has carried throughout and which no round has touched; the zero band is exact; and the `-213` band is one column in the wrong place. So the formatting is now right and what is left is a single geometry error, which is why the document's *mean* distance rises (49.19 → 67.99) while its state improves — the misplaced group grew from 7 spans to 25 because the correctly formatted cells joined it. **Not diagnosed**: nobody has yet established whether the column is a merge, a width or an anchor, and the whole-page 37.7 pt is a second, separate question on the same document. | `probes/odfpad-r142/results.md` §4 |
| O101 | **Three of the four word-processing readers do not read a character width at all.** `git grep WidthPerCent -- src/Paperless.WordProcessing` finds it in the DOCX path and nowhere else: the WW8 reader does not read `sprmCCharScale`, the RTF reader does not read `\charscalex`, and the ODF one does not read `style:text-scale`. `TextWidthScale`'s own remarks name the RTF control word, so the gap is recorded in the tree and was never closed. Round 143 made the DOCX path correct for a uniform paragraph, which is the half that decides line breaking, so the three remaining readers now differ from it in kind rather than in degree. **Uncensused** — the corpus holds 66 `.doc` and no `.rtf` or `.odt` of its own, so sizing this needs the converted columns, and the `.doc` half can be censused directly from the sprm. | `probes/charscale-r143/results.md` §5 |

## Open — measured as nil reach, kept only so they are not rediscovered

| # | what | reach |
|---|---|---|
| O95 | **`Read_BoldBiDiUsw`'s two toggles follow O86's rule through `m_n81BiDiFlags` and are not read at all -- NIL REACH.** `sprmCFBoldBi` (0x0858) and `sprmCFItalicBi` (0x085A) reach `SwWW8ImplReader::Read_BoldBiDiUsw` (`sw/source/filter/ww8/ww8par6.cxx`:3254-3320), which asks the same `sprmCIstd` and inverts against `m_vColl[pSI->m_nBase].m_n81BiDiFlags` exactly as `Read_BoldUsw` does against `m_n81Flags`. Neither sprm appears in `ApplyLayoutSprms`, so the defect is an unread attribute rather than a mis-resolved one, and O86's fix does not reach it. Seated only so that a later round meeting the pair does not re-derive the rule. | **0 of 66 corpus `.doc` state either sprm, with or without a character style beside it, over every story** -- body, footnotes, headers, annotations, endnotes and both text-box stories. Base rate: the western nine toggles the same walk counts in the same files, **12 804 CHPX in 65 of the 66**. So the class is well populated and the BiDi pair is absent from it. `probes/ww8char-r135/bidi-census.tsv`, `results.md` SS6.5. |
| ~~O75~~ | **REFUTED by round 128, and kept so it is not re-derived.** The reading of the tables is right — `U+0001` is UAX #14 `CM`, LB9 forbids a break before it, and `LineBreaker` really does offer no break opportunity between two adjacent anchors — but it does not govern the layout. Two probes, an inline pair of 4.5 in and a pair at the witness's own 6.5 in on a 6.5 in measure, are split across two lines by this tree and by 26.2.4.2 at the same rectangles. A run of nothing but anchors goes through `MeasuredParagraph`'s own path instead. A change resolving the anchor to `CB` was built and measured and moved nothing; it was reverted. `InlineObjectPairTests` pins the split. | `probes/wordsgroup-r128` §3. *A duplicate of this seat survived in **Open — seated** until the round-129 audit, carried through a merge that kept both sides; it stated the refuted mechanism as fact and has been removed.* |
| O76 | **A `.ppt` connector IS drawn from its preset -- the `SdrEdgeObj` never routes, and the seat as round 127 wrote it would have built a router that cannot be called. NOT WORK on reach.** `SvxMSDffManager::ImportShape` does replace the custom shape with an `SdrEdgeObj` (`filter/source/msfilter/msdffimp.cxx`:4792-4888), but it ends with `SetEdgeTrackPath(GetLineGeometry(true))`, and that sets `m_bEdgeTrackUserDefined` (`svx/source/svdraw/svdoedge.cxx`:1770-1772) while `ImpRecalcEdgeTrack` returns early for one -- *`#i120437# if bEdgeTrackUserDefined, do not recalculate`*, :574-580. The `SdrEdgeKindItem` and the four 630-unit node distances are metadata for a recalculation that never happens. **Confirmed twice**: source above; and 26.2.4.2's own `--convert-to fodp` of the three decks (twice each into separate profiles, byte-identical, C11) writes every connector as a `<draw:connector>` with an explicit `svg:d` whose first segment is **0.6657**, 0.5069 and 0.5063 of the span on three of the eight bent ones -- a router bends at the midpoint, so this is the preset at its stored adjustment. So round 127's 6.63 -> 8.73 pt does NOT say the preset is not drawn; it says a wrong conversion was applied to shapes that mostly sit at the default. **The remaining question is the conversion and the clamp, not the routing** -- and it is not worked. | **3 of 947 documents.** Census of every connector 26.2.4.2 exports from the three decks: **349 connectors, of which 193 are straight lines with no adjustment at all** and 36 more have no adjustable bend -- that is the base rate. The reachable population is the non-default bends: **6 of the 12 three-segment bent connectors** (the other 6 sit at the default) plus about **21 of the 86 curved** in `introduction_to_bea_tuxedo`, whose control fractions cluster at 0.745-0.757 on 30 and 0.666-0.667 on 21, with 34 unclassifiable by that instrument. And **327 of the 349 are in the two decks whose polylines round 127 measured as not pairing with the reference on their END points**, so on 94% of the population the adjustment is not the first defect. `probes/rulescale-r132/results.md` SS4, `probes/pptgeom-r127/results.md` SS3.1. |
| O77 | **`mso_sptFlowChartOfflineStorage` (type 129) has no entry in `GetCustomShapeContent` and 26.2.4.2 draws nothing for it, where this tree draws the DrawingML `flowChartOfflineStorage` -- NOT WORK.** The `switch` in `EnhancedCustomShapeGeometry.cxx` jumps from `mso_sptFlowChartMerge` to `mso_sptFlowChartOnlineStorage`; round 127's census asked 26.2.4.2 for all 148 name-mapped types and got 147 paths and one nothing. | **0 of the 49 passing `.ppt` state type 129**, and none of the 12 documents that state an adjustment at all names it. Drawing a shape the reference omits is the safer direction of the two. `probes/pptgeom-r127/results.md` SS1.1. |
| O69 | **A double underline on a SLIDE — the reference cannot carry one from a `.ppt` and no `.odp` or `.pptx` in the corpus states one — NOT WORK.** The slides half of O68, separated from it by round 123's census and left unimplemented on purpose. Two independent legs. **Structural:** `filter/source/msfilter/svdfppt.cxx`:5575-5576 imports `PPT_CharAttr_Underline` as `nVal != 0 ? LINESTYLE_SINGLE : LINESTYLE_NONE` — one bit — and `LINESTYLE_DOUBLE` appears **nowhere** in the `.ppt` reader, so 26.2.4.2 itself cannot draw a double underline from a `.ppt` whatever the file says. **Empirical:** `a:u="dbl"|"wavyDbl"` occurs **0 times in 251 `.pptx`**, and `style:text-underline-type="double"` **0 times in the 302 `.odp`** of 26.2.4.2's own conversion of the whole corpus — which is the instrument that sees the 51 `.ppt` as well. Cost of declining: the 90 Impress rows of round 120's authored sweep stay out, at a mean 0.3814 pt and a worst 1.5021, and no corpus rendering moves. `SlideTextLayout`'s `(bool Underline, bool Strikethrough)` keeps its shape; its single underline and strikeout **offsets** were fixed with everyone else's. | **0 of 302** slides documents state one, in either spelling, and 51 of them cannot. Base rate: **63 of 251 `.pptx` state 1110 underlines** and 136 of 302 `.odp` state 560, so the class is well populated and the double is absent from it. `probes/dblunder-r123` §1.2, §1.4; `census-corpus.tsv`, `census-odf.tsv` |
| O66 | **`TICAPCapability_Final.xls`'s nine cached metafiles DO reach the reader, the paired-`SRCAND` idiom fires on all nine, and what is left is an ActiveX control layer BIFF does not have here — NOT WORK.** Round 94 left the path question open: *"do not reach our WMF reader at all … whether those metafiles are read on a path this fix cannot see is not established."* Answered three ways. (i) They are **EMF**, not WMF — all 11 `msofbtBSE` entries in the workbook stream's blip store are `msofbtBlipEMF` (`0xF01A`), so `EmfReader` owns them and *"does not reach our WMF reader"* is true and empty. (ii) There are **nine** pairs, not eight: r94's byte-scanning walker found 9 metafiles where the store holds 11 and classified 8; the structured walk finds 9 `EMR_BITBLT` `SRCAND` + `EMR_STRETCHDIBITS` `SRCPAINT` pairs over one 13 × 13 destination each. (iii) Our render carries **9 image XObjects each with its own `/SMask`**, three on each of pages 4, 10 and 15, and decoding the two DIBs out of the blip and merging them outside Paperless reproduces the colour plane and the alpha plane **0 mismatches of 169 pixels** on each of the three page-4 icons, 112 of 169 opaque. An unmerged blit is opaque with no mask, so the `/SMask` is the merge. **The residue is not a metafile question**: the nine `OBJ` records state `EXC_OBJ_PIC_CONTROL \| EXC_OBJ_PIC_CTLSSTREAM` and their Escher shapes are type 201 `hostControl` with `fOleShape` and `pictureId`, i.e. ActiveX Forms 2.0 check boxes persisted in `_VBA_PROJECT_CUR/Ctls`. `IsOcxControl()` is `mbEmbedded && mbControl && mbUseCtlsStrm` (`sc/source/filter/inc/xiescher.hxx`:824) and `XclImpPictureObj::DoCreateSdrObj` (`xiescher.cxx`:3038-3068) builds a *control* and only falls back to the cached graphic when that fails, so 26.2.4.2 never draws the blip: it draws `#000000` 3.00 × 3.00 pt with `#FFFFFF` 2.60 × 2.60 pt inset 0.20 inside, and **zero rasters in all 17 pages**. We draw Excel's own cached picture of the same empty check box at 3.60 × 3.40 pt, 0.34 pt away. **Cost of declining**: ~110 pt² of our ink against ~81 pt² of theirs over nine controls, the label at an effective 3.82 × 3.40 pt against 4.00, on 3 of 17 pages of 1 of 947 documents that the gate scores `match` (17/17, 27 834/27 777 glyphs). Modelling it means a BIFF ActiveX layer — the `Ctls` stream and the Forms 2.0 models — for that. | Metafiles: **123 of 947** documents hold one, 453 metafiles, 4149 blits; **8 documents hold a paired `SRCAND`** (200 pairs) and 3 a lone one (6). TICAP is 1 of the 3 `.xls` with any metafile and the only `.xls` with a pair. `probes/o2-audit-r121` §2, `metafile-base-rate.txt`, `ticap-blip-reach.txt` |
| O67 | **The "table transform" is a `p:oleObj`, not a table; the mechanism is alive and its cost is 0.053 pt — NOT WORK.** Round 94 §8 left *"we lay a `p:graphicFrame` table out nominally and stretch it by 1.3151 × 1.3134 … could cost more where the factor is further from unity"*, and N7 refuted only the *ink* claim on the same document, which is a different claim. **Two corrections.** The frame on `NAS-…-Weather.pptx` slide 11 has `graphicData` uri `…/presentationml/2006/ole` holding `<p:oleObj progId="Excel.Sheet.12" imgW="11277718" imgH="3192990">`, so a round hunting the table layout would look in the wrong place **for that frame**. *Corrected at merge:* this round also wrote *"there is no `a:tbl` in the file at all"*, which is **false** and was why it never looked further — slides 8, 9 and 10 each hold one, five `<a:tbl>` across all parts. **Measured at merge, and the verdict survives on a reason this round did not give**: `pdf-ops.py diff` over all four frames classifies every difference on the three real table pages as `shows` (35/33/30) and `glyphs` (14/15/15) with **0 position and 0 size divergences**, so the table frames take no nominal-layout-plus-stretch either; page 11's 87 size differences are the 9.00 vs 9.01 below, and that 87 corroborates the paired-record count independently. And the cost does **not** grow with distance from unity: at HEAD we still emit `1.3151 0 0 1.3134 36 -240.726 cm` and draw the 87 runs at 8.9973 where the reference emits **no `cm` at all** and draws them at 9.01, and the worst of the 87 origins is **0.053 pt** apart (mean 0.030); on `RPA P4 - Advanced Material.pptx` page 3, whose factor is **0.379** — three and a half times further from unity — the worst of 64 is **0.030 pt** (mean 0.022) and the anisotropy falls from 0.129 % to 0.026 %. **Refuted here:** r94's "0.13 % of anisotropy" is right for NAS and is not a property of the path; the residual size difference is −0.14 % on NAS and **+0.32 %** on RPA, opposite signs, so it is layout rounding rather than the transform. | **45 `p:oleObj` in 10 of 251 zip presentations**, out of 721 `p:graphicFrame` on corpus slides and 302 presentations of 947 documents — 36 `Excel.Sheet.*`, 3 `Visio.Drawing.11`, 3 `Photohse.Document`, 3 singletons. The factor is not readable from the file (`imgW` equals the frame to 118 EMU on NAS while the emitted factor is 1.3151), so only the two rendered are measured. `probes/o2-audit-r121` §3, `oleobj-census.tsv`, `ole-transform.txt` |
| O60 | **A `.ppt` table cell's inner measure — CLOSED as established with the measurement; nothing to implement.** Round 115's ~3.3 pt was withdrawn by round 117 as a justified trailing space (3.890 pt at 14 pt Liberation Sans); page 10 was never evidence either way, its bracket being 14.8 pt wide. What was left is **one break on page 14 of `architecture6.ppt`**, brackets `[394.648, 400.829)` for the reference and `[400.850, 407.837)` here — disjoint, adjacent, the gap in **[0.021, 13.19) pt**. Round 118 re-took the load-bearing number independently, with an XML parser rather than a regex: 26.2.4.2's own `--convert-to fodp` gives page 14's table columns of **5.451 cm and 14.525 cm** at `svg:x="2.844cm"` with `fo:padding-left/right="0.191cm"` and `fo:border-left="0.23pt solid …"`, so the declared inner measure is **400.90 pt** (r117 wrote 400.88; the 0.03 is its arithmetic). **The direction is now named**: 400.90 is inside *our* bracket and 0.077 pt outside the reference's, while 400.90 − 0.46 (the two cell borders) is inside the reference's and outside ours — so 26.2.4.2 behaves as though a cell border comes off the text area, and `Cell::TakeTextAnchorRect` (`svx/source/table/cell.cxx`:642-649) in the 27.2 tree subtracts only the four text distances. **And the discriminator is below the instrument's floor**: 0.077 pt against an instrument whose own agreement on undisputed lines of the same deck is 0.06–0.44 pt. A change fitted to it would be a constant fitted to one line. | `probes/underline-r118/results.md` §8; `probes/chart-slide-r117` §3 for the de-confounding, which reproduces. **Class bound**: over the 251 OOXML presentations of the slides track, 484 tables, **30** documents with a cell side border, **8** with justified table text, **5 with both** — and being in the class is necessary, not sufficient, since the gap only shows where it flips a break. The `.ppt` half is not scanned, so 5 is a floor and the witness itself is not in it. **Closed, nothing implemented.** |
| O43 | **The two-word wrap restart has no witness in the corpus, and the seat closes on that census rather than on a mechanism.** Round 111 characterised the restart over 20 sweeps and declined to implement it — it moves with the *width* of the label's other words, not their count or order, scales with the font, and **moves with the number of categories**, which no property of a label and a tick pitch can do — and round 112 then found that none of the three documents the row was opened for was a two-word wrap case at all. Round 117 asked whether *any* corpus document is. **It is not.** The restart is gated in the reference's own source on `nLimitedSpaceForText > 0 && !m_bOverlapAllowed && rotation == 0 && nTick > 0 && lcl_hasWordBreak` (`VCartesianAxis.cxx`:888-903), and this tree restarts on a superset-narrower condition, so the divergence is one-directional: the reference turns an axis this tree wraps. Nothing about r111's mechanism is re-derived or withdrawn; what is settled is that it cannot reach a rendering here. **Closed as nil reach.** | `probes/chart-slide-r117` §1. Census over the **176 chart-bearing documents**, 176 of 176 rendered, with a temporary trace in `ChartAxisLabels.Resolve` (`axis-trace.diff`, reverted): **193 axes resolve in 96 documents; 15 are horizontal with `TextBreak` on, `TextOverlap` off and no stated rotation and carry a multi-word label past tick 0; 7 of those wrap; 6 of the 7 already restart here and are drawn turned 45°, which is what the reference draws.** The one axis in the corpus whose final arrangement keeps a wrapped label with overlap off is `8_P-Pavese_AIRBUS-ATB-journee-CRATB` at a 27.48 pt pitch, **41.3 % of a pitch clear** of r111's own restart form — and 26.2.4.2 wraps it, two upright lines per label at a 27.5 pt pitch, rendered twice and identical. The other three wrapped axes are `.xls` charts that **allow overlap**, which the reference's gate excludes outright; the nearest miss on the pitch is `023_Waterfall_Chart_Template`, whose pitch would have to be **4.0 % larger**. Limits: the census is our own pitch (bracketed per axis in §1.4) and the seven corpus extensions — the converted-ODF column was not swept. |
| O26 | **Which charts escape the block clip: none as a kind, and the unit is a mask.** The seat's own question is answered on both legs. The block clip **is** on the device's push/pop stack: `ScOutputData::PrePrintDrawingLayer` (`sc/source/ui/view/output3.cxx`:41-102) makes the printed cell block the paint window's redraw region, `SdrPageWindow::RedrawLayer` copies it into `DisplayInfo::SetRedrawArea`, and `ObjectContactOfPageView::DoProcessDisplay`'s `#114359#` hunk does `Push(vcl::PushFlags::CLIPREGION)` + `IntersectClipRegion` **once per layer, not per object** — while Calc itself sets no clip there at all (`printfun.cxx`:1650, and the `SetClipRegion` at :1681 is cleared at :1683). What discards it is **neither** reading the seat has had: an EMF's `MetaPopAction`s never reach the page device, because `MetafilePrimitive2D::create2DDecomposition` hands the metafile to `wmfemfhelper::interpretMetafile`, which turns its clip changes into `MaskPrimitive2D`. The device-level replace is `VclMetafileProcessor2D::processMaskPrimitive2D` (`vclmetafileprocessor2d.cxx`:2395-2453) — `ScopedPush(CLIPREGION)` then **`SetClipRegion`**, over a `maClipPolyPolygon` that starts empty and therefore never contains the block. **So no object kind escapes and none is safe**: on `DynamicBubbleChart` page 2 the same chart's axis strokes and text are cut at the block `(50.400, 122.995)-(431.291, 738.000)` while its bubble markers, each drawn inside a gradient mask of its own, are not — four of them clipped at x −76…28. Round 99's *"no chart in the corpus demonstrably escapes"* and its *"two `xdr:sp` shapes"* are both refuted on that document, whose drawing part holds one `graphicFrame` and one slicer-fallback `xdr:sp`; half of page 1's escaping ink is a **cell** background, which no drawing clip touches. There is therefore no predicate on `drawing.Chart` to write, which is what the seat was filed to settle. | `probes/chart-rest-r104` §1, `o26-clipstate.txt`, `o26-objects.tsv`. Of the seat's own 61 pages, **36 are a single ≤ 1 pt column at the block's edge** (2,864 px, and that is the base rate any `over_px` census sits against); of the other 25, 4 pages are the two documents holding an EMF (17,269 px), 2 are the chart (8,642) and 19 are a block-geometry difference (2,609). **2 of the 39 documents hold any vector media at all.** The permissive approximation r99 measured costs **+14.36 summed `\|ink\|%`**. |
| N30 | **Mask-level parity, which is what O26's residue really is.** 26.2.4.2 lets ink drawn inside a `MaskPrimitive2D` out of the block clip and cuts everything else at it. This tree emits no such mask for the ink in question — a bubble is one solid ellipse here and a clipped band gradient there, a metafile's inner clip is applied inside our own box rather than instead of the block — so there is nothing for a replace rule to apply to, and the rule cannot be measured without the masks first. | **6 pages, 3 documents, 25,911 px, 82.6 % of O26's own total** — `012`/`013` pages 1-2 and `DynamicBubbleChart` pages 1-2. `probes/chart-rest-r104` §1.5 |
| N1 | `wp14:sizeRelH`/`sizeRelV` unread | 1873 elements in 146 DOCX, **3** with a non-zero percentage |
| N2 | `MSO_CLR` system-colour branch | **0** corpus occurrences |
| N3 | Escher palette index 67 (button face) | 11 shapes, all form controls, which take no ink |
| N23 | A BIFF `TXO` formatting run's **posture** (the italic half of O33) | **0 of 367** applied runs in the 64 `.xls` state one, and 26.2.4.2's own resolved view of the nine shape-path workbooks holds **0** italic spans (`probes/sheet-wrap-r99` §4) |
| N4 | `FrameLayout` positions an anchored frame against `page.BodyArea` | **no** corpus document has a frame inside an indented text section |
| N5 | An `.xlsx` row with no `ht` and no `sheetFormatPr`: 12.800 pt from the reference, 13.777 from us | **no** corpus document reaches it |
| N6 | `c:layoutTarget val="inner"`; an axis `rot` of exactly ±90° | **0** corpus documents each |
| N7 | `NAS-Infrastructure-Roadmaps-Weather.pptx` as an ink defect — **refuted**. All 90 text records on its largest page sit at identical positions; the 3.52 % is a 0.13 % anisotropy seen through a 512 px instrument. | `probes/slides-ink-r94` |
| N8 | WMF raster op `0x7` (self-mask *and* invert the rectangle) — needs the destination back, and no corpus document reaches it | `probes/slides-ink-r94` |
| N10 | **Eight `cfRule` families with no corpus witness** — `beginsWith`, `notContainsText`, `uniqueValues`, `containsErrors`, `notContainsErrors`, `top10`, `aboveAverage`, `timePeriod`. The first three are one arm apiece of predicates already in `XlsxConditionalStyles`. | **0** rules each over 243 corpus `.xlsx`/`.xlsm`; `probes/cond-format-r96/cfrule-census.txt` |
| N11 | **A conditional-format rule count is not a reach figure.** 1215 newly-read rules move **3 of 947 renderings**: 358 search for `xxx` no cell holds, 663 sit on a `veryHidden` sheet or on hidden columns, 14 `duplicateValues` find no duplicate in 357 positions, and 66 `notContainsBlanks` hits are outside their printed block. The 27.04, 21.67 and 18.17 ink seats these were expected to take are **not** conditional formatting. | `probes/cond-format-r96` §3, `ink-nonmovers.tsv` |
| O12 | **A clipped shape's own text stays in the text layer** — the narrower question r93 left open, answered on a shape and not on cell text. 26.2.4.2 cuts an authored rectangle's fill at the block and writes its right-aligned word 51.9 pt past the clip anyway, on the paper, where `pdftotext` reads it; over the 947 banked renderings **150 pages in 61 documents, 5865 glyphs** of drawing-layer text lie wholly outside the drawing clip that governs them. **The change would have negative reach**: `ClipPath` in place of `ClipPathKeepingText` loses **3150 characters over 56 of the 74** renderings the clip touches and takes **8** outside the gate's `max(2%, 15)` glyph band while bringing 2 in — net **6 of 947** worse, and the 2 that improve do so because we over-draw glyphs there, which is a separate defect. The trap: **MuPDF's text extraction applies the clip and poppler's does not**, so the same question asked through `pymupdf` answers the opposite, crisply. | `probes/clip-seats-r97` §1; guard test `SheetStraddlingDrawingTests.TheBlockClipKeepsTheShapesOwnGlyphs` |
| O7 | **The six documents the clip "made worse" are not one seat and four of them were never worse.** Counted three ways at 120 dpi, the clip removed 43,154 px on `Template Pilot Logbook` p18 and 26.2.4.2 leaves **every one of them** blank; `SIL_TDB609`/`605` p6-p7 the same, `PC1000` p1 two pixels. Their `|ink|%` rose because a region's `luma_gap` is the **signed mean** brightness difference over that region (`pdf-image-diff.py`:246-272) and `|ink|%` takes the absolute value of each region's mean rather than the mean of the absolutes (`:434-437`), so removing ink we wrongly drew stopped cancelling ink we wrongly miss **inside the column this project ranks on**. Corpus-wide the clip removes **31,384 px** the reference keeps against **10,216,274 px** it still wrongly draws. The residue re-seats as O25 and O26. | `probes/clip-seats-r97` §2, `overreach.tsv` |
| N9 | An `.xlsx` whose `indexedColors` are written `ffRRGGBB`, where oox reads the top byte as transparency (`decodeIntegerHex_impl`) — modelling it faithfully took the one affected document from 20.15 to 68.17, so it is left | **1** corpus document; `probes/sheet-ink-r94` |
| N15 | The `bConsidered` asymmetry of O8 — a draw object with no text box that does not wrap through — is the only case where the old reading and the C++'s differ | **4** objects in 4 of the 272 DOCX, of 6055 positioned objects (`probes/words-close-r95/capture-census.txt`) |
| N16 | `caption` / `Caption` as an RTF style whose `\sbasedon` does not resolve | **0** of the 338 converted `.rtf` apply one (`probes/words-close-r95/standard-census.txt`) |
| N17 | `COLL_TEXT` as an *intermediate* — `List Indent` and the four other `COLL_TEXT`-parented names take *Text body*'s 7 pt below and 115 % line spacing, which `RtfStyleFormatting` cannot express | **0 of the 338 converted `.rtf`** apply any of them (`probes/rtf-style-r97/hasbyname-census.txt`); pinned by `AnIntermediateBelowTextBodyIsNotModelledYet` |
| N12 | tdf#123002's escape — a header- or footer-anchored object whose top has passed the area's bottom is returned unadjusted. Modelled because leaving it out clamps where the reference does not; **no fixture and no census can reach it**, since it is a property of a computed position rather than of the markup | `probes/words-close-r95` §1.6 |
| N13 | **The ODF 1.2 `style:map` spelling of a conditional format** (was O3) | **0 of the 307 converted `.ods`** state a table-cell `style:map` without a `calcext:conditional-format`, and **0 cells** in the 53 that state both fall outside a `calcext:target-range-address`. LibreOffice writes both from one `ScConditionalFormatList` (`xmlexprt.cxx`:4779-4800 and `xmlstyle.cxx`:700-810), so it cannot. *The "1 of 307" this replaces counted `style:map` on **number** styles.* `probes/ods-residue-r95` §2 |
| N18 | **The `CF` record's number-format block.** It changes what a cell *says* and this reader is the fill-and-font half; it is stepped over rather than read. | **0 of the 187** corpus `CF` records state one (`probes/biff-reader-r98/cf-census.tsv`, column 6) |
| N19 | **The `CF` record's border block**, and the check is at the reference rather than in the file. 13 of the 187 rules state one — twelve in `EHEST` and one in `TICAPCapability_Final` — but resolving every conditional style 26.2.4.2 writes for the five witnesses finds **exactly 12 carrying an `fo:border`, all of them `EHEST`'s**: the `TICAP` rule that states `font,border,area` comes back with a background colour and nothing else, so its block sentinels say unused and the reference applies no border from it either. All twelve of `EHEST`'s are `cellIs equal 2` over a blank checklist, so **no corpus cell takes a conditional border at 26.2.4.2**. It would also need a conditional-border layer in `SheetFormatting`, which does not exist. | `probes/biff-reader-r98` §6 |
| N20 | **A BIFF conditional format's reach is its matching cells, not its rule count** — N11 again, on the other reader. `EHEST-Pre-departure-checklist` states **120 `CONDFMT` and 144 `CF`, more than the other four witnesses together**, and reading them moves not one byte of its rendering: 126 of the 144 are `cellIs equal 2` over a blank checklist whose cells hold 0 or nothing, and the 18 `[.$E$103]>[.$P$110]` rules compare 0 against an empty cell. The control that separates *the reader found no match* from *the reader read nothing* is `patch-cf.py`: rewriting one rule's RPN from `tInt 2` to `tInt 0` makes **both** 26.2.4.2 and this tree paint the same fill on the same page, 0.05 `\|ink\|%` each. | `probes/biff-reader-r98` §2.3 |
| N21 | **A form control's label takes the *first* `TXO` run's font for the whole label**, where an ordinary text object takes every run — `XclImpTbxObjBase::ConvertFont` calls `WriteFontProperties(…, rFormatRuns.front().mnFontIdx)` (`xiescher.cxx`:2110-2119) against `lclCreateTextObject`'s per-run walk. Not modelled, and it cannot matter here. | **60 of 60** corpus `TXO` on a control object state only the opening run and its terminator — 54 buttons in `EHEST`, 6 in `PC1000` — so the two rules agree on every one (`probes/biff-reader-r98` §6) |
| N22 | **`XlsNameRanges.Read` folds a token's class as `opcode & 0x3F`**, which is not the fold — the base identifier is the low five bits with bit 5 put back, so a value-class `tArea3d` (`0x5B`) reads as `0x1B` and stops the walk. `XlsConditionFormula` (round 98) uses `(opcode & 0x1F) \| 0x20`. | **0 of 111** built-in `NAME` formulas over the 64 `.xls` state a value- or array-class token; all 111 open with `0x3B` (`probes/biff-reader-r98` §6) |
| ~~N14~~ | **Withdrawn — this was never nil reach. This row is where round 94's seat `O4` ("BIFF `CONDFMT` unread") ends, and it did not say so.** A BIFF8 conditional format cannot move a *row height* (`AdjustRowHeight()` inside an `#if 0`, `read.cxx`:1284-1288) and that half stands. The other half — *"reading the record anyway leaves 64 of 64 renderings byte-identical"* — was measured with a reader that parses `CONDFMT`, a count and a `SqRef`, and never reads the `CF` records that hold the rules, so it could not have moved a pixel. Re-seated as **O25** and closed there in round 98. *Its census was also short: the `CONDFMT` walk missed `EHEST-Pre-departure-checklist` entirely, so the corpus holds 149 formats and 187 conditions in **5** of 64 `.xls` rather than 29 and 43 in 4 (`probes/biff-reader-r98` §1).* *The witness note stands: `Special-Procedures_2025-07-10.xls` opens `PK\x03\x04` and was never BIFF.* | `probes/biff-condfmt-r97` |
| N24 | **Surface charts, in all four streams.** LibreOffice has no `SurfaceChartType` in either version — `template.Surface` is commented out (`ChartTypeManager.cxx`:223) and all three importers substitute `ColumnChartType` with the same `// Todo` (`typegroupconverter.cxx`:79, `xlchart.cxx`:469, `SchXMLTools.cxx`:148). This tree makes the same substitution. | **0 of 766** OOXML documents state `c:surfaceChart`/`c:surface3DChart`, **0 of 947** ODF renderings state `chart:class="chart:surface"`, **0** BIFF `CHSURFACE` in the 64 `.xls` or the 51 `.ppt`; `probes/chart-types-r101` §6 |
| N25 | **Six of the eight chartex chart types** — `cx:funnel`, `treemap`, `sunburst`, `waterfall`, `boxWhisker`, `regionMap`. All six exist in 26.2.4.2 (binary grep of `libmergedlo.so`), so `Funnel` is **not** new in 27.2. | **0 of 766** OOXML documents state one; the corpus's only chartex charts are the two Pareto workbooks of O36. `probes/chart-types-r101` §2.1, §6 |
| N26 | **Five BIFF chart-type discriminators `XlsChartReader` does not apply** — `CHPIE`'s `mnPieHole` (doughnut), `CHSCATTER`'s bubble flag, `CHLINE`'s stock condition, `CHRADARAREA` vs `CHRADARLINE`, and the two records absent from `BiffChartRecords` altogether, `CHSURFACE` 0x103F and `CHPIEEXT` 0x1061. | **0** witnesses each. The corpus holds 13 BIFF charts — 7 in `.xls`, 6 in `.ppt` OLE objects — and their type records are `CHBAR` ×8, `CHSCATTER.plain` ×5, `CHLINE` ×3, `CHAREA` ×2, `CHPIE.solid` ×1, with no `CHRADAR*`, `CHDROPBAR` or `CHCHARTLINE` anywhere. `probes/chart-types-r101` §3.3, §6 |
| N27 | **Histogram, any spelling** — `cx:clusteredColumn` + `cx:binning`, `chart:histogram`, `com.sun.star.chart2.HistogramChartType`. It is the one chart type of the 19 that **26.2.4.2 does not have**: no service, no template, no `HistogramDiagram`, no `binning` token anywhere in the install. The reference cannot draw one either. | **0** corpus witnesses and **0** in the reference binary; `probes/chart-types-r101` §2.1 |
| N28 | **Charts in `.doc`** — the whole MS-binary-Writer route. `Excel.Chart.8` (CLSID `00020821`) reaches the BIFF chart reader through `SvxMSDffManager::CheckForConvertToSOObj` (`msdffimp.cxx`:7130-7144); `MSGraph.Chart.8` (`00020803`) is absent from that table and 26.2.4.2 draws its stored replacement metafile instead — confirmed twice, by source and by the reference's own export failing on exactly the one corpus document holding MSGraph charts. | **0 of 66** `.doc`: no `00020821` and no `00020803` among any `.doc` `ObjectPool` storage CLSID, and 0 `.doc` in the reference's own resolution of the corpus. `probes/chart-types-r101` §1, §3.5, §6 |
| N29 | **3-D chart geometry, on all six corpus documents that hold one.** 26.2.4.2 draws the three-dimensional *scene* as a raster over the plot area and leaves the title, the axis labels, the legend keys and the wall as vectors and text around it — the three OOXML `c:pie3DChart` (791x602 and 1370x732 `DCTDecode`) and both BIFF `CH3D` bars (1528x656 `FlateDecode`, 1099x197 `DCTDecode`). Round 110 closes the sixth as well: the `.ppt` OLE 3-D pie r102 recorded as vector is Excel's stored replacement metafile, and the same chart loaded live rasterises like the rest — see O31. No vector geometry can match a raster and no path metric would read the difference honestly, so this is a scope decision, and there is now no corpus witness against which an implementation could be measured at all. | **6 of 6** raster once the OLE preview is removed; `probes/chart-smooth-r102` §2, `threed.tsv`, `probes/chart-3d-r110` |
| O49 | **`055_Project_timeline_with_milestones`' date axis is RIGHT, and the row that said otherwise is refuted — NOT WORK.** Round 111 recorded it as *"a `c:dateAx` whose range and step differ — 14 labels at 10-day steps against the reference's 38 at 30-day"*. It is C12. With the thirteen `<f ca="1">` elements of C20:C32 alone removed — at the cached 2023 serials **and** at the 2026 serials the reference itself recalculates to, so the freeze and not the year is the variable — 26.2.4.2 draws **exactly the fifteen upright 10-day labels this tree draws**, `5 Apr` to `23 Aug`, label for label, on both variants. (Ours is 15, not the 14 r111 counted.) **Its 85-character gate shortfall decomposes into +111 and −26 and neither half is a scale**: +111 is C12's extra labels, −26 is O50. **Its verdict cannot move from this seat** and is 960/1045 before and after round 112. Closing it means evaluating `TODAY()` *and* keeping the import-time cache, which is a spreadsheet seat and a strange one. | `probes/chart-axis-r112` §1, `freeze.tsv`, `freeze-055.py`. `ChartDateScaleTests.TheProjectTimelineAxisIsFifteenTicksTenDaysApart` pins the refutation. **Round 113 shipped the −26 (O50) and the row is now 960 → 934 of 1045, still a mismatch and still unmovable from here** — but on the frozen variants this tree and 26.2.4.2 now read 934 apiece, exact. |
| O98 | **~~An ODF cell's format kind is read from the owning element's name, so a number under an accounting format answers `Text`.~~ The reading is right and the consequence does not exist — REFUTED the same day it was seated, NIL REACH.** `OdsCellFormats.FormatKind` does map the `number:*-style` element's own local name where `XlsxCellFormats` takes `code.Sections[0].Kind`, and an accounting format's owner *is* a `number:text-style`, so an ODF numeric cell under one does answer `NumberFormatKind.Text`. What the seat asserted from there is wrong twice over. **There is no `ShowsHashesWhenTooNarrow`** — that name was invented while writing the seat; the property is `HasPlainNumberFormat`, and **the `###` rule does not consult it at all.** Its only consumer is `SheetTextLayout.Breaks`, which decides whether a *wrapping* cell holding a number line-breaks. Measured against 26.2.4.2 on three one-attribute variants of `features/sheet-odf-numfmt-sections.ods` — four-character columns; six-character columns with `wrapText` on every cell; and the same with the values widened to 1 234 567.89 so the accounting rendering cannot fit — **all six columns agree with the reference cell for cell and row height for row height on all three**, because `###` fires before the wrap can. The corpus case is real and plentiful and still shows nothing: **31 of the 307 converted `.ods` hold 102 690 numeric cells under a text-style owner, 7 documents and 75 402 of them wrapping.** The lesson is the round's, not the file's: *a seat written from reading the source and not from running it can name a property that does not exist and a consequence that does not follow — the probe cost eight minutes and the seat would have cost a round.* | `probes/odfnumfmt-r141/o98-wrap-census.tsv`, `o98-narrow-fixture.py`, `o98-wrap-fixture.py`, `o98-wrapbig-fixture.py` |
---

## Merging this file: audit for what you were NOT counting

This file is edited by every round in parallel, so it conflicts on almost every merge, and the
resolution is nearly always *keep both sides* — two rounds appending rows in the same place. Three
distinct ways that has gone wrong here, all found afterwards by someone else:

- **A seat closed by one round while another round's base still had it seated.** Keep-both then
  writes the row twice, once in *Fixed* and once in *Open — seated*. Happened to O68, O71, O72,
  O73 and O74. The stale copy reads as live work.
- **A refuted seat left in *Open — seated* stating its refuted mechanism as fact.** Round 128
  refuted O75, added a struck-through row to nil reach, and the original seated row survived the
  merge anyway — carrying a witness and a reach figure, so the next round would have implemented
  the change round 128 had already built, measured at zero effect and reverted.
- **Empty conflict markers committed and pushed.** Three lines of `<<<<<<< HEAD` / `=======` /
  `>>>>>>>` sat in the seated table for two merges. Nothing was between them, so nothing was lost
  and nothing looked wrong.

The third is the one that says what the check has to be. The audit run after those merges counted
seat numbers and looked for duplicates, and it was *right every time* — 80 seats, no duplicates —
because markers are not seat rows. **An audit that counts the thing you were thinking about cannot
see the thing you were not.** So after resolving a merge of this file, run all three:

```sh
git grep -n '^<<<<<<< \|^=======$\|^>>>>>>> ' -- .          # markers, anywhere in the tree
# every seat number appears exactly once, across ALL sections
# every row in "Open — seated" is still open — check none was closed or refuted elsewhere
```

The last of those is the one no script has caught yet: a seat is closed by *another branch*, so
nothing in your diff mentions it.

### Keep-both is wrong for `PROVENANCE.tsv`, and it cost 406 rows

The same *keep both sides* reflex applied to `PROVENANCE.tsv` is not harmless, because two rounds
that touch the same probe file both append a row for it, and concatenating the two sides then
stores that row twice. By the round-135 merge the file held **2519 rows for 2113 distinct paths —
406 redundant, 16 % of the file**, across 303 paths, one of them repeated three times. Every single
duplicate group was **byte-identical**, which is why no reader ever noticed and why nothing was
lost when they were collapsed.

It also hid a fourth failure mode of the audit above: the seat check reads `OPEN-ISSUES.md` and
says nothing about `PROVENANCE.tsv`, so a register growing 16 % of pure noise passed every merge.

**So: resolve a `PROVENANCE.tsv` conflict by keeping both sides and then dropping byte-identical
repeats — never by plain concatenation.** Rows that share a path but differ in any column are
*not* duplicates and must both be kept; there were zero of those, and that is the check:

```sh
awk -F'\t' 'NF{print $1}' dotnet/probes/PROVENANCE.tsv | sort | uniq -d   # paths appearing twice
# for each: if the whole rows are identical, one is redundant; if they differ, keep both
```

Do **not** reach for `provenance-index.py` to tidy this. Its `build()` regenerates only 1232 of the
1749 unique paths it should, so a rewrite destroys rows. Append and dedupe by hand.

### Seat numbers collide whenever rounds overlap, and they always will

O66, O69, O76, O78, O83 and O88 were each handed to two rounds at once, because the coordinator
read "next free" off the register while another round was already holding it. Renumbering at merge
works and has worked every time, but it is the coordinator's error, not the round's: **a round told
to take seat N should say so in its write-up, and expect to be renumbered.** O88 was given to
`agent/pagefield` after `agent/coverage` and `agent/ww8char` had both already been briefed on it;
coverage renumbered itself to O89-O94 and ww8char's row became O95 at merge.

## How to close an entry

1. **Fixed here** — cite the C++ rule, show the reach, show confinement by building both ways
   with `obj`/`bin` cleared per leg and comparing byte for byte under `SOURCE_DATE_EPOCH`, and
   move the row into *Fixed in this tree* rather than deleting it: the next round needs the
   mechanism as much as the previous one needed the seat.
2. **LibreOffice defect** — show that the reference is internally inconsistent or contradicts its
   own documented rule, and that our behaviour matches the branch that is right. Add it to the
   table at the top and to `CLAUDE.md` if it is a measurement hazard as well.
3. **Nil reach** — census the corpus and move it to the table above with the number.
